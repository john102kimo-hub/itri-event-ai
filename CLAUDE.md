# 這個專案的長期約定

朱朱（工研院公關）交代過、而且**每一次都適用**的幾件事。放在這裡是因為它們不屬於
任何一次的需求，卻每一次都要遵守；寫在對話裡會隨著對話一起消失。

## 1. 只用台灣的繁體中文

回覆、程式註解、文件、給記者看的每一個字，一律台灣慣用的繁體中文用語與語法。
朱朱的原話是「千萬不能出現簡體字好嗎」。

這件事**不靠 prompt 保證**。米亞送出去的每一段文字都會經過 `api/line.js` 的
`toTraditionalTW()` 強制轉換，新的出口也要接上去。理由見下一節。

## 2. 「絕對不能發生」的事，要擋在程式出口，不是寫在 prompt 裡

這個專案在同一個形狀上踩過四次：Markdown 符號、`[[NO_DATA]]` 標記、亂補新聞室
資料、簡體字。每一次的教訓都一樣——**prompt 是請求，不是保證**。模型照做九成九，
剩下那一次就是記者截圖傳給朱朱的那一次。

判準很簡單：這件事偶爾沒照做會不會出事？會，就寫成程式；不會（長度、語氣、要不要
用表情符號），才留給 prompt 或 `bot_memory`。

## 3. 米亞只有一種聲音

以後**任何要出聲的東西**——使用說明影片、活動預告、語音訊息——都用
`tools/guide-video/voice/` 裡的那一個聲音，不用再問、也不要另外挑語音。

朱朱在 Vidnoz 上挑的童聲，加工配方在 `tools/guide-video/voice/cute.py`，
怎麼用寫在同一個資料夾的 `README.md`。之前那版 edge-tts（`tools/guide-video/vo.py`）
被評為「太像機器人念稿」，只留作備援。

## 4. 影片一定要有聲音軌

LINE 對「只有影像軌的 mp4」是收下請求回 200、之後靜靜不顯示，伺服器 log 完全乾淨。
`test/test-guide-video.mjs` 會擋這件事。同一個道理再推廣一次：**「送出成功」不等於
「使用者看得到」**。

## 5. 更動的來龍去脈記在 `LINE-PLAN.md`

每一批改了什麼、為什麼這樣改、踩到什麼坑，都往那份文件後面接。下一個接手的人
（或下一次的我）需要的是「為什麼」，不是 git log 上的「改了什麼」。
