# AI 能見度追蹤（GEO）— 使用說明

後台側邊欄 **AI 能見度** → `https://itri-event-ai.vercel.app/geo`

---

## 一、同仁要做的事（只有這一件）

進 `/geo`，最上面那張綠框卡片：

1. **選一場活動** — 下拉會直接列出 ITRI EVENT AI 裡的活動，選了就自動綁定，日期和名稱不用重打
2. **打一個關鍵字** — 這場的主打技術或議題，例如 `固態電池`、`矽光子`、`碳捕捉`
3. **按「產生題目」** — 系統生 4 題，可以直接改文字、勾掉不要的
4. **按「開始追蹤這場活動」** — 存題目、建立追蹤、立刻先掃一次

結束。之後每天早上自動跑，回來看曲線就好。

> 題目裡不能出現「工研院」——一旦指名就是自問自答，分數會虛高。系統會強制擋掉。

---

## 二、上線設定（只要填 API 金鑰）

進 `/geo` → 最下面「設定」，會直接列出**還缺哪些金鑰、以及一步一步該點哪裡**。
照著做就好，這裡是同一份說明：

1. 開 [vercel.com/dashboard](https://vercel.com/dashboard) → 專案 `itri-event-ai`
2. 上方 **Settings** → 左側 **Environment Variables**
3. Key 填變數名，Value 貼金鑰，Environments 三個都勾，按 **Save**
4. 回 **Deployments** → 最新一筆 **⋯** → **Redeploy**
5. 回 `/geo` 按「重整」，灰掉的引擎就會亮起來

| 變數 | 說明 | 去哪拿 |
|---|---|---|
| `GEMINI_API_KEY` | 判官（打分）走它 | [aistudio.google.com/apikey](https://aistudio.google.com/apikey) |
| `OPENAI_API_KEY` | 想讓 ChatGPT 也入列才要填 | [platform.openai.com/api-keys](https://platform.openai.com/api-keys) |
| `PERPLEXITY_API_KEY` | 想讓 Perplexity 也入列才要填 | perplexity.ai/settings/api |

> ⚠️ **三家是三間不同公司、三個帳號、三筆帳單。** 在 Google 儲值只會開通 Gemini，
> 不會讓 ChatGPT 或 Claude 能用。每一家都要各自去它自己的平台開帳號、儲值、拿金鑰。
>
> **ChatGPT Plus／Claude Pro 訂閱不含 API 額度**——訂閱是給網頁版聊天用的，
> API 要在 platform.openai.com／console.anthropic.com 另外儲值。

`ANTHROPIC_API_KEY`、`ADMIN_PASSWORD`、Google 三把鑰匙沿用現有的，不用動。

**其餘都不用設。** 要問哪幾家 AI 在網頁「設定」裡點就好。

### 同仁專用連結

「設定」裡有一條 `…/geo?code=xxxx` 的連結，複製給同仁即可。

- 同仁**能**：開新追蹤、看曲線與建議、手動掃描
- 同仁**不能**：改設定、刪題目、看後台其他活動的問答資料，也拿不到後台密碼
- 連結外流就按「換一組」，舊連結立刻失效

Google 試算表的 `geo_prompts`／`geo_runs`／`geo_events`／`geo_settings` 四個分頁會自動建立。

排程每天 09:00（台北）跑一次、09:30 補掃一次。

**`CRON_SECRET` 必設。** 到 Vercel 環境變數新增一組隨機長字串（例如用密碼產生器隨便產一組），
設完記得 **Redeploy**。沒設的話，排程端點只能靠「User-Agent 是不是 vercel-cron」這種任何人
都能偽造的方式驗證，等於誰都能一行 curl 觸發全量重掃、燒光探測用的 API 額度。設定後不用改任何
程式碼，Vercel 排程本身會自動帶上這把密鑰。

---

## 三、看什麼

### 一頁報告 ← 要給主管看就印這個

右上「一頁報告」，或議題排行每一列下面的連結。內容：

1. **一句話結論** — 例如「這個議題上工研院排第一，但只領先陽明交大 1 次」
2. **誰是這個議題的答案** — 同一批問題、不指名任何單位，AI 各提到誰幾次，長條圖排出來
3. **三個數字** — 提到工研院的比例、被提到時排第一的比例、和最強對手的差距
4. **AI 實際上是怎麼講的** — 直接引用原話，包含一則「完全沒提到工研院」的對照
5. **下一步** — 具體要改什麼

按「列印／存 PDF」會只印報告本身，後台介面全部不會印出來。

報告刻意不用「能見度指數」「半衰期」這些詞——主管不需要學計分法，
他要看的是工研院在這個議題上的**話語權位置**，以及 AI 到底怎麼講我們。

### 現況快照 ← 掃完就有，不用等
最近一次掃描，各家 AI 各給幾分、幾題提到工研院、幾題引到自家網頁，加上診斷建議。
**這一段第一天就看得到。**

### 上面四個數字
近 14 天的能見度指數、提及率、自家網域引用率、累計樣本。

### 走勢圖
0–100 分，粗線是七日均線（看趨勢用），淡點是單日原始值，橘色虛線是活動日。
上方標籤可以切換只看某個議題。

**評分**：提及 45＋位置 20（第一個提到拿滿）＋引用到 `itri.org.tw` 20＋有具體內容 15。

### 事件效應表 ← 這張最有用

| 欄位 | 意思 |
|---|---|
| 事前基線 | 活動前 14 天均分 |
| 峰值 | 活動後 7 天內最高 |
| **半衰期** | 峰值掉回一半要幾天＝**這場替你留下幾天記憶** |
| **基線抬升** | 活動後 15–30 天均分 − 基線＝**真正沉澱下來的能見度** |

半衰期和基線抬升是潤利艾克曼給不了的數字——他們只有則數，這裡是留存天數。

**點任一列**會展開這場的診斷：哪裡在失分、為什麼、下次發稿具體要改什麼。
議題排行表也一樣可以點。

建議是規則算的，不是叫模型寫的——同樣的數字永遠給同樣的建議，每條都追得回是哪個數字觸發的。

---

## 四、什麼馬上有、什麼要等

| 你想知道 | 什麼時候有 |
|---|---|
| 現在 AI 怎麼回答、有沒有提到工研院、引誰的網頁 | **掃完就有**（現況快照） |
| 該改什麼（診斷建議） | **掃完就有** |
| 各議題的相對強弱（議題排行） | 掃完就有 |
| 趨勢曲線 | 累積幾天才看得出形狀，七日均線要 7 天 |
| **半衰期、基線抬升** | **必須事前累積，做不到回溯** |

**趨勢不能回溯的原因**：沒有任何地方保存「上個月 ChatGPT 怎麼回答固態電池」這種歷史，
只能從開始追蹤那天起一天一筆地量。這不是設計問題，是這類資料的本質。

所以大場的流程是：**發稿前兩週**開好追蹤 → 系統自己累積基線 → 活動後才算得出抬升。
臨時才開也不是不能用——現況快照與建議照樣有，只是那一場的「留下多少」算不出來。

### 不追了怎麼辦

事件效應表每一列右邊有垃圾桶：**停止追蹤**。該議題的題目會全部停用、不再掃也不再計費，
已經掃到的歷史資料保留。

---

## 五、成本

兩次呼叫：**探測**（要帶網路搜尋，貴）＋**判官**（純文字打分，便宜）。

**已實測：Gemini 免費層不提供 Google 搜尋接地**（定價頁寫 Not available，
實測接地一律回 429、純文字正常）。那 5,000 次／月免費接地要先綁帳單帳戶。

| 設定 | 每月概估 | 要儲值嗎 |
|---|---|---|
| 探測 Claude ＋ 判官 Gemini | 約 NT$450–900 | Anthropic 要 |
| 探測 Gemini ＋ 判官 Gemini | 儲值後 ≈NT$0（360／5,000 次免費額度內） | Google 要 |
| **加開 ChatGPT** | **再加約 NT$250–400** | OpenAI 要另外儲值 |

**加開 ChatGPT 的費用怎麼來的**：OpenAI 的網路搜尋工具是每 1,000 次 US$10，
外加每次搜尋固定計 8,000 個 input token。本工具每月約 360 次搜尋 ≈ US$3.6
＋ 約 288 萬 input token，合計大約 US$8–12。這是估算，實際以 OpenAI 帳單為準。

對照：Gemini 接地是每 1,000 次 US$14，但**每月前 5,000 次免費**，
所以 360 次落在免費額度內、實際不扣。這就是為什麼 Gemini 儲值一次之後幾乎不再花錢，
而 ChatGPT 是每個月都在燒。

判官走 Gemini 免費層已實測可用。控成本最快的方法是**到「設定」把用不到的引擎關掉、
把過期活動的題目停用**（停用的不掃、不計費）。

⚠️ Gemini 免費層的資料會被 Google 拿去改進產品。這裡送出去的只有公開新聞稿等級的問句，
可以接受——但別把未公開的東西寫進關鍵字。

---

## 六、疑難

| 症狀 | 處理 |
|---|---|
| 想確認有沒有跑通 | 右上「測 1 題」，實跑一題但不入庫，錯誤訊息會直接顯示 |
| 分數都很低、引用率 0% | 先按「測 1 題」看「來源網域」有沒有東西。空的代表搜尋沒生效，不是真的沒被引用 |
| 某家 AI 是灰的 | 那家的金鑰還沒填，填完 Redeploy |
| 半衰期顯示「未回落」 | 好事，還沒掉回一半 |
| 基線抬升顯示「觀察中」 | 活動後還沒滿 30 天 |
| Google 換模型名稱 | 到 AI Studio 看現在的 Flash 模型 ID，設 `GEMINI_MODEL` 即可 |

---

## 七、限制（對外引用前必讀）

量到的是**可用 API 的 AI 答案引擎**，不是 ChatGPT 或 Google AI Overviews 消費端畫面。
它是**代理指標**：趨勢與相對變化可用，絕對值不可宣稱等同某個產品。單次查詢有隨機性，
看趨勢一律以七日均線為準。
