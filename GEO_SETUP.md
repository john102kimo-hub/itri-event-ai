# AI 能見度追蹤（GEO）— 上線說明

後台側邊欄 **AI 能見度** → `https://itri-event-ai.vercel.app/geo`

---

## 一、它量什麼

用**不指名工研院**的問法去問 AI 答案引擎，每天記一次：

| 觀察項 | 配分 |
|---|---|
| 正文有提到工研院 | 45 |
| 是第幾個被提到的單位（1st=20 / 2nd=14 / 3rd=9 / 之後=5） | 20 |
| 引用來源裡有 `itri.org.tw`（含 i創） | 20 |
| 有講出具體內容（技術正式名稱／數字／案例），不是只出現在並列名單 | 15 |

合計 **0–100 能見度指數**。判斷由模型做「觀察」（有沒有提到、第幾個、有沒有具體內容），**分數由程式算**，所以同一段回答重跑分數一致。

> ⚠️ 這是**代理指標**：量的是可用 API 的 AI 答案引擎，不是 ChatGPT／Google AI Overviews 的消費端畫面。趨勢與相對變化可用，對外正式引用時務必註明此限制。

---

## 二、上線三步

**1. 環境變數**（Vercel → Settings → Environment Variables）

| 變數 | 必要 | 說明 |
|---|---|---|
| `CRON_SECRET` | ✅ | 自訂一組隨機字串，排程用 |
| `GEMINI_API_KEY` | 建議 | Google AI Studio 免費申請 |
| `GEO_ENGINES` | 建議 | 要掃哪些引擎，逗號分隔。**沒綁帳單就填 `claude`**（Gemini 免費層不給接地，見第四節） |
| `GEO_JUDGE_ENGINE` | 建議 | 填 `gemini`，打分走免費額度，這段確定免費 |
| `GEMINI_MODEL` | — | 預設 `gemini-3.6-flash` |
| `GEO_MODEL` | — | Claude 探測用，預設 `claude-opus-5` |
| `OPENAI_API_KEY` / `PERPLEXITY_API_KEY` | — | 有設、且列在 `GEO_ENGINES` 才會掃 |

`ANTHROPIC_API_KEY`、`ADMIN_PASSWORD`、Google 三把鑰匙沿用現有的，不用動。

> **不想綁信用卡的推薦設定**：`GEO_ENGINES=claude` ＋ `GEO_JUDGE_ENGINE=gemini` ＋ `GEMINI_API_KEY`。
> 探測走 Claude（付費、但有搜尋），判官走 Gemini 免費層，比全 Claude 省約三到四成。
> 綁了帳單帳戶之後改成 `GEO_ENGINES=gemini` 就幾乎不用錢——細節見第四節。

**2. 重新部署**（把本次檔案推上 GitHub → Vercel 自動 Deploy）
Google 試算表的 `geo_prompts`／`geo_runs`／`geo_events` 三個分頁會**自動建立**，不用手動開。

**3. 進 `/geo`，照這個順序按**

1. 展開「題庫」→ **帶入預設 12 題**
2. 右上 **測 1 題** ← **先按這個**。它會拿一題實跑到底但不入庫，把回答全文、來源網域、判官依據、分數全部攤開。
   - 看到分數與「來源網域」有東西 → 通了，往下走
   - 看到「沒有回傳任何來源」→ 接地沒生效，先檢查 `GEMINI_MODEL` 是不是還在支援清單上
   - 看到失敗訊息 → 照訊息修（多半是金鑰或模型名稱）
   - 順便看一下判官依據講得對不對，不對就調題目
3. 確認沒問題再按 **立即掃描**。第一次約 3–5 分鐘。

之後每天早上 09:00（台北）自動跑，另有 09:30 補掃一次。

> **Gemini 的來源只會存到網域，不會存完整網址。** Google 接地回傳的是
> `vertexaisearch.cloud.google.com/...` 轉址，真實網域藏在標題欄位裡。
> 判斷「有沒有引到 itri.org.tw」是靠那個網域欄位，功能正常；
> 但你沒辦法像 Claude 那條路一樣點進去看是哪一頁贏了引用位。
> 想追到「是 i創哪一頁被引用」，就要另外開 Claude 引擎跑。

---

## 三、日常怎麼用

**辦記者會前**：到「事件標記」把日期、名稱、關鍵字加進去（關鍵字要跟題庫的標籤一致）。
**辦完之後**：曲線圖會出現橘色虛線，事件效應表隔 30 天算出四個數字——

| 欄位 | 意思 | 拿去講什麼 |
|---|---|---|
| 事前基線 | 前 14 天均分 | 起跑點 |
| 峰值 | 事件後 7 天內最高 | 聲量高度 |
| **半衰期** | 峰值掉回一半要幾天 | **這場替你留下幾天記憶** |
| **基線抬升** | 事件後 15–30 天均分 − 基線 | **真正沉澱下來的能見度** |

半衰期和基線抬升是潤利艾克曼給不了的數字——他們只能給則數，這裡給的是留存天數。這就是這套工具在績效上的位置。

---

## 四、成本（2026-08-03 實測校正）

這套有兩次 API 呼叫：**探測**（要帶網路搜尋，貴）＋**判官**（純文字打分，便宜）。兩邊可以拆開走不同家。

**實測結論：Gemini 免費層不提供 Google 搜尋接地。** 定價頁對 Gemini 3.x 的
「Grounding with Google Search / Free tier」寫的是 **Not available**，實測每個模型的接地呼叫
都回 `429 RESOURCE_EXHAUSTED`（純文字呼叫同時是正常的）。那 5,000 次／月免費接地額度，
**要先綁定帳單帳戶**才拿得到。

| 設定 | 探測 | 判官 | 每月概估 | 要不要綁信用卡 |
|---|---|---|---|---|
| **A. 全 Gemini** | Gemini 接地 | Gemini | **≈NT$0**（360/5,000 次） | **要** |
| **B. 混搭（免綁卡最省）** | Claude 網路搜尋 | Gemini（免費） | 約 NT$450–900 | 不用 |
| C. 全 Claude | Claude | Claude | 約 NT$700–1,400 | 不用 |
| D. B 或 C 改 `GEO_MODEL=claude-sonnet-5` | | | 再降約 2/3 | 不用 |

**判官走 Gemini 免費層已實測可用**（純文字、不需接地額度），三種案例都判對，
包含「名字被埋在並列句裡、沒有具體內容」這種要扣分的狀況。所以 **B 是不綁卡情況下的最佳解**：

```
GEO_ENGINES=claude
GEO_JUDGE_ENGINE=gemini
GEMINI_API_KEY=...
```

想走 A 就到 Google Cloud 把專案綁上帳單帳戶，再設 `GEO_ENGINES=gemini`。
**綁卡後記得設預算警示**：每月 5,000 次以內免費，超過是每 1,000 次 US$14，
你 12 題／天約 360 次，離上限很遠，但警示還是要設。

⚠️ Gemini 免費層的資料**會被 Google 拿去改進產品**（付費層不會）。這裡送出去的只有公開新聞稿等級的
問句與回答，沒有內部資料，可以接受——但別把未公開的東西寫進題庫。

控成本最快的方法是**把題庫的題目停用**（停用的不掃、不計費），不是改頻率。建議常態留 8–12 題，記者會檔期前後再把相關主題打開。

### 想用 Claude 訂閱額度跑？

不行——**Claude Pro／Max 訂閱和 Anthropic API 是兩套帳，訂閱不含 API 額度**，
沒有官方途徑把會員額度接到 Vercel 後端。拿 Claude Code 的登入權杖去打 server 端 API 是違反使用條款的，不要做。

真正合法的替代路線是**在自己電腦上排程跑 `claude -p`**（headless Claude Code 目前仍計入訂閱池），
由它抓題庫、查完再把結果 POST 回 `/api/geo`。缺點：電腦要開著、多一個 ingest 端點、而且這條政策 Anthropic 曾預告要改（2026-06-15 那次暫停了，未來可能回來）。
除非 Gemini 免費額度不夠用，否則不建議繞這一圈。

---

## 五、題庫設計原則（來自 2026-08-02 那次 30 題實測）

- **題目裡不要出現「工研院」**——一旦指名就是自問自答，分數虛高。預設題庫只留 1 題指名題當對照組。
- 真正會掉分的是這三種問法：「這領域有哪些研發單位」「我想做○○可以找誰合作」「這個瓶頸誰在解」。題庫要壓在這一層。
- 一個主題出 2 題（一題找單位、一題找突破），比出 5 題同義句有用。

---

## 六、疑難

| 症狀 | 處理 |
|---|---|
| 掃描一直失敗 | 按右上「測 1 題」，錯誤訊息會直接顯示出來。多半是金鑰或模型名稱 |
| 分數都很低，自家網域引用率 0% | 先按「測 1 題」看「來源網域」有沒有東西。空的代表接地沒生效，不是工研院真的沒被引用 |
| Google 換了模型名稱 | 到 AI Studio 看現在的 Flash 模型 ID，改 `GEMINI_MODEL` 就好，不用動程式 |
| 曲線很跳 | 正常，單次查詢本來就有隨機性。看**七日均線**（粗線），別看淡色原始點 |
| 半衰期顯示「未回落」 | 好事，代表還沒掉回一半；也可能是樣本天數不夠 |
| 基線抬升顯示「觀察中」 | 事件後還沒滿 30 天 |
| 排程沒跑 | 確認 `CRON_SECRET` 有設，且 Vercel → Settings → Cron Jobs 看得到兩筆 |
