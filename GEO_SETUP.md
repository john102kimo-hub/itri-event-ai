# AI 能見度追蹤（GEO）— 使用說明

後台側邊欄 **AI 能見度** → `https://itri-event-ai.vercel.app/geo`

---

## 一、同仁要做的事（只有這一件）

進 `/geo`，最上面那張綠框卡片：

1. **選一場活動** — 下拉會直接列出 ITRI EVENT AI 裡的活動，選了就自動綁定，日期和名稱不用重打
2. **打一個關鍵字** — 這場的主打技術或議題，例如 `固態電池`、`矽光子`、`碳捕捉`
3. **按「產生題目」** — 系統生 4 題，可以直接改文字、勾掉不要的
4. **按「開始追蹤這場活動」** — 存題目、建立追蹤、立刻先掃一次

結束。之後每天早上自動跑，回來看曲線就好。

> 題目裡不能出現「工研院」——一旦指名就是自問自答，分數會虛高。系統會強制擋掉。

---

## 二、上線設定（只要填 API 金鑰）

Vercel → Settings → Environment Variables：

| 變數 | 說明 |
|---|---|
| `GEMINI_API_KEY` | Google AI Studio 免費申請。判官（打分）走它，這段免費 |
| `OPENAI_API_KEY` | 想讓 ChatGPT 也入列才要填 |
| `PERPLEXITY_API_KEY` | 想讓 Perplexity 也入列才要填 |

`ANTHROPIC_API_KEY`、`ADMIN_PASSWORD`、Google 三把鑰匙沿用現有的，不用動。

**其餘都不用設。** 要問哪幾家 AI、誰來打分，全部在網頁「設定」裡點——
沒填金鑰的引擎會是灰的，填了金鑰 Redeploy 後就能打開。

Google 試算表的 `geo_prompts`／`geo_runs`／`geo_events`／`geo_settings` 四個分頁會自動建立。

排程每天 09:00（台北）跑一次、09:30 補掃一次，不需要額外設定。
（想更嚴謹可自行加 `CRON_SECRET`，不加也能運作。）

---

## 三、看什麼

### 上面四個數字
近 14 天的能見度指數、提及率、自家網域引用率、累計樣本。

### 走勢圖
0–100 分，粗線是七日均線（看趨勢用），淡點是單日原始值，橘色虛線是活動日。
上方標籤可以切換只看某個議題。

**評分**：提及 45＋位置 20（第一個提到拿滿）＋引用到 `itri.org.tw` 20＋有具體內容 15。

### 事件效應表 ← 這張最有用

| 欄位 | 意思 |
|---|---|
| 事前基線 | 活動前 14 天均分 |
| 峰值 | 活動後 7 天內最高 |
| **半衰期** | 峰值掉回一半要幾天＝**這場替你留下幾天記憶** |
| **基線抬升** | 活動後 15–30 天均分 − 基線＝**真正沉澱下來的能見度** |

半衰期和基線抬升是潤利艾克曼給不了的數字——他們只有則數，這裡是留存天數。

**點任一列**會展開這場的診斷：哪裡在失分、為什麼、下次發稿具體要改什麼。
議題排行表也一樣可以點。

建議是規則算的，不是叫模型寫的——同樣的數字永遠給同樣的建議，每條都追得回是哪個數字觸發的。

---

## 四、重要：要在活動前兩週就開始追蹤

沒有事前基線，就算不出抬升——你只會看到一根衝高的線，卻不知道它比原本高多少。

所以大場的流程是：**發稿前兩週**開好追蹤 → 系統自己累積基線 → 活動後看抬升。

---

## 五、成本

兩次呼叫：**探測**（要帶網路搜尋，貴）＋**判官**（純文字打分，便宜）。

**已實測：Gemini 免費層不提供 Google 搜尋接地**（定價頁寫 Not available，
實測接地一律回 429、純文字正常）。那 5,000 次／月免費接地要先綁帳單帳戶。

| 設定 | 每月概估 | 綁信用卡 |
|---|---|---|
| 探測 Claude ＋ 判官 Gemini（**預設**） | 約 NT$450–900 | 不用 |
| 探測 Gemini ＋ 判官 Gemini | ≈NT$0 | 要 |
| 加開 ChatGPT／Perplexity | 各自再往上加 | 依各家 |

判官走 Gemini 免費層已實測可用。控成本最快的方法是**到「設定」把用不到的引擎關掉、
把過期活動的題目停用**（停用的不掃、不計費）。

⚠️ Gemini 免費層的資料會被 Google 拿去改進產品。這裡送出去的只有公開新聞稿等級的問句，
可以接受——但別把未公開的東西寫進關鍵字。

---

## 六、疑難

| 症狀 | 處理 |
|---|---|
| 想確認有沒有跑通 | 右上「測 1 題」，實跑一題但不入庫，錯誤訊息會直接顯示 |
| 分數都很低、引用率 0% | 先按「測 1 題」看「來源網域」有沒有東西。空的代表搜尋沒生效，不是真的沒被引用 |
| 某家 AI 是灰的 | 那家的金鑰還沒填，填完 Redeploy |
| 半衰期顯示「未回落」 | 好事，還沒掉回一半 |
| 基線抬升顯示「觀察中」 | 活動後還沒滿 30 天 |
| Google 換模型名稱 | 到 AI Studio 看現在的 Flash 模型 ID，設 `GEMINI_MODEL` 即可 |

---

## 七、限制（對外引用前必讀）

量到的是**可用 API 的 AI 答案引擎**，不是 ChatGPT 或 Google AI Overviews 消費端畫面。
它是**代理指標**：趨勢與相對變化可用，絕對值不可宣稱等同某個產品。單次查詢有隨機性，
看趨勢一律以七日均線為準。
