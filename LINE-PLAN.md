# 活動行事曆 × LINE 媒體窗口 — 施工規格（給接手的 AI）

> **狀態：規劃完成、尚未動工。** 建立於 2026-08-19，程式基準 `d0a6ee6`。
>
> 給誰讀：Sonnet 5，或任何接手實作的 AI。**動工前把第 0～3 節讀完**——第 3 節那七個坑
> 每一個都會讓你整支重寫。行號一律不要照抄，自己 grep 現行程式碼。
>
> 這份文件的定位跟 [FIX-PLAN.md](FIX-PLAN.md) 不同：那份是已完成的紀錄，這份是待辦。

---

## 0. 雙軌定位（先把「誰負責什麼」釘死）

| | 網頁 AI 問答（現有） | LINE 官方帳號（新增） |
|---|---|---|
| 場景 | **記者會現場與會後 72 小時** | **平時，沒有活動的日子** |
| 深度 | 深。整份新聞稿、技術資料、圖檔下載 | 淺。查得到、找得到人、問得出基本資料 |
| 入口 | 該場專屬連結／QR | 加一次好友，長期留著 |
| 主力問題 | 「這項技術的量產時程是什麼？」 | 「你們最近有什麼活動？」「我想採訪固態電池團隊」 |
| 不做 | 不處理邀訪 | 不取代網頁版的深度問答，答不動就給連結 |

**LINE 不是網頁版的行動版，是它的常設前廳。** 記者在 LINE 問到需要深入的東西，正確行為是給出該場的 `/event?id=` 連結，而不是在 LINE 裡把新聞稿全文倒出來。

---

## 1. 「深度同步」的正解：不要同步

使用者的原話是「兩邊要深度同步」。**做同步是錯的路**——兩份資料 + 一個對帳機制，最後一定會不一致，而且不一致的那天你不會知道。

**正解：只有一份資料。** 後台行事曆、網頁問答、LINE 回答，讀的是 Google Sheet 裡**同一張 `events` 表的同一批列**。沒有「兩邊」，所以沒有同步問題。

這也是為什麼行事曆必須先做（見第 5 節批次順序）：

> **行事曆不是新功能，是 `events` 表長出一個他每天都會用的介面。**

而它順帶解決了 LINE 最難的技術問題——上一版規格裡我設計了一個「AI 自動生成活動摘要卡」給跨場次路由當索引，那東西的問題是**沒人維護就會爛掉**。行事曆取代了它：使用者為了自己的工作會天天維護行事曆，路由索引因此永遠是新的。一件事解決兩個問題。

---

## 2. 資料模型（兩個正交維度，不要混在一起）

### 維度一：可見性 = `status`（三態，取代現有兩態）

| 值 | 意義 | 網頁 `/event?id=` | LINE 行事曆查詢 | LINE 深度問答 |
|---|---|---|---|---|
| `draft` | 已排定但**不對外** | **404** | **完全不可見** | 不可見 |
| `active` | 對外 | 正常 | 列得出來 | 看維度二 |
| `archived` | 已結束 | 存檔頁 | 列得出來（標「已結束」） | 看維度二，回答要加時間戳警語 |

- **新增活動的預設值是 `draft`**，使用者明確按「發布」才轉 `active`。行事曆一旦對記者可見，「這個月有幾場」就等於預告工研院的發布節奏，預設必須是不對外。
- **舊資料遷移**：現有只有 `active`/`archived`（空值視同 `active`）。遷移時**一律不要自動判成 `draft`**——寧可漏判成 `active`（現況本來就是如此，不是新風險），也不要把已上線場次誤關成 404。
- ⚠️ `api/chat.js`、`api/events.js`、`api/event-page.js`、`api/training.js`、`api/geo.js` 都有讀這欄，**要一起改並逐支確認**。漏掉 `event-page.js` 會讓 draft 場次的網頁還開得起來，等於防了 LINE 沒防網頁。

### 維度二：回答深度 = `knowledge_base` 有沒有內容（不需要新欄位）

| 狀況 | LINE 能回什麼 |
|---|---|
| `knowledge_base` 有內容 | 深度問答（走現有 `api/chat.js` 那套 prompt） |
| `knowledge_base` 空 | 只回基本資料：日期、時間、地點、性質、新聞聯絡人，並說明「詳細資料將於活動當日提供」 |
| `event_date` 在未來 | **即使有 kb 也只回基本資料**——未辦的場次不得預先釋出內容 |

這兩個維度**正交**：`active` + 空 kb = 記者知道有這場但問不到內容，這是正常且正確的狀態。

### `events` 表要新增的欄位

> **批次 1 實作時發現規劃有誤，以下是實際上線的版本（已上線，2026-08-20 部署）：**
> `event_date` 不是新欄——F 欄雖然標題還叫 `created_at`，但 `api/events.js` 早就把它當活動日期在讀寫（`create`/`update` 的 `event_date` 參數就是寫這欄）。所以只新增了 **L–O 四欄**，不是五欄，`event_date` 沒有另外佔欄位。下面是真的欄位對應，之後接 LINE 就照這個：

| 欄 | 名稱 | 說明 |
|---|---|---|
| F | `event_date`（別名，實體欄名仍是 `created_at`） | 活動日期。API 回應裡 `event_date` 與 `created_at` 兩個 key 同時存在、值相同，新程式碼一律讀 `event_date` |
| L | `event_time` | 時間字串，如 `10:00`。空著就不顯示 |
| M | `venue` | 地點 |
| N | `event_type` | `記者會` / `發表會` / `論壇` / `參訪` 等，自由文字，給行事曆分色與 LINE 回答用 |
| O | `press_contact` | 該場新聞聯絡人姓名 + 分機。**答不出來時要給的就是這個**，比什麼都重要 |

- `event_date` 為空的舊資料：行事曆歸到「未排定日期」區，LINE 的月份查詢跳過它，**不要猜**。
- 不要新增「摘要」欄。行事曆的 `name` + `event_type` + `event_date` 已經足夠當路由索引，多一欄就多一個沒人維護的欄位。
- `api/events.js` 已有 `action=list_admin`（後台密碼）與預設列表（公開，不含 draft）兩個現成端點，兩者都已附上述欄位 + `has_kb` 布林值。**批次 2/3 直接讀這兩個既有 action，不要再開新的。**

---

## 3. 七個會害你重做的坑（必讀）

### 坑 1：raw body —— LINE 簽章驗證唯一的正解

LINE 的 `x-line-signature` 是對**原始 bytes** 做 HMAC-SHA256 再 Base64。

Vercel 的 `req.body` 是一個 **JavaScript getter，一旦存取就會把 stream 讀掉**，之後你再也拿不到原始 bytes。而 `JSON.stringify(req.body)` 產生的字串**不保證**與 LINE 送來的原文逐 byte 相同（空白、跳脫、欄位順序都可能不同），簽章會時好時壞——這種 bug 最難查，因為它平常會過。

**做法：整支 `api/line.js` 從頭到尾不要碰 `req.body`。** 自己讀 stream：

```js
function readRawBody(req) {
  return new Promise((resolve, reject) => {
    const chunks = [];
    req.on('data', (c) => chunks.push(c));
    req.on('end', () => resolve(Buffer.concat(chunks)));
    req.on('error', reject);
  });
}

// handler 內：
const raw = await readRawBody(req);                    // Buffer，先驗簽章
const expected = createHmac('sha256', process.env.LINE_CHANNEL_SECRET)
  .update(raw).digest('base64');
if (req.headers['x-line-signature'] !== expected) return res.status(401).end();
const payload = JSON.parse(raw.toString('utf8'));      // 驗完才自己 parse
```

簽章比對用 `crypto.timingSafeEqual`（長度先比對，避免 throw）。
**簽章沒過一律 401 並直接結束，不要做記 log 之外的任何動作**——這支是全公開端點，沒有這道就是開放的 LLM 代理。

### 坑 2：Function 額度只剩 2 格，全程只能用掉 1 格

Vercel Hobby 上限 12 支，`api/` 底下每個 `.js` 都算一支。目前 10 支。

- 行事曆**不准開新 API**——用現有 `api/events.js` 加參數就好。
- 邀訪收單**不准開新 API**——寫在 `api/line.js` 裡。
- 共用邏輯一律放根目錄 `lib/`（不佔額度）。

做完全部四批應該是 **11/12**。如果你發現自己要開第 13 支，停下來重看 [SETUP.md](SETUP.md) 最後一節的合併 + rewrites 分流做法。

### 坑 3：reply token 只活 60 秒，而且只能用一次

- 主路徑：webhook → 驗簽 → `POST /v2/bot/chat/loading/start`（loadingSeconds 60，讓記者看到輸入中動畫）→ 呼叫 Anthropic → `POST /v2/bot/message/reply`。**reply 不計費、無則數上限。**
- 意圖路由會多一次 API 呼叫（+1～2 秒）。路由那段務必用 Haiku、`max_tokens` 壓在 200 以內。
- **一定要寫 fallback**：reply 回非 2xx（多半是 `Invalid reply token`）時改用 `POST /v2/bot/message/push`。push 會計費，但輕用量方案每月免費 200 則，記者量級用不完；重點是記者不會「問了沒下文」。
- `vercel.json` 幫 `api/line.js` 加 `"maxDuration": 60`（照 `api/chat.js` 的寫法）。

### 坑 4：Google Sheets 配額會被 LINE 放大

服務帳號限制是**每分鐘讀 60 / 寫 60，全平台共用**（含後台、GEO 排程、露出上傳）。

**做法**：`events` 全表與 `line_users` 在冷啟動時一次讀進記憶體，TTL 60 秒，比照 `api/chat.js` 現有的 `eventCache` 寫法。只有「綁定活動」「填寫媒體名稱」「邀訪收單」三個動作才寫 Sheets。

### 坑 5：未發布場次外洩

LINE 統一窗口會讀「所有場次」。**`events` 表裡一定會有草稿場次**（後台開好框架、同仁還在填）。`status` 三態（第 2 節）**必須在 LINE 路由上線之前完成**，這是硬性前置條件，不是「順便做」。

驗收要當成資安測試在做：拿 draft 場次的活動全名去問，必須答「沒有相關場次」。

### 坑 6：跨場次歸納會生出工研院沒說過的話

記者一定會問「工研院今年在半導體發表了幾項技術？」「這跟去年那場比如何？」。AI 一開始跨場總結，就是在替主辦單位發言。prompt 必須明文禁止（第 6 節），只能回「以下這幾場提到」+ 分場引述原文。

### 坑 7：不要用向量資料庫

看到「跨多場次檢索」就想裝 embedding + vector DB 是錯的路。場次是幾十到幾百不是幾萬；行事曆清單一次全塞進 prompt 就能選得準，而且更會處理「上個月那場醫療的」這種時間＋主題混合的模糊指涉。引入向量庫等於引入外部服務 + npm 依賴 + 一份會過期的索引，違反本專案「零依賴、單一資料來源」的核心約束。

---

## 4. LINE 的四類意圖（一次路由全部分完，不要串接多次呼叫）

第一段路由用 Haiku 做**一次**呼叫，同時完成「意圖分類」與「選場」，回 JSON：

```json
{ "intent": "calendar|qa|interview|other", "event_ids": ["..."], "confidence": "high|low" }
```

輸入只給**行事曆清單**（`id｜名稱｜日期｜性質｜有無詳細資料`，一場約 40 tokens，200 場也才 8000 tokens），不給知識庫全文。這份清單逐 byte 穩定，套 `cache_control: ephemeral` 後幾乎不花錢（`api/chat.js` 已在用這招，照抄）。

### intent: `calendar` —— 活動查詢

「這個月有哪些活動」「下週有沒有記者會」「你們今年辦過哪些發表會」

- 直接用行事曆資料組回覆，**不呼叫第二段**，快又零成本
- 格式：日期 + 名稱 + 性質 + 一個標記（`可線上問答` / `基本資料`），最多列 8 筆，超過就說「還有 N 場，可以縮小範圍再問」
- 未來場次與過去場次分開列，不要混在一起

### intent: `qa` —— 特定活動的問答

- 有 kb 且 `event_date` 已過 → 走第二段，跟網頁版同一套 prompt
- 沒有 kb 或活動還沒辦 → 回基本資料 + `press_contact`，並附該場 `/event?id=` 連結（若已發布）
- 路由選不出場次 → 列最近 5 場讓記者用 quick reply 挑，**不要呼叫第二段**

### intent: `interview` —— 邀訪／聯絡（這是 LINE 平時最有價值的用途）

「我想採訪你們固態電池的團隊」「有沒有專家可以談 AI 晶片？」

**AI 只做收單，不做任何承諾與安排。** 流程：

1. 追問缺的欄位（媒體、記者姓名、採訪主題、期望時間、截稿時間），一次問一個，不要一口氣丟五個問題
2. 湊齊後寫進新分頁 `media_requests`
3. 回：「已收到您的採訪需求，新聞聯絡人會在**一個工作天內**與您聯繫。急件請直接來電 ○○○○。」
4. **同時 push 一則通知到使用者自己的 LINE**（把自己的 userId 存成環境變數 `LINE_ADMIN_USER_ID`），否則收了單沒人知道

> ⚠️ 這一項把 LINE 從「查資料」變成「業務入口」。有人問了就會期待回覆，**三天沒回比沒有這個功能更傷**。上線前使用者要先想清楚自己接不接得住，接不住就先不要開這個 intent。

### intent: `other` —— 其他

閒聊、立場評論、政治議題、與其他機構比較、未公開的財務或合作條件 → 婉拒 + 引導 + 提供新聞聯絡人，**不呼叫第二段**。

### 新增的 Sheets 分頁

用 `lib/sheets.js` 現成的 `ensureSheets()` 自動建立，不要叫使用者手動開。

**`media_requests`**：`timestamp｜line_user_id｜media_name｜reporter_name｜topic｜preferred_time｜deadline｜status｜note`
（`status` 預設 `new`，使用者在 Sheet 裡自己改成 `contacted` / `done`）

**`line_users`**：`line_user_id｜event_id｜media_name｜bound_at｜last_active｜note`

- `event_id` 是記者會現場掃 QR 綁的場次，**必須有 6 小時 TTL**——沒有這條，記者三個月後問別場會被鎖在當初掃碼那場，而且他不會知道為什麼，只覺得這 bot 壞了。用 `bound_at` 跟現在時間比對即可，不需要排程清理。
- 多輪對話**不要寫進 Sheets**。Function 記憶體保留最近 3 輪、TTL 10 分鐘，冷啟動掉了就退化成單輪。不值得為此引入 Redis／KV。

---

## 5. 施工批次（順序很重要，不要跳）

### 批次 1：後台活動行事曆 ✅ 已完成並上線（2026-08-20）

實際做法跟規劃有兩處出入（已在第 2 節訂正），記錄在這裡供批次 2 銜接：

- `status` 實際是**四態**：`draft`／`active`／`ended`／`archived`，不是規劃時想的三態——`ended` 是既有狀態（觸發新聞稿全文開放給 AI 搜尋引擎收錄），跟 `draft`（未發布）語意不同，兩個都要保留。`draft` 一律視同不存在（404／不列入），`ended` 仍完全可問答，只是多了「已對外索引」的含義。
- 新增的是 **L–O 四欄**（`event_time`/`venue`/`event_type`/`press_contact`），不是 L–P 五欄——`event_date` 沿用既有 F 欄，見第 2 節。
- `api/events.js` 新增了 `action=list_admin`（後台密碼保護，含 draft/archived + `has_kb`）；預設列表（公開）已排除 `draft`。
- `public/index.html` 有「活動行事曆」月曆視圖（側邊欄新分頁），卡片與月曆都有「發布／收回草稿」操作。
- 已驗收：draft 場次 `/event?id=` 404、發布後恢復正常、舊資料沒有欄位也不報錯、現有五個功能（列表/編輯/訓練/露出/GEO）沒壞。

**LINE 批次要用到 `status` 時，記得判斷式是「`draft` 才擋，`ended` 不擋」**，不要誤把 `ended` 當成要排除的狀態。

### 批次 2：LINE webhook 跑通 ✅ 程式碼已完成、待人類設定 LINE 帳號才能實測（2026-08-20）

已實作，`node --check` + 兩套獨立測試全過（簽章驗證單元測試 6 項、把 Sheets／Anthropic／LINE API
全部攔截掉的端對端整合測試 26 項，涵蓋未綁定擋 Anthropic、亂代碼、**draft 場次代碼一樣綁不進去**、
正確綁定、媒體名稱擷取、正式問答寫入 `qa_log(source=line)`、TTL 過期、限流、亂簽章 401、LINE Verify
空事件陣列）。**沒有打過真的 LINE 伺服器**——這步驟需要人類先完成第 7 節的帳號設定，見下方待辦。

實作內容：

- `api/line.js`：raw body → 驗簽 → `follow`／`message.text` → `#代碼` 綁定 → 問答 → reply（失敗 fallback push）→ 寫 `qa_log`
- `lib/line.js`：`replyMessage()` / `pushMessage()` / `replyOrPush()` / `startLoading()` + 簽章驗證
- `lib/prompt.js`：從 `api/chat.js` 抽出 `buildSystemPrompt()` / `formatImages()`，`api/chat.js` 改呼叫共用版——已用逐 byte 比對測試證明搬移後輸出完全相同
- `qa_log` **H 欄新增 `source`**（`web` / `line`），`api/chat.js`／`api/line.js` 兩邊寫入時 G 欄都補空字串佔位；`api/analytics.js` 讀取範圍跟著改 `A2:H`，`by_event` 多一個 `line_count`；`report.html` 的活動卡片有 LINE 問答時才多顯示一格「來自 LINE」

跟原規劃有一處**刻意偏離**：`line_users` 的 `bound_at`／`last_active` 沒有照原規劃存成人看得懂的
在地化時間字串，改存 **epoch 毫秒數字**。原因：6 小時 TTL 要拿這欄跟 `Date.now()` 做數學比較，
而 `new Date("2026/8/20 下午2:30:00")` 這種格式在 Node 不保證能解析回來，會讓 TTL 判斷整個失準。
要看人看得懂的時間，`qa_log` 的 timestamp 欄本來就有。

- **驗收**（待人類完成第 7 節設定後執行）：Webhook Verify 成功；亂簽章回 401 且無 Anthropic 呼叫；掃 QR 綁定成功；問資料裡沒有的題目要回「我沒有資料，建議洽新聞聯絡人」而不是編；手動把 `bound_at` 改成 7 小時前的 epoch 毫秒數，再問要走引導而非沿用舊場次；`qa_log` 有列且 `source=line`

### 批次 3：意圖路由 ✅ 已完成兩類意圖，`interview` 未做（2026-08-27）

實際只做了 `calendar`／`qa` 兩種意圖，範圍比原規劃小——起因是使用者直接反饋「誰會知道代碼」，
要的是「打活動名稱就能問」跟「沒綁定也能問『最近有哪些活動』」，不是完整四類意圖。
`interview`（邀訪收單）跟 `other` 的獨立分支**沒有做**：`media_requests`／`LINE_ADMIN_USER_ID`
推播通知都還沒有，`other` 目前併在同一句引導文案裡，沒有獨立的婉拒文案。這兩塊留給未來
使用者明確要「邀訪」這個功能時再做——見第 8 節「上線前使用者要先想清楚自己接不接得住」。

- `lib/router.js`：`buildCalendarCards()` 組行事曆清單、`routeIntent()` 一次 Haiku 呼叫回
  `{intent, event_ids, confidence}`、`formatCalendarReply()` 純函式組活動列表（不呼叫 Anthropic）
- `api/line.js`：
  - `#代碼` 對不上時不再回死板的「找不到」，改把 `#` 拿掉當一般文字重新路由一次
  - 沒有綁定時導向 `handleUnbound()`：`calendar` 意圖直接回列表；`qa` 單一高信心命中直接
    回答並**順手軟綁定**（沿用既有 6 小時 TTL，下一題不用再重打名稱）；多候選列出來讓記者選；
    其餘一律安全退回引導文案
  - 問答邏輯抽成 `answerQuestion()` 共用函式，`#代碼` 綁定路徑與路由命中路徑都呼叫同一份，
    不會兩邊各自維護一份、之後改一邊忘了改另一邊
- 行事曆清單沿用批次 2 既有的 `getAllEventRows()` 60 秒快取，沒有另外開一份（坑 4）
- **測試**：新增 30 項端對端整合測試（`verify-line-router.mjs`），涵蓋 calendar 查詢排除
  draft/archived、打全名直接命中並軟綁定、多候選列表、`#`代碼對不上退回路由、**draft／archived
  場次無論路由怎麼回都不能被真正回答／不能軟綁定成功**（資安關鍵項，刻意模擬路由「誤判」
  也要驗證 `isUsable()` 擋得住）、路由本身失敗時優雅退回不崩潰。批次 2 原有測試同步更新
  兩處已被取代的舊斷言（「代碼找不到」的固定文案已不成立），全數重跑通過。
- **驗收**（已用 mock 測試涵蓋，尚未打過真實 LINE／Anthropic）：
  1. 全新使用者問「這個月有哪些活動」→ 列得出來，且不含任何 draft／archived 場次 ✅（測試涵蓋）
  2. 拿 draft 場次的活動全名去問 → 不得出現該場任何內容、不得軟綁定成功 ✅（測試涵蓋，含路由誤判情境）
  3. `#代碼` 打錯（其實在打名稱）→ 退回路由，答得出來而不是卡死在「找不到」 ✅（測試涵蓋）
  4. 全流程含路由的實際延遲、跨場次歸納題的防線（第 6 節 prompt 規則）—— **這兩項尚未在真實環境測過**，等使用者實機測試回報

### 批次 4：內部職員模式 ✅ 已完成（2026-08-27）

使用者原話：「主管/同仁可以輸入暗號後，可以進一步問 GEO 狀況、每場活動後台數據、甚至直接
調動開新活動（同仁專屬編輯連結，不用跟我要）、媒體訓練」。同一個 LINE 帳號、同一支 webhook，
講對密語後永久記為職員，之後全部用自然語言下指令，不用記兩套語法。

**安全模型（這批的重點，不是隨手加一個 if 判斷）**：
- 密語存 `LINE_STAFF_PASSCODE` 環境變數，不寫死在程式碼裡
- 這是「一次性口令」不是帳密——講對就永久記住，直到你去 `line_staff` 分頁手動刪那一列。
  跟 edit_code 外流時的處理方式一致（SETUP.md 已有的「連結外流要作廢」同一套邏輯）
- 第一次登入成功，機器人會把使用者自己的 LINE userId 回給他——解決「怎麼知道自己
  userId」這個先有雞先有蛋的問題，不需要額外工具
- 設定 `LINE_ADMIN_USER_ID` 後，**每一次新的人登入職員模式都會推播通知使用者**，密語外流
  被亂用時才有機會第一時間發現。沒設定也不會報錯，只是沒有通知

**跟記者端的關鍵差異**：`qa` 意圖**不套用 `isUsable()`**——職員問得到 draft／archived 場次
的內容，記者絕對問不到，這是刻意的分野，不是漏防。批次 4 開發時踩到一個真的會讓這個分野
失效的 bug：職員的候選活動清單如果沿用記者版的 `buildCalendarCards()`（會濾掉 draft／
archived），路由回傳的活動 id 因為不在候選清單裡，會被白名單檢查濾掉，變成「職員問得到
內容、卻永遠比對不到活動」——查完全通、答完全不通，behavior 看起來像壞掉但方向是對的
（沒有洩漏），所以必須用 `lib/router.js` 新增的 `buildAllCalendarCards()`（不濾狀態）。

- `lib/ids.js`（新增）：`generateId()` / `generateEditCode()` 從 `api/events.js` 抽出來共用，
  職員「開新活動」跟後台「新增活動」要是同一套 id／編輯碼產生邏輯，不要兩邊各刻一份
- `lib/line.js`：新增 `getProfile()` 取顯示名稱——只給職員登入通知用，一般記者問答完全
  不呼叫，不蒐集記者的顯示名稱（跟「不要存記者的 LINE 顯示名稱」原則不衝突，職員是
  已知同事，記者是匿名公眾）
- `lib/staff.js`（新增）：密語比對、`line_staff` 表讀寫、七種職員意圖的路由
  （`calendar/qa/geo_status/event_analytics/create_event/training_link/other`）、
  後台數據輕量彙整（直接讀 `qa_log`）、GEO 狀態查詢（**不重新實作 `api/geo.js` 的邏輯**，
  內部打它既有的 `action=status`，因為那支還在被另一批工作持續修改，重新實作等於自己
  維護一份會跟正本兜不起來的複製品）、新增活動（直接寫 `events` 表，狀態一律 `draft`）
- `api/line.js`：密語判斷放在 `#代碼` 判斷之前、整段優先攔截；已登入的職員所有訊息都交給
  `handleStaffMessage()`，不再落到記者的 `#代碼`／`handleUnbound()` 流程
- **測試**：30 項端對端整合測試，含前述踩到的 bug 修好後的回歸驗證、職員問 draft／archived
  都答得出來、**一般記者完全不受影響（對照組）**、`LINE_ADMIN_USER_ID` 有無設定兩種情況、
  owner 自己登入不會自己通知自己。批次 2/3 既有測試全數重跑通過

**刻意沒做的事**（使用者沒要求，不要自己加）：
- 沒有做「登出職員模式」指令——密語只能加人、不能自己退出，要退出得使用者自己去 Sheet 刪列
- 職員的「新增活動」只給名稱＋可選日期，不支援一次把新聞稿內容也用 LINE 打進去——那應該
  在編輯連結裡貼，LINE 對話框不適合貼一大段新聞稿
- 沒有做「刪除活動」「封存活動」等破壞性指令——這些留在後台做，LINE 對話框誤觸的代價
  太高，不適合開放

---

### 批次 4 之後的修正＋小功能：媒體名稱擷取 bug、LINE 附圖（2026-08-27）

使用者用截圖回報：記者在 LINE 問「給我完整新聞稿」，機器人兩次都回「已記錄，謝謝！請直接
輸入您的問題即可。」——問題完全沒被回答。同一則訊息追問「可否…回答所有記者會問題 甚至提供
照片」，一併處理。

**根因（真實環境的 bug，不是使用者操作錯誤）**：媒體名稱擷取那段判斷式原本寫成
`!binding.media_name && looksLikeNameOrSkip(text)`——只看「media_name 還沒填」，不管
「有沒有真的被問過名稱」。自然語言軟綁定（`handleUnbound` 命中 qa 意圖）的記者從頭到尾
沒被問過名稱，`media_name` 永遠是空字串；只要問題剛好不含「？」也沒用疑問詞開頭
（「給我完整新聞稿」就是這種句型），就會被 `looksLikeNameOrSkip` 誤判成「像名稱」，
永遠卡在「已記錄，謝謝」，問幾次都一樣——**真正的問題從未被回答過，記者也不會知道要
換句話說才問得到**。

**修法**：`line_users` 表 F 欄（note）新增一次性旗標 `ask_name`——只有 `#代碼` 綁定當下
明確問過「請問您是哪家媒體」時才會標上；不管下一則訊息判斷結果像不像名稱，用掉這一次
就立刻清空，之後永遠不會再攔截。自然語言軟綁定完全不會標這個旗標，所以從一開始就不會
誤觸。連帶把 `looksLikeNameOrSkip()` 的排除清單加了「給我／請給／麻煩／幫我／提供」等
常見祈使句開頭，降低唯一那一次擷取視窗裡的誤判率。

⚠️ **跟批次 5 規劇的衝突**：批次 5 規劃用同一個 `line_users.note` 欄位標記 `pending`
（真人接手）。目前兩者不會同時發生（`ask_name` 只存在於 #代碼綁定後、第一則訊息處理完
之前的極短視窗，此時不可能有 qa 對話觸發真人接手），但真的要做批次 5 時，要先確認
`note` 欄位的值域規劃（例如改成用逗號分隔存多個旗標，或另外加一欄），不要直接假設
`note` 只會是空字串或 `pending` 兩種值。

**LINE 附圖**：`events` 表 H 欄（images，格式「網址」或「網址|圖說」）如果有值、且記者的
問題含照片類關鍵字（照片／圖片／相片／圖檔／新聞照／image/photo），文字答案送出後另外
`push`（不是塞進同一則訊息，reply token 已經用掉、且要避免一張壞掉的網址連累文字答案
送不出去）最多 4 張 LINE image 訊息。只有 `https` 開頭、`.jpg/.jpeg/.png` 結尾的網址
會被送——這個欄位其實什麼網址都能貼（webp、Google Drive 分享連結…），LINE 的 image
訊息規定內容要是 JPEG 或 PNG，貼錯格式送過去只會在聊天室變一張壞掉的圖示；篩掉的照片
不會真的不見，文字答案裡本來就會附完整下載網址可以點開看（沿用既有 system prompt 規則）。
沒有另外做縮圖，`originalContentUrl`／`previewImageUrl` 用同一個網址。

**測試**：新增 23 項端對端整合測試，直接重現截圖裡「問兩次都被吃掉」的情境並驗證修好、
`#代碼` 綁定的正常擷取流程沒有回歸、綁定後第一句就直接問問題（不是名稱）的情況、附圖的
格式篩選（jpg/png/https 通過，webp／http 被篩掉）、活動沒有照片欄位時不會多送。批次
2/3/4 既有測試全數重跑通過。

---

### 批次 4.5：綁定期間的出口、歡迎圖卡、圖文選單

**問題一（真實回報）**：記者掃 QR 綁定某一場之後，打「最近活動」得到的是那場 AI 的
自我介紹，不是活動清單。

**根因**：`handleEvent` 只要 `line_users` 有有效綁定，就把**每一則**訊息無條件送進
`answerQuestion()`。那場的 AI 手上只有該場知識庫，收到「最近活動」只能再自我介紹一次。
記者等於被鎖死在掃到的那一場，6 小時 TTL 到期前沒有任何出口——而且他不會知道問題出在
哪，只會覺得這個帳號很笨。

**修法**：進問答之前先攔三種「這句不是在問某一場內容」的意圖（`lib/menu.js`
的 `detectMetaIntent()`）：

| 意圖 | 觸發 | 行為 |
|---|---|---|
| `calendar` | 最近活動／其他活動／活動列表／有哪些場次 | 列清單，**不解除綁定**（多半只是想看看，看完還要繼續問原本那場），並附一句「您目前在問的是《X》」 |
| `switch` | 換一場／換活動／切換活動／不是這場 | **解除綁定**（下一句多半是新場次名稱，留著舊綁定會讓那句被舊場吃掉）並列清單 |
| `help` | 使用說明／怎麼用／help | 回 `HELP_TEXT` |

另外加「整句就是別場活動名稱」→ 直接換過去（`matchEventByName()`）。活動清單的快速回覆
按鈕送出的就是完整活動名稱，沒有這條的話按了按鈕只會讓**舊那場**的 AI 去回答
「某某記者會」這句話。

**為什麼是純關鍵字、不呼叫 AI**：`routeIntent()` 只在沒綁定時跑一次，成本可以接受；
但綁定後每一則都是正式提問，為了判斷「這句是不是想換一場」而在每則問題前多打一次
Haiku，等於讓所有記者的每一次提問都變慢、也多一份帳單，真正用到的只有極少數幾句。
而這幾句又剛好都是我們自己控制的字串（圖文選單與快速回覆按鈕送出的就是那些詞），
關鍵字比對可以 100% 命中。誤判方向也刻意選過：**寧可漏判也不要誤判**——漏判只是記者
再打一次，誤判會讓真正的問題從沒被回答，就是上面 `ask_name` 那個已經修過一次的坑。

⚠️ **跟 `ask_name` 的交互**：meta 意圖與「打別場名稱換場」兩條路徑都會清掉 `ask_name`
旗標。不清的話，記者在「請問哪家媒體」的視窗裡改按了選單按鈕，旗標會殘留到換場之後，
他打的第一句真正的問題會再次被 `looksLikeNameOrSkip()` 吃掉。

**問題二**：新加好友的人不知道怎麼開始。

**修法**：
1. `follow` 事件改送 Flex 歡迎圖卡（`buildWelcomeFlex()`）——三步驟＋兩顆按鈕，純文字
   會被滑過去。Flex 送失敗會退回原本那則純文字歡迎詞。
2. 聊天室下方常駐**圖文選單**，三顆按鈕（最近有哪些活動／換一場活動／使用說明），
   對應上面三種 meta 意圖。這是「不知道怎麼開始」最有效的解法——不必先知道任何指令。

**兩套選單，記者與職員各一**（2500x1686 大版、六格）：

| 記者選單 | 職員選單 |
|---|---|
| 最近有哪些活動 | 活動列表（含未發布） |
| 換一場活動 | 新增一場活動 |
| 使用說明 | 後台數據 |
| 新聞稿全文 | 媒體訓練連結 |
| 活動照片 | GEO 狀態 |
| 新聞聯絡人 | 退出職員模式 |

⚠️ **為什麼一定要分兩套，不能共用一套**：職員那六格有「新增一場活動」「後台數據」
「GEO 狀態」，全是內部操作。一套共用的選單等於把內部功能的入口攤在每一個記者眼前——
就算他按下去會因為沒有職員身分被擋掉，也已經洩漏了這個帳號背後有哪些內部功能。
所以用 LINE 的 **per-user rich menu**：記者選單設為預設（所有人），密語登入後才把該
userId 個別覆蓋成職員選單（個人連結優先度高於預設），退出職員模式時解除連結、自動
落回記者選單。

**怎麼安裝**：用職員模式打一句「**設定圖文選單**」，程式會建立兩套選單、上傳底圖、
把記者版設為預設、把現有職員逐一換成職員版、刪掉舊的。做成職員指令而不是新開一支
API，是因為 Vercel Hobby 的 12 支 Function 上限目前已用掉 11 支，而這動作一輩子大概
只會執行個位數次，不值得占掉最後一格。

> 底圖由 `node assets/build-richmenu.mjs` 從 `lib/menu.js` 的 `REPORTER_MENU`／
> `STAFF_MENU` 定義**自動產生**（`assets/` 不會被部署）。刻意不手寫 HTML：底圖上的字
> 必須跟定義逐格對齊，手維護兩份就會出現「圖上寫 A、按下去送出 B」，而且沒有任何測試
> 抓得到。格線寬高也直接從 `buildRichMenuDefinition()` 的可點區域反推，不重算——差一個
> 像素使用者就會按到隔壁那格。
>
> 改完文案要重新跑一次腳本產圖，並在 LINE 重打「設定圖文選單」才會生效。**按鈕文字有
> 更動時，對應的路由（記者版是 `detectMetaIntent()`，職員版是 `routeStaffIntent()`／
> `isExitStaffCommand()`）要一起改**，否則會變成「按了沒反應」——`test/test-menu.mjs`
> 有守這件事。

**問題三：進得去職員模式，出不來。**

打「退出職員模式」只會拿到一份能力清單。根因是那句話交給 `routeStaffIntent()`（AI）判
意圖，被歸到 `other`，而 `other` 的處理就是印能力清單——程式裡根本沒有退出這條分支，
唯一的收回方式是自己去 Google Sheet 的 `line_staff` 分頁刪列。

**修法**：`isExitStaffCommand()` 用**字面比對**，在 `routeStaffIntent()` 之前攔下來。
權限的關閉不該取決於模型當下判得準不準。退出時把 `line_staff` D 欄標 `revoked`
（不刪列——這是權限授予紀錄，誰在什麼時候拿過內部權限、什麼時候交回來，出事要查得到），
並解除個人選單連結。重新輸入密語會把原本那列的標記清掉復權，不會長出第二列。

「退出」「離開」單獨一個詞不算——記者也可能打，要求至少帶一個修飾詞（職員／模式／身分）。

**測試**：`npm test`，共 175 項。`test/test-menu.mjs` 129 項（意圖判斷、換場比對、圖卡與
兩套選單結構、退出指令比對），`test/test-flow.mjs` 34 項端對端（用假的 Sheets／LINE／
Anthropic 直接跑 webhook handler），`test/test-og.mjs` 12 項（社群預覽卡）。

意圖判斷特別列了一整組「正常提問絕對不能被攔截」的反例（「這技術怎麼用在消防救災」
「還有哪些應用場景」「換算成新台幣是多少」…），這組是這次改動的主要風險所在。選單那
六格也有一項專測「六格剛好鋪滿 2500x1686，沒有縫隙也沒有重疊」——有縫隙是點了沒反應，
有重疊是按到隔壁那格。

---

### 批次 6：GEO 狀態去雜訊＋趨勢圖表、活動清單濾掉已結束、群組模式

**問題一：GEO 狀態回覆內容太技術性**。原本把「啟用引擎」「判官」「CRON_SECRET 未設定」
全部倒給同仁看——這些是工程除錯用的資訊，不是公關人員想知道的「現在狀況」。改成只留
日期、進度、剩幾題，以及唯一真的算警訊的 `ready` 檢查（代表環境變數少設了什麼，掃描
可能根本沒在跑）。

同仁也提了「最好能直接傳圖表」。改成用 [QuickChart.io](https://quickchart.io) 的公開
GET API 把 `action=series` 的 7 日移動平均組成一個 URL，讓 LINE 直接抓那張 PNG 當
image 訊息送出（`lib/staff.js` 的 `buildGeoTrendChartUrl()`）。沒有另外做圖片生成
服務或碰 headless browser／canvas 這類在 Vercel Serverless Function 裡容易出包的原生
依賴——純粹是組一個 URL，圖是 QuickChart 的伺服器畫的。

⚠️ 這條 URL 刻意不帶 `ADMIN_PASSWORD`：LINE 的伺服器會直接 GET `originalContentUrl`，
把密碼放進一個會被 LINE CDN 快取、可能被轉傳的連結裡不划算。改用「AI 能見度分數走勢」
本身當可公開內容——這不是新聞稿或個資，跟 `/event` 頁面本來就對外開放同一個道理；
真正機密的判官設定、`CRON_SECRET` 仍然留在需要密碼的 `action=status`／`series` 端點
後面。資料不足（剛啟用、一次掃描都沒跑過、或全部失敗）時 `buildGeoTrendChartUrl()`
回 `null`，不送圖片，不然 LINE 會顯示一張空白或壞掉的圖，比不傳還糟。

**問題二：「最近有哪些活動」連已經辦完的場次都列出來**，滑一輪手機螢幕全被過去式塞滿。
`lib/router.js` 的 `sortUpcoming()` 改成三種狀態各自判斷（不是單純比日期）：
- `draft` 一律留著——草稿是「還沒發生、需要有人去處理」的事，職員版本來就要看得到
  才不會忘記發布
- `archived` 一律濾掉——已下架不該占用日常清單版面，真要找回來走後台
- `active`／`ended` 只在日期還沒到或日期未定時留著；日期已經過去的直接濾掉，這正是
  要拿掉的「已結束」清單。「日期未定」以前被誤歸進「已結束」，一併修正

想問已經辦過的場次，文案有提示「直接打活動名稱即可」——這份清單只是「快速掃一眼現在
有什麼」，不是這個帳號能不能回答已結束場次的問題（那條路一直都在，跟清單顯不顯示無關）。

**問題三：群組模式（仿美玉姨）——已完成，不再是選配。**

- 前置作業（人類要做，程式碼管不到）：LINE Official Account Manager →「設定 → 回應
  設定」把「允許加入群組/多人聊天」打開，官方帳號才有辦法被邀進群組；沒開這個，LINE
  根本不會讓人把帳號拉進群組
- 核心規矩：**沒被 @ 到就完全安靜**，不回覆、不留任何痕跡。`api/line.js` 的
  `handleGroupEvent()` 用 `lib/line.js` 的 `isBotMentioned()` 判斷——依賴 LINE 官方
  為此加的 `mentionee.isSelf` 欄位，不用自己存一份 bot 的 userId 去比對；這個欄位不
  存在或不是 `true`，一律當作沒被叫到（失敗方向選「安靜」，不是「誤觸插話」）
- `stripMentionText()` 把 @ 的文字片段全部拿掉，只留真正的問題，再走跟 1 對 1 完全
  同一套路由（`handleMetaIntent`／`handleUnbound`／換場／軟綁定）——用 `groupId`
  當 `line_users` 表的 key，等於「這個群組」自己有一份軟綁定狀態，直接複用整套 TTL／
  換場機制，不必為群組另外維護一份幾乎一樣的邏輯
- ⚠️ **群組裡完全不接職員模式**：密語比對／`#代碼` 綁定整段跳過，被 @ 到一律走記者端
  的自然語言路由。同一群組可能同時有記者、公關同仁、甚至長官，密語一旦在群組裡打出來
  所有在場的人都看得到——職員身分只能在私訊裡取得，這裡沒有例外
- 限流用 `groupId` 當 key（不是個別成員的 userId），道理跟 1 對 1 用 `line_user_id`
  一樣：保護的是「這個群組」的額度，不會因為某個人連環發問就把同群組其他人也鎖住
- `qa_log` 的 `mediaName` 統一記成「（群組提問）」，`source` 欄位仍然是 `line`（沒有
  另外開一個 `line-group`）——`api/analytics.js`／`lib/staff.js` 的後台數據都用
  `=== 'line'` 精確比對算 LINE 通道的問答數，多一個新值會讓那些統計默默漏算，這批
  先不動；真要在後台分開看群組跟 1 對 1 的用量，是另一個報表功能，不是「讓群組能用」
  的必要條件
- 「輸入中」動畫（`/chat/loading/start`）LINE 官方文件明講**不支援 group／room**，
  `answerQuestion()` 群組呼叫端傳 `loading:false` 跳過，不然每則群組提問都送一次
  注定失敗的 API 呼叫，白白在 log 裡累積錯誤

⚠️ **實際回報的體感落差，修過一次**：一開始的版本是「每一則都要 @」——群組裡 @ 問
「最近有哪些活動」拿到清單之後，接著（沒有再 @）打清單裡某場的活動名稱，完全沒反應。
規則本身沒有邏輯錯誤，但使用者的體感是「剛剛不是才理我嗎」；而且我們自己組的
quick reply 按鈕（`calendarQuickReplyItems()`）在群組裡點下去送出的也只是純文字，
不會自動帶 @，所以連我們自己給的按鈕都會踩到這個坑。

修法是加一個 `GROUP_SESSION_MS`（5 分鐘）續問視窗：被 @ 到並且我們真的回答之後，
接下來 5 分鐘內同一個群組不用重新 @ 也算在跟我們對話；超過時間窗，或這段期間都沒人
開口，就退回「一定要 @」的預設安全模式。視窗時間戳存在 `line_users` 新增的 G 欄
（`group_session_until`），持久化而不是放記憶體——跟 `pending`（職員追問）踩過同一個
坑：Vercel 的 serverless instance 不保證下一則訊息會落在同一個 instance 上。

⚠️ 這個時間戳刻意跟活動綁定（`bound_at`／`event_id`，TTL 6 小時）分開存在獨立欄位，
不能共用：綁定管的是「這個群組現在問的是哪一場」，沒 @ 也可能持續有效很久；session
管的是「剛剛是不是才被 @ 過」，只有幾分鐘。混在同一欄會讓「還沒問過任何一場」的群組
被誤判成「綁定了一個空字串的活動」，`answerQuestion()` 拿到空 `event_id` 會直接掛掉。

（批次 26 之後 `line_users` 還多了 H 欄 `last_topic`——「上一則剛回答完的是產業趨勢
題還是工研院技術題」，同樣是持久化、同樣跟活動綁定分開存，理由見批次 26。）

續問視窗內如果判不出問題在問什麼（`routeIntent()` 判成 `other`、或完全比對不到活動），
`handleUnbound()` 新增的 `silentOnOther` 參數會讓它安靜略過，不會跳出「不確定您想問
哪一場活動」——那句引導文案對一個直接 @ 我們的人是體貼，對群組裡剛好在聊別的事的人
就是插話。非文字訊息（貼圖…）在續問視窗內也是安靜略過，不用來亂回。

**還沒做、仍是選配的部分（人工接手轉接標記）**：

- AI 判定「資料裡沒有」或屬敏感題時，多回一則 quick reply「需要真人回覆嗎？」→ 按下
  即在 `line_users.note` 標 `pending` 並 push 通知使用者
- 使用者用手機 **LINE Official Account App** 在同一個對話框接手。不需要寫程式——
  第 7 節步驟 2 的「聊天 + Webhook 同時啟用」就是為了這個
  - ⚠️ 人工在 LINE App 回的訊息**不會經過 webhook，也不會進 `qa_log`**。這是 LINE
    的行為不是 bug。要完整紀錄只能靠 LINE 後台聊天記錄匯出，或加購「聊天進階方案」
    （月費 100 元、紀錄留存 5 年）。**交付時要講清楚，不要讓使用者以為分析數據是全的**
  - `line_users.note` 目前也被 `ask_name`（#代碼綁定後等媒體名稱的一次性旗標）在用；
    真的要做這塊時要先確認 `note` 的值域規劃（例如改成逗號分隔存多個旗標，或另外
    加一欄），不要直接假設只會是空字串或 `pending` 兩種值

**測試**：新增 `test/test-router.mjs`（18 項，行事曆清單濾掉規則）、
`test/test-geo-chart.mjs`（17 項，狀態文字精簡與圖表 URL 組法）、`test/test-mention.mjs`
（14 項，`isBotMentioned`／`stripMentionText` 純函式）；`test/test-flow.mjs` 新增群組
情境（沒被 @ 到不回應、@ 到才回答、群組軟綁定、密語與 #代碼在群組裡不接、room 來源、
群組限流、續問視窗成功／過期／非文字訊息略過、silentOnOther）。`npm test` 全部
合計 263 項。

---

### 批次 7：LINE 綁定後附上快速提問按鈕（chips），跟網頁版同步

**問題**：網頁版問答介面（`public/event.html`）一直都有同仁在後台「本場次提供資料」
欄位設定的快速提問 chips（`events!G`，例如「新聞稿」「新聞照片」「合作廠商」），記者
在網頁上點一下就能問，不用自己想問題。LINE 這邊完全沒接這個資料——`api/line.js` 的
`rowToEvent()`／`lib/staff.js` 的 `getEventRawById()` 根本沒讀 `events!G` 這一欄，
同仁特地在後台設定的關鍵字，LINE 記者完全看不到。

**修法**：`answerQuestion()` 的每一則回答都附上 `eventQuickChips(event)`——直接讀
`event.chips`（跟網頁版同一個資料來源，`events!G`），轉成 LINE 的 quick reply 按鈕。
同仁在後台改一次，網頁跟 LINE 同步生效，不需要另外維護一份「LINE 專用關鍵字」。
沒設定自訂 chips 的活動退回 `DEFAULT_CHIPS`（跟 `public/event.html` 的
`defaultChips` 文案一致——⚠️ 兩邊各自維護一份同樣的字串，不是共用模組：
`event.html` 是純瀏覽器 `<script>`，沒有打包流程可以匯入 `lib/` 的 ESM 模組；這只是
建議問題的預設文案，跟「不要做兩邊同步」講的知識庫／答案內容性質不同，頂多措辭跟
網頁版不完全同步，不是功能壞掉）。

chips 不是「選過一次就收起來」的一次性選單，而是**每一則答案都帶著**——跟網頁版
一樣，記者問完一題還想換方向，點一下就好，不用自己想下一句要打什麼。三個附加點：

1. `answerQuestion()` 的每則回答（涵蓋 1 對 1、職員模式、群組——都走同一支函式）
2. `#代碼` 綁定後 `ask_name` 解決的那一刻（「已記錄，謝謝！請直接輸入您的問題即可。」）
   ——這是記者準備開始問問題的第一個時間點，不用等他問完第一題才第一次看到

⚠️ **`#代碼` 綁定的確認訊息本身刻意不附 chips**（「已為您接上《X》✅\n\n請問您是哪家
媒體？」那則）：這個時間點下一則訊息會被 `ask_name` 邏輯攔截去判斷是不是媒體名稱
（見批次 4 之後的修正那節）。`looksLikeNameOrSkip()` 對含問號的文字會直接放行當
問題處理，但同仁自訂的 chips 未必都是問句（例如「新聞照片」這種關鍵字，不含問號、
不以疑問詞開頭），如果在這個時間點就附上 chips，記者點了「新聞照片」會被誤判成
在報媒體名稱。等 `ask_name` 解決之後才附 chips，完全避開這個衝突。

**測試**：`test/test-flow.mjs` 新增情境 10（6 項）——已設定 chips 的活動附自訂
chips、沒設定的退回預設問題、`#代碼` 確認訊息不附 chips、`ask_name` 解決後的
「已記錄」訊息要附 chips、職員模式與群組都附 chips（驗證都走同一支
`answerQuestion()`，沒有分岔邏輯）。`npm test` 全部合計 269 項。

---

### 批次 8：邀訪聯絡窗口分工＋自然語言綁定順手問媒體名稱

**問題一**：不同議題該找誰，記者常常猜不到，只能一律洽詢單一的「新聞聯絡人」
（`press_contact`，一場活動只能填一組）。同仁反饋想要「像 AI 問答一樣，點一下
關鍵字就給對應窗口」。

**背景決策**（先問過使用者才動工，兩個資料模型問題都會影響風險與 schema）：
- 這些窗口的電話／LINE 是**同仁的公司分機**，不是私人手機——公開曝光的風險遠低於
  私人號碼，才敢做成任何加好友的人都問得到的公開功能。**如果之後要加新的窗口類型，
  先確認填的是公司專線，不是個人手機**，不然等於把同仁的私人聯絡方式散布出去。
- 窗口分工**是每場活動各自設定**（這場技術問題找 A，那場找 B），不是全公司共用一套
  固定分工——即使同一個人固定負責某個議題，也是每場各自填一次，不做跨活動的全域
  聯絡人資料庫，避免多一張表、多一套「這個人現在還在不在職」的維護負擔。

**修法**：`events` 表新增 **P 欄 `contacts`**（每行「關鍵字｜姓名｜電話｜LINE ID
(選填)」，格式跟 `images`／`chips` 同一套「半形｜全形都收」的分隔規則）：

- `api/events.js`：`RANGE` 從 A2:O 拓寬到 A2:P，`buildContentRow()`／`create`／
  `update`／`archive`／所有 GET 回應都補上這個欄位
- `public/index.html`、`public/edit.html`：新增「邀訪聯絡窗口分工」textarea，跟
  `chips` 欄位同一個位置風格；**刻意不放進「以既有活動為範本」的複製範本邏輯**，
  跟 `press_contact` 一樣——聯絡窗口是這場活動專屬的資料，複製範本時帶過去等於
  給新活動配錯人
- `api/line.js`：
  - `eventQuickChips()` 固定在內容 chips 最後加一格「媒體邀訪需求」（LINE quick
    reply 上限 13 顆，內容 chips 截到 12 顆留一格給它，不會把入口擠掉）
  - `parseEventContacts()` / `matchContact()` / `formatContactReply()`：純字串
    處理，不呼叫 AI——這類回覆要求精準，電話號碼、LINE ID 這種資訊不該讓 AI 用
    自然語言重新生成一次（打錯一碼就是記者聯絡不到人）
  - `matchContact()` 只認**完全比對**（去空白、忽略大小寫），不做模糊比對：寧可
    命中不了讓記者換句話問一次，也不要把「技術規格」跟「技術突破」這種相近但不同
    的關鍵字搞混、給錯聯絡人
  - 判斷放在 `answerQuestion()` 裡而不是各呼叫端各自檢查——1 對 1、群組、職員
    模式最後都走這支，寫一次三邊都受惠，不會有人漏接
  - `detectMetaIntent()`（`lib/menu.js`）新增 `'contacts'` 意圖，`handleMetaIntent()`
    處理「媒體邀訪需求」：沒綁定活動就引導先選一場；活動沒設定過窗口分工就退回
    既有的單一 `press_contact`（幾乎每場都會填，不會讓記者點了按鈕卻什麼都拿不到）；
    有設定就列出關鍵字讓記者點選或直接打字

**問題二**：用打活動名稱軟綁定（`handleUnbound` 的 `qa` 高信心分支）的記者從頭到尾
沒被問過媒體名稱——跟 `#代碼` QR 掃碼綁定（有 `ask_name` 一次性擷取視窗）不一樣，
後台的問答分析永遠只看到「（未填寫）」，沒辦法統計哪些媒體來過。

**修法**：新增 `getStoredMediaName()`，跟 `getBinding()` 分開一支——媒體名稱（C 欄）
沒有 TTL 概念，是跟著這個人走的個人資料，不是「這次綁定」的一部分，綁定過期只代表
「不知道現在要問哪一場」，不代表「不知道這個人是誰」，兩者不能混在一起判斷。

自然語言命中時：先查有沒有存過名字，有就直接沿用（答案的 `mediaName` 不再固定傳
`''`）；沒有的話正常回答之後，**用 push 補問一次**（不擋住剛剛的答案——記者不用先
回答完媒體名稱才拿得到他真正想要的內容），設 `note='ask_name'` 沿用既有的一次性
擷取機制（跟 `#代碼` 那邊完全同一套邏輯，沒有另外維護一份）。

⚠️ **群組不問**：`handleUnbound()` 新增 `askMediaName` 參數，群組呼叫端傳 `false`。
一個群組裡有多個不同媒體的人，「貴媒體名稱」這句話對群組沒有意義，問了也不知道
該記在誰頭上。

**測試**：`test/test-flow.mjs` 新增情境 11（媒體名稱補問，7 項）與情境 12（邀訪
窗口分工，9 項）；`test/test-menu.mjs` 新增 `detectMetaIntent('contacts')` 的正反例
（13 項）。`npm test` 全部合計 301 項。

---

### 批次 9：全域技術窗口分工（跨活動，回報的新功能）

**跟批次 8 的決策衝突，記錄一下為什麼推翻**：批次 8 當時明確決定「不做跨活動的全域
聯絡人資料庫」，理由是窗口分工是每場活動各自設定的資料。這個決策在「記者為了某一場
記者會想找對應窗口」這個情境下仍然成立，批次 8 那套（`events!P`）完全沒動。批次 9
要解決的是另一個情境：**媒體臨時想問某個技術領域「該找誰」，跟任何一場記者會都無關**
（例如朱朱給的例子：「綠能所：能源、綠能領域技術：徐喬涵」）——這種問題沒有活動可以
綁定，批次 8 的模型完全接不住，只能卡在「請先告訴我您想問哪一場活動」。兩套資料模型
因此並存，各自對應不同的真實情境，不是互相取代。

**資料放哪裡**：新開一個分頁 `contacts_directory`，不是塞進 `events` 表——這份資料是
組織架構（哪個所對應哪個議題、誰負責），跟活動的生命週期無關，活動封存了這份還在。
整份存成**一個儲存格**（`contacts_directory!A2`），格式沿用跟 `events!P` 一樣的
「用｜分隔、每行一組」寫法：`主題｜單位｜聯絡人｜電話｜LINE ID(選填)｜一行簡介(選填)`。
不拆成一列一列的表格：這份清單同仁會常整批調整（換人、加新單位），在後台一個
textarea 貼過去存檔，比維護多欄表格好編輯，也不用另外做欄位對應的表單 UI（跟
`events!G` chips／`events!H` images 同一套「一格塞多行」的既有做法一致）。

`lib/contacts-directory.js` 是共用模組（`api/line.js` 讀取查詢、`api/events.js` 後台
編輯都 import 同一份），格式定義、預設種子、比對邏輯只有一份，不會兩邊各自維護一份
走鐘。分頁第一次被 `ensureSheets()` 建立時，自動把朱朱確認過的名單種進去（生醫所→
丁嘉琳、機械所→林潔玲、資通所→戴孟錚、綠能所→徐喬涵…），同仁看到的是「可以直接改」
的現成清單，不是從零開始的空白表格；種進去之後完全交給後台編輯，程式碼不會再覆蓋。

**後台編輯**：`api/events.js` 加兩個 action（沿用既有 `ADMIN_PASSWORD` 驗證，沒開
新的 serverless function——Vercel Hobby 12 支上限目前用 11 支）：
- `GET ?action=contacts_directory` → 讀原始文字
- `POST {action:'contacts_directory_save'}` → 整份覆蓋儲存

`public/index.html` 側邊欄加一個獨立頁籤「邀訪窗口分工」，一個大 textarea＋存檔按鈕，
跟活動管理完全分開（這份資料本來就跟任何一場活動無關）。

**LINE 端觸發**：`detectMetaIntent()` 判成 `'contacts'` 的路徑（`handleMetaIntent()`）
改成兩層：先看目前綁定的活動有沒有自己的 `events!P` 窗口或 `press_contact`，有就照
批次 8 的邏輯給精準的那組；**沒有**（沒綁定活動，或綁定的活動兩者都沒填）就退到全域
清單（`sendGlobalContactMenu()`）——不會再卡在「請先告訴我您想問哪一場活動」，這是
批次 9 唯一改動到批次 8 既有行為的地方，而且是「退路變得更好」，不是行為改壞。

全域清單的按鈕文字是朱朱要求的固定選項（活動名稱／產業趨勢分析／生醫／資通／機器人／
無人機／綠能／量測／機械／材料／感測／其他，共 12 顆，在 LINE quick reply 13 顆的
上限內）：
- 「活動名稱」送出既有的「最近有哪些活動」，直接借用批次 3 的 `calendar` 意圖，
  記者其實是想找特定記者會的窗口時不用另外處理
- 「中分院」「產業學院」「電光」這三個朱朱也有給聯絡人，但不佔按鈕格——透過
  「其他」的自由輸入比對仍然查得到，只是不放進固定選單（按鈕格數有限，這三個
  被問到的頻率預期比另外 10 個技術領域低）
- 「其他」：按下去先提示記者自己打字描述想問的主題，**下一則**訊息用寬鬆比對
  （`t.includes(topic) || t.includes(unit)`，不要求整句完全等於）去配對，配不到
  就退回朱則瑋（技術傳播組總機性質的窗口）。這裡刻意跟批次 8 `matchContact()`
  的「精準比對」不同：批次 8 那組是「按鈕點下去」精準命中，這裡的對象是記者隨手
  打的一句話，要求完全比對永遠配不到；抓錯的代價也比批次 8 小很多——比對到的仍是
  清單裡設定好的正確聯絡人，最壞情況只是轉去問了一個不是最相關的單位，不是給錯
  電話號碼

**主題按鈕文字不能跟記者自己打的問題撞在一起**：按鈕上顯示「生醫」，但送出的文字是
固定前綴的「邀訪：生醫」，不是「生醫」本身——不然記者剛好在問某場跟「生醫」有關的
活動內容時會被誤判成在找邀訪窗口。這需要 quick reply 按鈕「顯示的字」跟「送出的字」
分開，`lib/line.js` 的 `buildQuickReply()` 因此從只吃字串陣列，擴充成也接受
`{label, text}` 物件（字串陣列的舊呼叫方式完全不變，純粹加一種新用法）。

**一次性等待輸入的旗標**：跟批次 4.5 的 `ask_name`、批次 4 規劃的 `pending` 一樣，
借用 `line_users.note`（F 欄）存 `await_contact_topic`，用掉就清。但這裡多一個批次
8 沒遇過的情況：**這個旗標常常是在完全沒有活動綁定的情況下要設的**（記者可能從沒
綁定過任何活動就直接問邀訪窗口）——`setBindingNote()` 在沒有既有列時會直接放棄
（`idx===-1` 就 return），所以另外寫一支 `setContactPending()`，沒有列就新增一列，
`event_id`／`bound_at` 都留空，`getBinding()` 讀到 `bound_at=0` 一樣會判定成沒有
活動綁定，不會誤觸發任何跟活動有關的邏輯。

**攔截順序**：`handleContactTopicMessage()` 放在 `metaIntent` 判斷之後、綁定判斷
之前，跟 `metaIntent` 同一優先順序——不管有沒有綁定活動、綁定的是哪一場，「邀訪：
主題」的按鈕點擊跟「其他」後的自由輸入都要優先攔下來，不能被送進當前綁定活動的
問答。1 對 1（`handleEvent`）跟群組（`handleGroupMessage`）各自的 dispatch 都要接
這一段，兩邊共用同一支函式，比對邏輯只寫一次。

**測試**：`test/test-flow.mjs` 情境 12 最後一項（活動兩個窗口欄位都沒填）改成驗證
退到全域清單；新增情境 13（全域技術窗口分工，涵蓋：沒綁定直接問、按鈕文字含
「邀訪：」前綴、已綁定活動時按鈕仍優先於當前活動問答、查不到主題、「其他」自由輸入
命中／退回朱則瑋、一次性旗標只消費一次、群組裡的 dispatch 有接上）。`npm test`
全部合計 311 項。

### 圖文選單放大字級＋修一個真的漏問媒體名稱的坑（回報）

**圖文選單字太小**：`assets/build-richmenu.mjs` 縮小每格 padding（32/24px →
16/12px）、放大圖示圓圈（210px→280px）與兩層文字字級（70px→92px／42px→52px），
讓內容盡量撐滿每一格，不留大片空白邊。

**媒體名稱漏問**：回報「開活動好像沒問媒體名稱」，查出來是批次 8 沒蓋到的第三條路。
批次 8 只顧到兩條路會問媒體名稱——`#代碼` 綁定（`ask_name` 一次性視窗）跟自然語言
「軟綁定」（`handleUnbound()` 的 qa 高信心分支）——但 `handleEvent()` 裡「綁定中打
另一場活動完整名稱直接換場」那條路（`matchEventByName()` 命中）**從頭到尾沒有問過媒體
名稱**，不管換過去之前有沒有被問過。只靠打活動名稱換場的記者，`media_name` 永遠是
空字串，後台問答分析永遠看到「（未填寫）」。

修法沿用跟 `handleUnbound()` 完全同一套邏輯：換場後檢查舊綁定的 `media_name` 是否
已有值，有就跟批次 8 原本一樣不重問；沒有就用 push 補問一次（不擋住剛剛換場後的
答案），設 `note='ask_name'` 沿用既有的一次性擷取機制。群組那條換場路徑不用改——
群組 `media_name` 固定寫死「（群組提問）」，本來就不會落到「沒有值」這個分支。

**測試**：情境 2（按活動清單換場）補上「已有名稱不重問」的顯式斷言，新增一組
「換場前沒被問過 → 換場後補問一次」的情境；原本「換場也清掉 ask_name」的測試改成
驗證換場後 `note` 是**重新設定**的 `ask_name`（因為這個人剛好還沒填過名稱），不是
單純清空——重點還是接下來真正的問題不會被誤判成在報名稱。`npm test` 全部合計
315 項。

### 批次 10：媒體邀請函（活動前只給邀請函，不給正式新聞稿／照片，回報的新功能）

**問題**：新聞稿常常活動前都還在改、照片也還沒有正式版；記者臨時問「給我新聞稿」
「給我照片」，直接把 `knowledge_base`／`images` 端出去，等於提早曝光還沒定案的
內容。回報想加一個「媒體邀請函」欄位（活動前對外說的版本），活動前問答只用這份
內容，不要碰正式新聞稿與照片。

**設計決策**（AskUserQuestion 問過，三題都選推薦選項）：
1. 邀請函內容用**貼文字**存進系統，不是上傳 Word 檔——貼文字才能讓 AI 直接引用
   內容回答，跟專案「零外部套件」的既有原則一致（上傳 Word 檔案要嘛額外裝一套
   docx 解析套件，要嘛只能給記者一個下載連結、AI 完全答不出內容細節，兩者都比
   貼文字差）
2. 活動前**所有**問答都只用邀請函回答，不是只攔「給我新聞稿」「給我照片」這幾句——
   知識庫換掉是全域的，比另外維護一份「哪些問題該攔」的關鍵字清單簡單，也比較
   不會漏防（記者換句話問一樣問得到還沒定案的內容）
3. 系統**自動**用活動日期判斷是不是「活動前」（`event_date` 嚴格晚於今天），
   不是同仁手動切換的開關——activity 越接近，資料通常越準，讓程式自動接手，
   同仁不用記得在活動當天手動切換

**資料模型**：`events` 表新增 **Q 欄 `invite_letter`**，跟 `knowledge_base` 同一種
「一格塞多行文字」欄位，`api/events.js`／`public/index.html`／`public/edit.html`
補上這個欄位（同仁自助編輯跟管理員後台都能填），**刻意不放進「以既有活動為範本」的
複製範本邏輯**，跟 `press_contact`／`contacts` 一樣——邀請函是這場活動專屬的內容，
複製範本時帶過去等於給新活動配錯邀請函。

**替換邏輯集中在 `lib/prompt.js`**（不是讓 `api/chat.js`、`api/line.js` 各自判斷）：
- `isBeforeEventDate(rawDate)`：跟 `lib/router.js` 的 `parseEventDate()` 同一套
  日期格式判斷（`events!F` 欄兩種既有格式），這是第四份重複的日期解析（`router.js`／
  `event-page.js`／`index.html`／這裡），是專案已經接受的低風險重複，不是新增一種
  重複的種類。沒填日期或解析不出來一律當「不是活動前」——保守方向，不要誤鎖進
  邀請函模式回不去。
- `isPreEventMode(event)`：`event.invite_letter` 有值 **且** `isBeforeEventDate(event.event_date)`
  才算——沒填邀請函的活動完全不受影響，這是新增欄位，舊活動不能因為沒填新欄位就
  被回答不出東西。
- `resolveEventContent(event)`：活動前模式時回傳 `{ ...event, knowledge_base: event.invite_letter, images: '' }`，
  否則原樣回傳。`buildSystemPrompt()` 內部也呼叫 `isPreEventMode()`（可以，因為
  `invite_letter`／`event_date` 欄位在 `resolveEventContent()` 的 spread 之後還在
  物件上）幫 AI 補一條規則，提醒它「這是邀請函不是正式新聞稿，記者要完整新聞稿或
  照片時要說明活動當天才會發布」，不要讓 AI 把邀請函內容講成新聞稿全文。

**兩個管道都要接**：`api/chat.js`（網頁版）跟 `api/line.js`（LINE）都在丟進
`buildSystemPrompt()` 之前呼叫一次 `resolveEventContent()`——只接一邊的話，記者從
另一個管道一樣問得到還沒定案的內容，等於防了一半。

**⚠️ 職員模式要排除在外**：`answerQuestion()` 的內容問答是 1 對 1、群組、職員模式
共用的同一支函式，如果無條件套用 `resolveEventContent()`，同仁自己在準備活動時
反而會被自己設的「活動前」規則卡住、看不到真正的新聞稿內容。加了
`allowPreEventSubstitution` 選項（預設 `true`），職員模式呼叫端明確傳 `false`。

**測試**：新增 `test/test-prompt.mjs`（單元測試，不需要 fake Sheets／LINE），涵蓋
`isPreEventMode`／`resolveEventContent`／`buildSystemPrompt` 的日期邊界（明天／
今天／昨天／沒填日期）、沒填邀請函不受影響、日期兩種既有格式都認得。
`test/test-flow.mjs` 新增情境 14（整合測試，用假的 fixture `soon`：日期動態算
「明天」，knowledge_base 跟 invite_letter 刻意不同）驗證記者拿到的是邀請函、
職員拿到的是正式新聞稿。

#### 批次 10.1：chips 沒跟著換、公開活動頁也要擋（回報的追問）

**問題一**：`resolveEventContent()` 一開始只換 `knowledge_base`／`images`，沒換
`chips`（快速提問按鈕，LINE quick reply 跟網頁版共用）。活動前記者點原本那組
「這次活動的主要發表內容是什麼？」，AI 只能照實說「這部分我沒有資料」——不是壞掉，
但沒有用。回報問「有可能活動前關鍵字另外設嗎」。

**修法**：`events` 表加 **R 欄 `invite_letter_chips`**（活動前快速提問，格式跟
`chips` 一樣一行一題）。`resolveEventContent()` 活動前模式一併把 `chips` 換成
`event.invite_letter_chips || event.chips`——有填就用活動前那組，沒填就退回原本
的 chips（不會讓活動前的按鈕整排消失，這是新增欄位、舊活動不填也要正常運作）。
`api/line.js` 的 `eventQuickChips()` 內部直接呼叫 `resolveEventContent()`，呼叫端
不用先自己判斷、也不用先手動 resolve 一次——`resolveEventContent()` 對已經 resolve
過的 event 再呼叫一次是安全的（`invite_letter`／`event_date` 欄位在 spread 之後
還在物件上，重算會得到同一個結果，不會疊加或跑掉）。

**問題二**（追查 chips 時順便發現的相關漏洞）：`public/event.html`（記者前台頁面）
直接顯示 `event.chips`／`event.images`，資料來源是 `api/events.js` 的
`get_public` action——這支完全沒套用 `resolveEventContent()`。等於 AI 問答那邊
擋了活動前的正式照片，但公開網頁的活動頁面照樣把 `images` 欄位的官方照片原封不動
顯示出來，防了一半。

**修法**：`get_public` 也套一次 `resolveEventContent()`，只是輸入輸出都要小心：
- 輸入只給 `resolveEventContent()` 判斷跟替換需要的欄位（`status`／`event_date`／
  `chips`／`images`／`invite_letter`／`invite_letter_chips`），不要把整列都餵進去
- 輸出要把 `invite_letter`／`invite_letter_chips` 這兩個原始欄位濾掉才送出去——
  公開頁面只需要「已經算好」的 `chips`／`images`，不需要邀請函文字本身通過這支
  公開 API 外流（邀請函是要給 AI 問答用的，不是要在網頁上顯示全文）

**測試**：`test-prompt.mjs` 補上 chips 替換與「沒填 invite_letter_chips 時退回原本
chips」的斷言；`test-flow.mjs` 情境 14 補上「活動前的快速提問按鈕真的換了一組」的
整合測試（用兩組刻意不同的問句，證明不是剛好長一樣）。`npm test` 全部合計 341 項。

---

### 批次 11：換場不再灌水提問數、媒體名稱手動補、歡迎圖卡改版（2026-08-27）

背景：本機資料夾另外還有一份沒推上來的分支，跟這條線各自獨立改了同一個問題領域
（媒體名稱擷取）。核對過後，那份分支的「GEO 職員簡報改版」「退出職員模式」「換場
自然語言判斷」這幾塊，這條線自己的批次 6／8／9 已經用不同做法做掉（甚至更完整）；
真正還沒有、而且確認有價值的部分，整理成這批：

**換場不再算一則「提問」**：綁定中打另一場活動的完整名稱（多半是剛按了活動清單
按鈕）會經 `matchEventByName()` 直接切過去，但切過去之後一直是呼叫 `answerQuestion()`
把「半導體先進封裝技術發表會」這句話當成問題送進 Anthropic、寫進 `qa_log`——後台
「累積回答題數」「今日問答」因此被純粹的選單操作灌水，跟記者真的發問混在一起。
改成只回一句「已為您換到《XXX》✅ 請直接問問題即可。」的純確認訊息，不呼叫 AI、
不寫 qa_log；1:1 與群組兩條路徑都改了。原本「換場後補問媒體名稱」的 push 邏輯不動。
`test-flow.mjs` 三項舊斷言（原本檢查換場後真的拿到 AI 回答）改成檢查換場確認訊息，
其餘既有斷言（換場保留媒體名稱、不重複補問、push 補問時機）不受影響。

**媒體名稱手動補／清理**（`api/analytics.js`「問答分析」頁）：
- `action:'update_media'`：LINE 沒辦法像網頁彈窗那樣強制記者一定要填媒體名稱，
  現場公關人員通常認得出對方是哪家媒體，這個入口讓後台可以直接手動改一筆的媒體
  名稱，不用去試算表改。跟既有的 `delete` 同一套「先比對這一列現在的內容再寫入」
  防線，避免試算表被整理過、row_num 指向別筆資料時改錯人。
- `action:'scan_dirty_media'` / `'clean_dirty_media'`：回頭掃 `qa_log` 找出「媒體
  名稱欄其實是記者問題」的髒資料（同一次性擷取視窗誤判時會發生的那種），只回清單
  給人看、勾選確認要清的才會真的寫入——不是掃到就自動清，誤判的代價是洗掉一個
  湊巧很像問句的真實媒體名稱，人工看一眼比較保險。
- `public/index.html`「最新問答紀錄」表格每列加「✎ 媒體」按鈕；表格上方加「清理
  媒體欄」按鈕，點下去列出候選清單（勾選＋套用清理）。

**歡迎圖卡改版**：使用者提供新文案並要求「親切可愛一點、簡潔」，套進既有的
`buildWelcomeFlex()` 框架——標題改「嗨，我是米亞 👋」，內文三步驟改成使用者的版本
（含「活動前基本資訊、活動當天完整資料」這個對應 `resolveEventContent()` 的說明），
兩顆按鈕文字不動（`detectMetaIntent()` 認得的字串不能改，改了按鈕會變成按了沒反應，
`test-menu.mjs` 已經守住這條）。人設名字「米亞」只用在歡迎卡與 altText，不動
`lineExtraRules`／system prompt 的回答語氣——使用者只要求改歡迎卡，範圍刻意收窄。

**同時搬過來、確認跟這條線完全不衝突的小修正**（原作者：另一份分支，`Claude Opus 5`
共同作者）：
- 網頁問答限流改兩層（`client_id` 每分鐘 15 題＋IP 每分鐘 120 題防線）：記者會現場
  全場記者共用會場 Wi-Fi、對外同一個 IP，原本單靠 IP 限流會讓全場記者共用 15 題
  額度，開場最熱的十分鐘第 16 位就被擋
- 後台「今日問答」時區＋日期比對兩個坑（伺服器 UTC 時區、`includes()` 誤命中同月
  另一旬）；每小時分佈沒吃「上午／下午」，下午的記者會全部被算成早上
- `ensureLineUsersSheet()`／`ensureStaffSheet()` 從「失敗也記成已完成」改成「成功
  才記住，失敗 60 秒後重試」——冷啟動撞到 Sheets 暫時性錯誤的話，原本這個 instance
  會永遠不再嘗試建表
- 結案報告 CSV 匯出（`api/export.js`）漏濾 `source=staff` 的職員試問
- 網頁記者前台記住上次填過的媒體名稱（localStorage），避免同一人被算成兩家媒體
- 媒體訓練加分數持久化（`training_log` 分頁、`log_session`／`?action=summary`）：
  原本受訓者練完 5 題，AI 訓練師的評分就直接消失，成效報告永遠算不出「演練場次／
  平均分」。新增 `test/training.test.mjs`（`parseValidScores`／`avgOf`／
  `authorizeTraining` 純函式測試，含一個真的會把分數算錯的邊界案例：
  `Number(null)`／`Number('')` 都是 `0` 不是 `NaN`）

**刻意沒搬的部分**：GEO 職員簡報的 Flex 長條圖與「D+31 才准講基線墊高」的判定
邏輯——這條線的批次 6 已經用 QuickChart 圖片＋去雜訊文字獨立做掉一版，兩邊互相
不知道對方存在，貿然合併沒有完整脈絡（尤其 D+31 那個判定門檻背後的方法論）風險
較高，先不動；`media_roster` 快速回覆按鈕（挑最常見媒體讓記者點）——`api/media.js`
的名錄系統已經存在，只是還沒接到 LINE 的快速回覆選單，之後想做可以直接接。

**測試**：`npm test` 新增 `test/training.test.mjs`（22 項）與
`test/analytics-media-edit.test.mjs`（13 項，mock Sheets 驗證 update_media／
scan_dirty_media／clean_dirty_media 三個新動作），既有 341 項全數重跑通過
（`test-flow.mjs` 3 項舊斷言依上述換場邏輯更新後）。

---

### 批次 12：GEO 職員簡報改用 LINE 原生 Flex 長條圖，不再靠外部服務組圖表網址（2026-08-28）

使用者截圖回報：`GEO現在狀況` 回覆的圖表訊息長期顯示一個壞掉的圖示。

**根因**：舊版 `formatGeoStatusReply()` + `buildGeoTrendChartUrl()`（曾在 `lib/staff.js`）
把 14 天的 Chart.js 設定整包塞進 QuickChart.io 的查詢字串組成一個 GET 網址，直接當
LINE image 訊息的 `originalContentUrl` 送出去。實測這個網址在 14 天資料量下長度落在
1200＋字元，**超過 LINE image 訊息 originalContentUrl 官方文件記載的 1000 字元
上限**——不是偶發網路問題，是這個做法資料量到一定規模後注定會壞，資料愈完整、
問題愈穩定重現。

**修法**：新增 `lib/geo-brief.js`，改用 LINE 原生 Flex Message 畫圖表——長條圖是
巢狀 `box` 的寬度百分比（跟 `lib/menu.js` `numberedStep()` 畫圓形號碼同一招），
沒有網址、沒有外部服務、沒有長度上限這個天花板。一則卡片同時涵蓋：
- 今日掃描進度＋環境檢查警訊（沿用既有 `action=status`）
- 近 14 天總覽三個數字（平均分數／提及率／引用率）
- 監視中的議題排行（長條圖＋漲跌箭頭＋提及率／引用率／樣本數）
- 追蹤中的活動（基線→峰值→半衰期，**只有 `settled`（30 天觀察期已過）才給留存
  數字**，還沒到就明講「觀察期還沒到，暫不評斷留存」）

⚠️ **刻意不重新實作評分／半衰期／基線抬升／結構化稿判定的門檻邏輯**——那些規則
背後是 `api/geo.js` `diagnoseEvent()`／`eventEffects()` 已經校準過的方法論（`settled`
就是「30 天觀察窗期夠不夠長」的判定，等同批次 4 之前顧慮過的「D+31 門檻」，只是
命名不同）。這裡直接消費 `action=series` 已經算好的 `events[].findings`／
`board[].findings`，只負責排版，不重判——避免兩邊各自維護一份判定邏輯、之後改一邊
忘了改另一邊，也不需要我重新推導那套方法論背後的完整脈絡。

Flex 送不出去（舊版 LINE App、格式被拒、或 `action=status`／`action=series` 兩邊都
查不到資料）自動退回純文字版，長條圖改用全形方塊字元湊——跟 `lib/menu.js`
`buildWelcomeFlex()` 同一套「Flex 優先、失敗退文字」的降級模式。兩份資料來源各自
獨立失敗也不會讓整份簡報開天窗（`Promise.all` 平行查，其中一邊是 `null` 另一邊
還在就照樣組得出卡片）。

移除舊的 `buildGeoTrendChartUrl()`（已確認是根因，不是能修一修留著的功能）與
`formatGeoStatusReply()`（併入新版）；`lib/line.js` 的 `pushChartImage()` 本身沒有
問題（是「用網址組圖」這個手法在資料量大時會爆），留著當通用工具，只是目前沒有
呼叫端在用了。

**測試**：`test/test-geo-chart.mjs` 整支重寫（原本測的函式已移除），26 項純函式
測試涵蓋 Flex 結構、長條寬度百分比與分數對得起來、漲跌箭頭、`settled` 門檻正確
擋住留存數字、兩份資料來源各自失敗的容錯、純文字退版。`test-flow.mjs` 既有的
GEO 整合測試更新斷言方式（Flex 訊息在測試 fake 裡沒有 `.text` 欄位可比對）。
`npm test` 全數重跑通過。

### 批次 13：綁定改成預設值，不再是鎖（2026-08-28）

長官回報：選哪個活動，LINE 就變那場的專屬問答機器人；活動一換場，記者就再也
問不到其他場——這是原本設計的實際限制，不是 bug。

**根因**：`handleEvent()`／`handleGroupMessage()` 綁定中的問答路徑，只有
`matchEventByName()`（整句剛好是活動名稱才認得出來的規則式比對）能換場，一句
夾著別場線索的完整問題（多半帶著問號、或不是完整活動名稱）認不出來，直接被送
去問目前綁定的那一場，答非所問。反而是**沒綁定**時的 `handleUnbound()` 早就在用
`routeIntent()`（批次 3）做這件事——路由零件本來就有，只是綁定之後完全不會再
被呼叫到。

**修法**：綁定中的問答路徑，在 `matchEventByName()`（選台）之後、真正送進 AI
問答之前，多加一次 `routeIntent()` 檢查。只有 `confidence: 'high'`、剛好指到一場、
而且不是目前這場，才自動換場並直接用新場次回答；其餘一律留在原場繼續回答——
寧可誤判成「留在原場」也不要誤判成「換去別場」，換錯場比換不了場更糟：記者不
會發現答案其實來自另一場，還可能直接截圖引用（跟坑 6 同一種風險）。

換場時在答案最前面加一行「🔄 已切換到《活動名稱》：」——不能悄悄把答案換一場
卻不說一聲。這段提示接在 `answerQuestion()` 裡統一處理（新增 `switchNotice`
參數），邀訪關鍵字直接命中、跟走 AI 問答兩個分支都會接上，1:1、群組共用同一套。

**刻意不做的事**（留給下一批，先看這批的實際回報再決定要不要做）：
- 一題同時比對、彙整多場的內容（例如「哪幾場提到固態電池」）——目前一次只能
  換到一場，答不到就照現有規則說「這部分我沒有資料」。真的要做，第 6 節「跨場
  次模式專用」那四條防線 prompt 規則要先補齊，不能繞過。
- 沒有更新 `HELP_TEXT`／歡迎圖卡／圖文選單的文案（仍然寫著「先選一場活動」）——
  這批只動行為，不動說明文字，先看記者實際會不會靠自然語言直接問到別場，再決定
  文案要不要跟著改。

**測試**：`test-flow.mjs` 新增情境 15——明確指向別場的問句自動換場並附提示、
換場後續問不重複跳提示、問不出場次線索時留在原場不亂跳、群組共用同一套邏輯。
`npm test` 全數重跑通過（128 項流程測試）。

### 批次 14：群組續問視窗，@ 到別人時要安靜（2026-08-28）

長官實測回報：群組續問視窗內，只要有人講話機器人就會回，即使明顯是在跟另一個
人講話（截圖案例：「我再跟 @朱朱 小朱 說話」），機器人還是煞有其事答了一段
歡迎詞，感覺像亂回。

**根因**：`handleGroupMessage()` 綁定中的問答路徑，從一開始就沒有
`handleUnbound()` 那道 `silentOnOther` 門檻——只要訊息通過 `handleGroupEvent()`
的外層關卡（真的被 @，或還在 5 分鐘續問視窗內），不管內容是不是真的在問活動，
一律送進 `answerQuestion()` 硬答。而且每答一次都會呼叫 `touchGroupSession()`
把視窗續命 5 分鐘，形成雪球：只要群組還有人持續講話，視窗就永遠不會真的過期，
變成「有人講話它就回」。

**曾經考慮、後來否決的做法**：直接拿批次 13 已經在算的 `routeIntent()` 結果，
`intent === 'other'` 就安靜跳過。**驗證時發現這樣會製造新的迴歸**：`routeIntent()`
一次只看單一句話 + 行事曆清單，沒有對話記憶，分辨不出「那合作廠商有哪些」這種
依賴上一句才聽得懂的合法續問，跟純聊天的差別——兩者在它眼裡都是「看不出跟哪場
活動有關」，一律判成 `other`。真的套用這道門檻，會連批次 6 續問視窗這個功能
本身要保護的案例都一起擋掉（`test-flow.mjs` 情境 9 第三步那個既有測試證實了這點：
沒改用別的方法之前，先跑一次草稿版本就直接打掉這項既有測試）。

**改用的做法**：在 `handleGroupEvent()`（比 `handleGroupMessage()` 更早、原始
LINE mention 資料還在的地方）新增一道否決——沒被 @ 到（純靠續問視窗撐著）、
但訊息本身的 `mention.mentionees` 裡有人、卻沒有一個 `isSelf`，代表這句話明確
是 @ 給另一個人聽的，不是在跟我們講話，直接安靜，連 `routeIntent()` 都不用呼叫。
這個訊號比事後用 AI 猜「是不是閒聊」精準得多，也不用多花一次 Anthropic 呼叫。
安靜的分支刻意不呼叫 `touchGroupSession()`，讓視窗真的能到期，打破上述雪球。

真的被 @ 到時完全不受影響——跟 1 對 1、跟 `handleUnbound()` 的
`silentOnOther:false` 同一個原則，明確叫了機器人就不能不理人。

**刻意不做的事**：一般的、不含 @ 的純聊天（例如「大家中午吃什麼」）在**已綁定**
狀態下這批**沒有**修——上面否決的做法説明了為什麼直接擋很危險，真要做需要先
讓 `routeIntent()` 拿得到對話歷史（例如最近一兩輪的問答），才能真的分辨「延續
上一句的續問」跟「無關的聊天」，這是需要另外設計、驗證的較大改動，不在這批
範圍內。這批只解決「明確 @ 別人」這個高信心度、零風險的子集。

**測試**：`test-flow.mjs` 新增「@ 到別人 → 安靜」情境，並確認續問視窗沒有因此
被續命；同時重跑既有的「續問視窗內繼續追問」情境確認沒有迴歸。`npm test` 全數
重跑通過（130 項流程測試）。

### 批次 15：活動清單附上「媒體邀訪需求」按鈕（2026-08-28）

長官實測回報：問「換一場活動」或查活動列表時，按鈕列只看得到活動名稱，找不到
入口問「媒體邀訪需求」——這件事本來就不是針對某一場活動，是跨活動的議題／
窗口詢問（批次 9 的 `sendGlobalContactMenu()`），塞在「先選一場」的清單裡反而
選錯位置，記者只能自己打字才問得到。

**根因**：`calendarQuickReplyItems()` 只回傳活動名稱，`handleMetaIntent()` 的
`switch`／預設 `calendar` 分支跟 `handleUnbound()` 的 `calendar` 分支三處直接拿
這組結果當按鈕列，都沒有附上批次 7 就已經存在、`eventQuickChips()` 每則問答
都會附的 `CONTACT_MENU_LABEL`（「媒體邀訪需求」）。

**修法**：新增 `calendarQuickRepliesForReporter()`，跟 `eventQuickChips()` 同一招——
固定把 `CONTACT_MENU_LABEL` 加在清單最後一格，換掉上述三處記者端的呼叫。點下去
沿用既有的 `detectMetaIntent()` → `'contacts'` → `sendGlobalContactMenu()` 那條路，
不需要新邏輯，只是讓記者在「先選一場」的畫面就看得到這顆按鈕。

**刻意不套用的地方**：`handleStaffMessage()` 自己的 `calendar` 分支（第四個
`calendarQuickReplyItems()` 呼叫點）維持原樣不動——「媒體邀訪需求」是講給記者聽
的措辭，同仁已經有整套 `STAFF_QUICK_REPLIES`，加這顆只是多一顆用不到的按鈕。

**測試**：`test-flow.mjs` 新增情境 16，涵蓋「換一場活動」、綁定中查列表、沒綁定
查列表（刻意用不含活動／場次字樣的「最近如何」，確保真的測到 `handleUnbound()`
自己那條分支，不是被 `detectMetaIntent()` 攔走）三處都附上按鈕、點下去正確導向
全域窗口清單、以及職員模式不受影響。`npm test` 全數重跑通過（135 項流程測試）。

### 批次 16：給 routeIntent() 補上「目前在問哪一場」，續問視窗的閒聊安靜門檻補齊（2026-08-28）

長官實測回報：批次 14 上線後，群組續問視窗內講一句完全不相關的話（「友信你覺得
呢」，沒有 @ 任何人），機器人還是硬答了一段「這超出本次活動範圍」之類的內容，
感覺像亂回。

**根因**：批次 14 只做了「明確 @ 別人」這個高信心度、零風險的子集，並且在批次
14 的紀錄裡就已經寫明白為什麼——`routeIntent()` 一次只看單一句話 + 行事曆清單，
沒有對話記憶，分不出「那合作廠商有哪些」這種依賴上一句才聽得懂的合法續問，跟
「友信你覺得呢」這種純聊天的差別，兩者在它眼裡都是「看不出跟哪場活動有關」，
一律判成 `other`。當時因為這個限制沒解，不敢直接拿 `intent==='other'` 當安靜
門檻，只做了「@ 別人」這個子集，把「其餘的閒聊」留給下一批。

**修法**：`routeIntent()` 新增可選的 `currentEventId` 參數——把「目前這個對話
正在問哪一場」補進提示，讓它能夠分辨「看起來像是在延續這場的討論」跟「真的
無關」。這段提示刻意不塞進原本那份逐 byte 穩定、吃 ephemeral cache 的
`systemPrompt`，獨立成第二個不快取的 system 區塊——因人而異的內容混進去只會
打散大清單本來的快取命中率。

有了這個提示，`other` 才是「連目前這場都接不上」的結果，`handleGroupMessage()`
才能放心把「沒被 @、又判成 other」也當安靜門檻，補齊批次 14 留下的缺口；`@`
到時完全不受影響（跟 1 對 1、跟 `handleUnbound()` 的 `silentOnOther:false` 同一
原則）。同一個提示也順手用在 1 對 1 跟群組既有的換場判斷上，減少沒有明確線索
時被誤判成 `other`、進而誤觸換場的機會（批次 13 的邏輯本身沒變，只是判斷更準）。

**測試上的坑**：測試裡假的 `fakeReporterRoute()` 純靠關鍵字比對文字，不會真的
理解語意，加了 `currentEventId` 提示後如果不同步更新，既有的「續問視窗內繼續
追問（那合作廠商有哪些）」測試會被新的安靜門檻誤傷——先跑一次草稿版本就直接
打掉這項既有測試，跟批次 14 曾經否決「直接用 `other` 當門檻」踩到的是同一個坑。
修法是讓 `test/fakes.mjs` 的 fetch stub 一併解析第二個 system 區塊，模擬「沒有
關鍵字線索、但也不像在問別的事，就當作延續目前這場」的簡化判斷（不是要精準
模擬真的 LLM，只求測試能同時顧到「合法續問不擋」跟「回報案例會擋」兩頭）。

**測試**：`test-flow.mjs` 新增「訊息跟目前這場無關（沒有 @ 別人）→ 安靜」情境
（含確認續問視窗沒有因此被續命）、「1 對 1 完全不受影響」情境、以及重跑「合法
續問不能被連帶擋掉」的迴歸驗證。`npm test` 全數重跑通過（139 項流程測試）。

### 批次 17：活動清單文字補上「媒體邀訪需求」入口，措辭修正（2026-08-28）

長官測完批次 15 的按鈕後回報：按鈕真的有出現，但滑一排按鈕不夠明顯，容易被
忽略——希望文字裡也講到這個入口，不能只靠按鈕。

**修法**：新增 `CONTACT_MENU_TEXT_HINT`，跟 `calendarQuickRepliesForReporter()`
同一組、同樣只給記者端的活動清單用，固定接在 `formatCalendarReply()` 的結果
後面。`handleStaffMessage()` 自己的 `calendar` 分支維持不套用，理由同批次 15。

**第一版措辭有誤，緊接著就被實測抓到**：第一版寫「如果是**跨活動**的採訪需求，
不是問特定一場，直接打『媒體邀訪需求』」——長官綁定著「2026晶鏈高峰論壇」（該
場剛好已經填了新聞聯絡人）時點下去，拿到的是**這場自己的**聯絡窗口，不是全域
清單，跟文字說的對不上。

**根因不是新 bug，是重新發現了批次 8/9 既有的設計**：`handleMetaIntent()` 的
`'contacts'` 分支本來就是兩層——目前綁定的這場如果自己有設定聯絡窗口
（`events!P` 或 `press_contact`），優先給那組最精準的資訊；只有這場完全沒設定
時才退到 `sendGlobalContactMenu()` 的全域清單。這個優先順序是刻意的，這批**沒有
改**（跟使用者確認過，維持現況：已經綁定某場又設定過窗口時，就該優先給那場
自己的，這比一律丟全域清單更有用）。

**真正要修的是文字**：拿掉「跨活動」這個過度承諾的措辭，改成「有採訪窗口相關
的需求，直接打『媒體邀訪需求』或點下面的按鈕即可」——只承諾「這裡有這個入口」，
不承諾「一定給你全域清單」，跟 `'contacts'` 分支的實際行為（不管走哪一條分支）
都對得上。

**測試**：`test-flow.mjs` 在既有的按鈕測試旁補上文字內容的斷言（含職員模式不會
多出這段文字）；措辭修正沒有新增測試——既有斷言只檢查有沒有出現「媒體邀訪需求」
這個關鍵字，不檢查「跨活動」，改文字不影響既有測試。`npm test` 全數重跑通過
（143 項流程測試）。

### 批次 18：群組「只 @ 沒接問題」的引導詞不夠全面，改成自我介紹＋雙入口按鈕（2026-09-01）

回報的意見（附截圖）：群組裡只打 `@工研院MIA｜公關小特派`、後面不接問題，拿到的是
一句純操作說明「請在 @ 我的後面接您的問題，例如：@我 最近有哪些活動」——只教「怎麼
問」，沒講「可以問什麼」。而且範例只舉了「最近有哪些活動」一種，記者猜不到「媒體
邀訪需求」（批次 8/9 就已經做好的邀訪窗口查詢）這條路也走得通，等於功能做了記者
找不到入口。

**修法**：`handleGroupEvent()` 這句文案改成先簡短自我介紹（「你好，我是工研院 AI
助手米亞」），接著把記者最常問的兩個方向都講出來（最近有哪些活動／媒體邀訪需求），
再附三顆快速回覆按鈕（`最近有哪些活動`／`媒體邀訪需求`／`使用說明`）讓對方不用自己
打字——跟 `eventQuickChips()`／`calendarQuickRepliesForReporter()` 同一個既有原則：
只教「怎麼問」不夠，把「可以問什麼」直接攤開讓人點。「媒體邀訪需求」沿用既有的
`CONTACT_MENU_LABEL` 常數，不是另外造一個新字串，維持跟按鈕、chips 同一份路由
（`detectMetaIntent()` 已經認得，不需要改路由邏輯本身）。

⚠️ **附了按鈕之後，這句回覆本身也要 `touchGroupSession()`**：按鈕點下去送出的是
沒有 @ 的純文字，如果不順手把續問視窗打開，`handleGroupEvent()` 開頭那道「沒被 @
又不在視窗內 → 安靜」的門檻會直接把按鈕點擊吃掉，變成「按了沒反應」——這是這批
唯一改到行為（不只是文案）的地方：舊版「只 @ 沒接問題」是刻意不算「有回答」、不
續命的（沒有按鈕，續命也沒有意義），加了按鈕之後這個假設不再成立，比照其餘所有
「有回答」的路徑補上 `touchGroupSession()`。

**這批沒有動的地方**：「媒體邀訪需求」本身查到的內容（優先給活動自己的窗口，沒有
才退回全域清單）是批次 8/9/17 已經定案的行為，這批只是把「這個入口存在」講得更
清楚，不改查詢邏輯本身；1 對 1 私訊沒有這個問題（每則訊息本來就都算在對我們講話，
不會有「只 @ 沒接問題」這種狀態），所以只改群組這一支。

**還可以往下做、但需要使用者先拍板的部分**（見這批 commit 對話的討論）：目前
「媒體邀訪需求」問到的是**聯絡窗口**（電話／LINE ID），記者問完還是要自己撥電話
或加 LINE 才算真的送出邀訪需求。LINE-PLAN.md 第 4 節規劃過的 `intent: interview`
（AI 直接在對話裡收單：媒體、記者姓名、主題、期望時間、截稿時間，寫進
`media_requests` 分頁並推播通知同仁）從批次 3 就刻意沒做，原因寫在第 8 節：
「有人問了就會期待回覆，三天沒回比沒有這個功能更傷」，是否要接、接了誰在多久內
要回覆，需要使用者自己先想清楚、不是工程問題，這批不擅自決定要不要做。

**測試**：`test-flow.mjs` 情境 8 更新「只 @ 沒接問題」的既有斷言（訊息內容、附帶
按鈕），新增「按鈕點下去在續問視窗內真的接得住（不用重新 @），且沒有活動綁定時
正確導向全域邀訪窗口清單」的情境，獨立開一個乾淨群組避免被前一步驟的軟綁定狀態
干擾。`npm test` 全數重跑通過（146 項流程測試）。

### 批次 19：「妳能幫我什麼」答非所問，HELP_TEXT／兜底文案語氣加點米亞人設（2026-09-01）

回報的意見（附截圖）：群組裡 @ 問「妳能幫我什麼」，拿到的是「不確定您想問哪一場
活動——可以直接輸入活動名稱…」——記者問的明明是「你能做什麼」，回覆卻完全答非
所問。同時要求整體語氣更人性、更可愛，符合米亞的人設。

**根因**：`detectMetaIntent()` 的 `HELP_ABOUT_BOT_RE` 只認「你／這個帳號＋怎麼／
如何＋用／使用／操作…」這種「問怎麼操作」的句型，「妳能幫我什麼」是「問能力」，
不含「怎麼／如何」，接不住，落空之後掉進 `routeIntent()`——那句話跟任何場次都
無關，被判成 `other`，最後回到批次 3 就存在的萬用兜底文案，答案自然對不上問題。

**修法**：`lib/menu.js` 新增兩條規則式判斷（跟既有的 `HELP_ABOUT_BOT_RE` 同一個
保守原則：`$` 錨在結尾，只認「問句到此為止」的完整短句）：
- `HELP_CAPABILITY_RE`：「你／妳＋能／可以／會＋[幫我／提供／做]？＋什麼／哪些
  [忙／事／功能／服務]？[嗎／呢]？」——「你能幫我什麼」「你可以做什麼忙嗎」這類
  問句，「你能不能查一下這場記者會幾點開始」「你能提供什麼資料」這種問句後面還
  接著具體內容的正常提問不會誤觸（動詞後面還有受詞就不是在問能力）。
- `HELP_WHOAMI_RE`：「你／妳＋是＋誰／什麼／哪位／做什麼的」——「你是誰」是第一
  次遇到這個帳號很自然的開場白，一樣不該落到答非所問的萬用回覆。

兩條都命中就回 `'help'`，跟既有的 `HELP_EXACT_RE`／`HELP_ABOUT_BOT_RE` 一樣走
`HELP_TEXT`，不需要改路由邏輯本身。

**語氣**：`HELP_TEXT` 開頭加一句「嗨，我是米亞 🙂 來跟您說說怎麼使用這個帳號～」
（`test-flow.mjs` 既有斷言只檢查「怎麼使用這個帳號」這個子字串還在，這句本來就
留著當小節標題，沒有拿掉），順手把批次 8 就有、但 `HELP_TEXT` 一直沒提到的
「媒體邀訪需求」補進「② 直接問問題」那段——問「使用說明」的人本來就該知道這個
入口存在。`handleUnbound()` 的萬用兜底文案（`api/line.js`，"不確定您想問哪一場
活動"）語氣也一併放軟，並補上「媒體邀訪需求」按鈕（跟批次 15/17/18 同一個「三個
核心入口一起給」的原則）——這句其實是任何 `routeIntent()` 判不出來的情況都會
走到的萬用兜底，不是只有「真的問到別場」才會出現，讓記者順手看到三個入口比較好。

**這批沒有動的地方**：只改了「妳能幫我什麼」「你是誰」這兩類、以及被這兩類問題
直接波及的兩段文案；其餘幾十句既有回覆的語氣沒有全面重寫——使用者具體回報、
且這次的問答流程有實際碰到的地方才動，範圍刻意收窄，不是整個帳號的人格重寫。

**測試**：`test/test-menu.mjs` 補上 `HELP_CAPABILITY_RE`／`HELP_WHOAMI_RE` 的
正反例（11 個正例、4 個反例，157 項）；`test/test-flow.mjs` 新增三個情境（149
項）：群組 @ 問「妳能幫我什麼」→ 回使用說明、1 對 1 問「你是誰」→ 回使用說明、
真的判不出意圖時兜底文案仍在且附三個核心入口按鈕（迴歸，確認安全網沒被拿掉）。
`npm test` 全數重跑通過。

### 批次 20：產業趨勢問答（IEK 免費焦點清單，新功能，2026-09-01）

使用者原話：「想做一個產業趨勢，你直接連結 https://ieknet.iek.org.tw 抓取上面
資料，媒體問哪個趨勢，可以直接連結上後回答，但不提供完整簡報，如果媒體問後續
要詳細資料或邀訪，則連到產業趨勢窗口 朱則瑋」。

**動工前先查證網站，不是憑空猜規格**：實際用 `curl` 抓 IEKnet 首頁與「免費焦點」
清單頁（`https://ieknet.iek.org.tw/iekrpt/DefaultFree.aspx`）的原始 HTML 查證，
結論：
- IEKnet 是工研院自己的產業科技國際策略發展所（IEK）對外公開的產業情報平台，
  不是外部第三方網站。
- 「免費焦點」清單頁是**伺服器端直接渲染**的傳統 ASP.NET 頁面（無 JavaScript、
  無 Cloudflare／驗證碼），單純 `fetch()` 就能拿到完整內容，不需要 headless
  browser 或裝 HTML 解析套件，符合本專案「零依賴」的既有原則。
- 清單頁本身就同時附了標題、日期、**約 100 字的摘要**，不需要再逐篇進站抓詳情頁。
- ⚠️ 關鍵發現：**連「免費」文章本身都是摘要／前言，完整內容一樣要登入會員才看得到**
  （頁面明講「本文為免費文章，請您登入後使用」）。這代表使用者要的「抓資料回答但
  不提供完整簡報」不是我們自己要另外做一層閹割邏輯——**這本來就是來源網站自己的
  免費／付費分界線**，我們從頭到尾不會、也不可能碰到付費內容（沒有處理登入、沒有
  存任何帳密），天生就符合需求。

**資料流**（新模組 `lib/industry-trends.js`）：
- `fetchIndustryTrendDigest()`：`fetch()` 清單頁 → `parseDigestHtml()` 用 regex
  逐項挖出 `{title, date, abstract, url}`（約可拿到最近 10 篇跨產業別的摘要）→
  失敗（網路問題、網站改版讓 0 筆解析成功）一律回空陣列，不丟例外。
- 刻意**不做** `indu_idno`／`domain` 參數的產業別篩選——這兩個參數對應哪個產業別
  沒有公開文件可查，猜錯的風險比乾脆不篩選、把標題＋摘要全部交給 AI 去匹配記者
  問題還高。v1 先覆蓋「最近這 10 篇」，之後真的要擴大覆蓋範圍再視情況加。
- `parseDigestHtml()` 是對照**實測的真實原始 HTML** 寫的 regex，不是查文件得出的
  ——網站改版時這支會需要跟著調整，這是接受的維護成本，不是 bug。

**快取放在 `api/line.js`，不是 `lib/industry-trends.js`**：跟 `contactsDirCache`
同一個既有模式（`industryTrendCache`），TTL 拉長到 6 小時（不是 Google Sheets
配額考量，是「IEK 免費焦點實測更新頻率約每週幾篇，沒必要每次提問都重抓外部
網站」）。刻意不把快取邏輯放進 `lib/industry-trends.js` 本身——這支只管「抓＋
解析」這一步，維持跟 `lib/contacts-directory.js` 同一個「資料模組不管快取，快取
統一在 `api/line.js`」的分工，也讓 `test-flow.mjs` 的 `freshModule()`（重新
`import` `api/line.js` 清快取）能一併清掉這份快取，不會有測試情境之間互相汙染
殘留資料的問題。

**觸發方式**（AskUserQuestion 問過，選了推薦選項）：併入既有的自然語言路由，
不做成像「媒體邀訪需求」那樣的明確關鍵字入口。`lib/router.js` 的 `routeIntent()`
從三選一（`calendar/qa/other`）擴成四選一，多一種 `industry_trend`——記者不管
1 對 1 還是群組、有沒有綁定活動，隨口問「半導體最近有什麼趨勢」都答得到，不用
先知道要打什麼關鍵字。三個呼叫端都要接：`handleUnbound()`（沒綁定）、
`handleEvent()` 綁定中的判斷、`handleGroupMessage()` 綁定中的判斷——綁定中問到
產業趨勢題，**不會**被硬塞進目前綁定活動的問答（那場知識庫跟產業趨勢無關，只會
說「這部分我沒有資料」），也**不會**打亂原本的活動綁定，這題答完下一題還是回到
原本那場。

⚠️ **`currentEventId` 提示要一併調整**：批次 16 加的「目前正在問哪一場」提示原本
偏向「沒有明確指向別場就判成延續這場的 qa」，會把產業趨勢題也拉去硬答成目前綁定
的活動。補了一句「如果訊息是在問整體產業趨勢……請判成 `industry_trend`，不要因為
目前有綁定就硬塞成 qa」，明確排除這個誤判。

**回答邏輯**（`answerIndustryTrend()`，`api/line.js`）：不呼叫網頁版那套帶知識庫
全文的 `buildSystemPrompt()`——資料來源、規則完全不同，硬塞進同一支反而要多加
一堆「這不是活動內容」的例外判斷。獨立寫一份精簡 system prompt，規則明確要求：
只能根據清單裡的標題與摘要回答、不要用既有知識延伸、清單裡沒有明顯對應項目就
誠實說沒有、控制在 4 行以內、明確讓記者知道這是免費摘要不是完整報告。抓取失敗
時（`items.length === 0`）**不呼叫 Anthropic**，直接誠實告知抓不到資料，不要
裝死或硬答。

**回答結尾的警語＋公關窗口聯絡資訊**（使用者原話：「僅供參考，正式媒體報導引用
請聯繫 公關窗口 朱則瑋」）：跟 `lib/prompt.js` `buildSystemPrompt()` 既有的
「內容僅供參考，以工研院官網新聞稿或發言為準」警語同一種目的——這是 IEK 的免費
摘要，不是正式新聞稿，記者可能直接截圖引用，要把「這不是正式引用來源」講清楚。

聯絡人姓名電話**優先**讀 `events!contacts_directory` 裡「產業趨勢分析」這個
**既有**主題（批次 9 就有，種子資料在 `lib/contacts-directory.js` 的
`DEFAULT_CONTACTS_DIRECTORY`，目前已補上電話）；同仁之後在後台「邀訪窗口分工」
改名字或電話，這裡自動跟著更新，不需要改程式碼、不會有兩份聯絡資訊各自維護、
之後兜不起來的風險。

⚠️ **第一版使用者給的電話號碼「09342667766」是 11 碼，比台灣手機標準的 10 碼
多一碼**——當時判斷不出多的那一碼該從哪裡拿掉，先不敢塞進 `DEFAULT_CONTACTS_DIRECTORY`，
只在對話裡回報請使用者確認。使用者重新給了乾淨的 10 碼版本「0934267766」
（`0934` + `267` + `766`），姓名／電話都已經寫進 `DEFAULT_CONTACTS_DIRECTORY`；
同時 `industryTrendContactLine()` 也內建同一組號碼當**程式碼層級的預設值**——
`contacts_directory` 裡「產業趨勢分析」這個主題**任一欄位**（姓名或電話）還沒
填時，個別退回這組預設值，功能一上線就完整可用，不用等同仁先去後台補資料；
同仁之後在後台把任一欄填了，會自動改用後台那組，不是永遠鎖死這組預設值。
`DEFAULT_CONTACTS_DIRECTORY` 只在分頁第一次建立時當種子用（見批次 9），既有
正式站的 `contacts_directory` 分頁早就建立過，改這個常數不會回頭改到線上已經
存在的資料——這也是為什麼另外需要程式碼層級的預設值，不能只靠改種子常數。

**這批沒有做的事**：
- 沒有做產業別篩選（`indu_idno`），見上面的說明。
- 沒有寫進 `qa_log`——產業趨勢問答沒有對應的 `event`，`qa_log` 的既有欄位設計
  是圍繞著「這是哪一場活動的問答」，硬塞一個不存在於 `events` 表的假 id 進去，
  風險比暫時不記錄這批數字還高（`api/analytics.js` 的事件別統計可能因此對到一個
  查無此活動的 id）。之後真的要納入問答分析，需要先確認 `qa_log`／
  `api/analytics.js` 能不能安全地容納「沒有 event」這種列，不是這批的範圍。
- 沒有動 `lib/staff.js`／`routeStaffIntent()`——這次要滿足的是媒體端的需求，
  同仁的職員模式不受影響。
- 沒有做分頁／抓更多篇——v1 先覆蓋「最近這 10 篇」，這是刻意收斂的範圍。

**測試**：新增 `test/test-industry-trends.mjs`（15 項），`parseDigestHtml()` 對照
**實測的真實原始 HTML 節錄**（不是手工簡化過的乾淨片段，刻意保留 script／圖片區塊
等雜訊）驗證解析正確、HTML 實體解碼、防呆（空字串／null／不相關 HTML／缺標題）。
`test/fakes.mjs` 新增 IEKnet 網址的 fetch 攔截（`state.iekHtml`／
`state.iekFetchFail`）與 `fakeReporterRoute()` 的 `industry_trend` 判斷。
`test/test-flow.mjs` 新增情境 17（15 項）：沒綁定 1 對 1／群組都答得到、system
prompt 真的帶了 IEK 清單內容、回覆附上警語與聯絡窗口（使用者要求的措辭）與快速
回覆按鈕、後台完全沒設定該主題時退回程式碼內建的預設聯絡人、已綁定活動時不會
被硬塞進當前活動問答且不打亂原本綁定、群組續問視窗內（沒 @）照樣答得到並續命
（不被 `silentOnOther` 安全門檻誤擋）、抓取失敗時誠實告知且不呼叫 Anthropic。
`npm test` 全數重跑通過（新增 `test/test-industry-trends.mjs` 後，`test/` 底下共
10 個測試檔、合計 460 項全綠燈）。

### 批次 21：「想問什麼技術」問答＋圖文選單改版（工研院官網新聞中心，新功能，2026-09-01）

使用者原話（節錄）：「1對1 圖卡 活動照片改 產業趨勢分析。使用說明 改想問什麼技術？
幫我抓工研院官網新聞室去抓取相關資料 https://www.itri.org.tw/ListStyle.aspx?
DisplayStyle=06&SiteID=1&MmmID=1036276263153520257，如果要進一步詢問，導到媒體
邀訪窗口聯絡。群組對話 也加入產業趨勢分析、想問什麼技術兩個小按鈕 並且如何記者
問相關問題也能回答」。

**動工前先查證網站**（跟批次 20 同一個紀律，不是憑空猜規格）：
- 該網址是工研院官網「新聞中心 > 最新新聞」清單頁，伺服器端直接渲染，無 JS、
  無防爬蟲機制，跟 IEK 那頁同一種傳統 ASP.NET 頁面。
- ⚠️ 關鍵發現：這頁本身就支援關鍵字搜尋——在 `../js_page/DisplayStyle06.js` 裡
  找到「搜尋」按鈕的 click handler，組出來的是 `...&Page=1&keyword=<關鍵字>` 這個
  純 GET query string（不是 AJAX API）。這是這個功能能不能真的答得出東西的關鍵：
  工研院官網橫跨的技術領域太廣，只抓「最新 10 篇」很難剛好蓋到記者問的那個技術，
  要能用記者給的技術名稱去查才有意義。
- 每頁固定 10 篇（`lblPageSize` 標籤實測），v1 只看第一頁，跟批次 20 同一個「先求
  可用」的取捨。

**資料流**（新模組 `lib/itri-news.js`）：`fetchItriNews(keyword)` → `fetch()` 帶
關鍵字的搜尋結果頁 → `parseNewsListHtml()` 用 regex 挖出 `{title, date, abstract,
url}` → 回傳 `{ok, items}`，`ok:false` 是「抓取失敗」（網站連不上），`ok:true` 但
`items` 空陣列是「查無資料」（正常結果，不是官網每個技術都報導過）——兩者要分開
處理，見該支開頭的說明。`decodeHtmlEntities()` 抽成新的共用模組
`lib/html-text.js`，`lib/industry-trends.js` 跟這支共用同一份，不重複維護兩份
幾乎一樣的 HTML 實體解碼邏輯。

**兩顆按鈕、兩套故事，不硬併成一個**：
- 「產業趨勢分析」：直接答，跟打字問「最近趨勢」是同一條路（`detectMetaIntent()`
  新增完全比對規則，見 `lib/menu.js`），資料來源仍是批次 20 的 IEK 免費焦點清單。
- 「想問什麼技術」：不能直接答——按鈕本身不是技術名稱。先問一次「想了解工研院
  哪一項技術」，記一個一次性旗標（`TECH_QUERY_PENDING_NOTE`，複用
  `setContactPending()` 讀寫 `line_users` F 欄，跟批次 9「邀訪：其他」那個一次性
  旗標同一支、不同字串），下一則不管記者打什麼都當成技術名稱直接去查
  （`handleTechQueryMessage()`），跟 `handleContactTopicMessage()` 同一個模式、
  同一個優先順序（1 對 1、群組都要接，且要排在 `binding` 判斷之前）。

**自然語言也要能問**（記者不用先按按鈕）：`lib/router.js` 的 `routeIntent()` 從
四選一（`calendar/qa/industry_trend/other`）擴成五選一，多一種 `tech_query`。
判斷依據刻意選「問句裡有沒有明確提到『工研院』」這個機械化訊號，不是憑語意猜
「這是在問市場還是在問工研院自己」——兩者用詞常常很像（「機器人技術最近有什麼
進展」既可能是問整體市場、也可能是問工研院自己），硬要 AI 靠語意分辨風險高，
用「有沒有點名工研院」這個明確訊號分流才可靠。三個呼叫端都要接，跟批次 20 加
`industry_trend` 時同一套：`handleUnbound()`、`handleEvent()` 綁定中的判斷、
`handleGroupMessage()` 綁定中的判斷，一律不做軟綁定、不打亂原本活動綁定。

**回答邏輯**（`answerTechQuery()`，`api/line.js`）：跟 `answerIndustryTrend()`
平行的另一支，不共用——資料源不同、查詢方式不同（這支要帶關鍵字去查，
`answerIndustryTrend()` 是固定抓最新清單）、結尾窗口導引邏輯也不同。system prompt
沿用批次 20 加的「來源編號」機制（見批次 20 之後的另一個修正：`extractSourceIndices()`
／`resolveSourceUrls()`，讓 AI 引用了清單第幾則就換回對應網址附在回覆後面，不用
另外開一批）。結尾窗口導引**優先用記者的關鍵字比對出對應技術領域的專屬窗口**
（複用批次 9 `lib/contacts-directory.js` 的 `matchGlobalContactByText()`——記者
打「機器人」，直接給機器人技術領域那位窗口，不是只回一句「請洽媒體邀訪窗口」讓
記者自己再點一次按鈕），比對不到才退回一般性的邀訪窗口指引。

**圖文選單改版**（`lib/menu.js` `REPORTER_MENU`）：六格是稀缺資源，把「使用說明」
換成「想問什麼技術」、「活動照片」換成「產業趨勢分析」——換掉的兩個都沒有真的
消失，「使用說明」還是打這幾個字就叫得出來，「活動照片」也還是打「給我照片」就
問得到，只是不再佔固定選單的一格。底圖是 `assets/build-richmenu.mjs` 從
`REPORTER_MENU` 定義自動生成的截圖，改完定義要重新跑這支腳本重新產生
`public/richmenu-reporter.png`，**上線後同仁還要在 LINE 用職員模式重打一次
「設定圖文選單」，正式站才會換成新的圖跟新的按鈕行為**（`handleSetupRichMenu()`
會去抓部署後的 PNG 網址重新建立／連結圖文選單，不是自動生效）。

**群組快速回覆也加了同樣兩顆**：群組沒有持續顯示的圖文選單，「只 @ 沒接問題」
那則歡迎訊息附的按鈕是群組唯一一次看得到「還能問什麼」的機會（見批次 18），把
新按鈕加進那則訊息的快速回覆清單，跟 1 對 1 圖文選單同步。

**這批沒有做的事**：
- 沒有做技術別的自動分類或篩選——關鍵字搜尋直接把記者的原話（或按鈕之後打的
  技術名稱）丟給工研院官網自己的全文檢索，不在程式碼這層再做一次篩選判斷。
- 沒有針對搜尋結果做快取——跟批次 20 的 IEK 清單不同，這裡每個關鍵字理論上都是
  不同的查詢，快取每個關鍵字的價值有限，v1 先求可用、不做這層優化。
- 沒有動 `lib/staff.js`／`routeStaffIntent()`——這次一樣是媒體端的需求。

**測試**：新增 `test/test-itri-news.mjs`（16 項），`parseNewsListHtml()` 對照實測
的真實原始 HTML 節錄驗證解析正確；`decodeHtmlEntities()` 抽成 `lib/html-text.js`
後 `test/test-industry-trends.mjs` 全數重跑仍然通過，證明搬移沒有改變行為。
`test/test-menu.mjs` 補上兩顆新按鈕的 `detectMetaIntent()` 判斷（含「絕對不能
誤觸正常提問」的防呆案例）與圖文選單按鈕清單更新。`test/fakes.mjs` 新增工研院
官網新聞中心網址的 fetch 攔截、`fakeReporterRoute()` 的 `tech_query` 判斷，並把
`contactsDirectory`／`iekHtml`／`itriHtml` 這三個原本只在物件字面量裡寫死、
`reset()` 不會還原的假資料抽成獨立常數——這是**實際加測試時踩到的坑**：批次 20
「後台完全沒設定產業趨勢分析窗口」那個情境會把 `contactsDirectory` 換成精簡版，
`reset()` 沒有把它還原，導致跑在它後面的新情境讀到殘留的精簡版、找不到「機器人」
這個主題，比對永遠落空——三個一併抽成常數、`reset()` 統一還原，之後不管哪個
情境換了假資料，後面的情境都拿得回原本的預設值。`test/test-flow.mjs` 新增情境 18
（12 項）：按鈕先問再查、答案帶對關鍵字比對到的專屬窗口、群組續問視窗內接得住、
自然語言直接問工研院技術也答得到且不打亂原本活動綁定、查無資料誠實告知、抓取
失敗誠實告知且不呼叫 Anthropic。`npm test` 全數重跑通過。

### 批次 22：修「想問什麼技術」自然語言問句查不到資料——routeIntent() 順手抽關鍵字（2026-09-01）

批次 21 上線後陸續回報兩類問題，都是「新功能上線後才會被使用者踩到」的落空：

**其一（小修，不另開批次）**：`TECH_QUERY_EXACT_RE`／`CONTACT_EXACT_RE`／
`INDUSTRY_TREND_EXACT_RE` 補上「技術呢」「邀訪呢／窗口呢」「趨勢呢」這類口語
追問句型——群組續問視窗內打這幾句完全沒反應，跟批次 19「你能做啥」同一種落空
（固定觸發詞沒認出來 → 掉進 `routeIntent()` → AI 判成 `other` → 群組安靜門檻），
刻意不讓裸詞（「技術」「窗口」「趨勢」）單獨命中，只接帶語助詞的講法，避免跟
既有情境（例如「技術規格」只打「技術」不該算命中）互相踩線。

**其二（這批的重點）**：使用者原話「最近半導體 工研院有什麼新聞嗎」——問句裡有
「工研院」，`routeIntent()` 正確判成 `tech_query`，但 `answerTechQuery()` 把
**整句原話**直接拿去查工研院官網的關鍵字搜尋，查到 0 筆，回覆「沒有找到直接相關
的報導」。

**動工前先實測查證**（不是憑空猜「應該是關鍵字太長」）：用 `curl` 直接打同一個
搜尋端點比較兩種輸入：
- `keyword=最近半導體 工研院有什麼新聞嗎`（整句原話）→ 搜尋找到 **0** 筆資料
- `keyword=半導體`（抽出來的核心關鍵字）→ 搜尋找到 **214** 筆資料

證實工研院官網的關鍵字搜尋接近精準比對，不是能容忍語助詞、疑問句尾的全文檢索——
批次 21 當時「整句拿去查通常比程式自己再猜一次可靠」的假設是沒有實測驗證過的
錯誤假設。

**修法**：不額外多打一次 API——`routeIntent()` 本來就會呼叫一次 Haiku 判斷意圖，
判成 `tech_query` 時**同一次呼叫**順手要求 AI 從問句裡抽一個簡短技術關鍵字（例如
「半導體」「機器人」），放進回傳的 `tech_keyword` 欄位；`answerTechQuery()` 的
三個呼叫端（`handleUnbound()`、群組／1 對 1 綁定中的判斷）都改成優先用
`tech_keyword`，抽不出來（空字串）才退回整句原話當保底。「想問什麼技術」按鈕
之後記者自己打技術名稱的那條路（`handleTechQueryMessage()`）不受影響——那條路
本來就沒有經過 `routeIntent()`，記者通常直接回一個技術名稱，不是完整問句。

**測試**：`test/fakes.mjs` 的 `fakeReporterRoute()` 補上簡化版的關鍵字抽取模擬
（不是精準模擬真的 LLM 抽詞，只求「大致抽掉語助詞」讓測試分得出「用了抽出來的
關鍵字」跟「還是整句原話」）；`test/test-flow.mjs` 情境 18 新增案例，重現「工研院
半導體有什麼新聞嗎」這句，驗證送去查、送給 AI 回答的都是抽出來的「半導體」，不是
整句原話。`npm test` 全數通過。

---

### 批次 23：修「想問什麼技術」按鈕流程打一整句一樣查不到——fetchItriNews()
查無資料自動去語助詞重試（2026-09-02）

批次 22 上線後又收到一則回報（附截圖）：這次不是自然語言問句，是**按鈕引導
流程**——按了「想問什麼技術」，機器人問「請問您想了解工研院哪一項技術呢？」，
使用者回「最近的國際合作」，得到跟批次 22 修的那則一模一樣的「工研院官網新聞
中心目前沒有找到跟「最近的國際合作」直接相關的報導」。

批次 22 寫的時候明確假設這條路「不受影響」：「『想問什麼技術』按鈕之後記者自己
打技術名稱的那條路（`handleTechQueryMessage()`）不受影響——那條路本來就沒有
經過 `routeIntent()`，記者通常直接回一個技術名稱，不是完整問句。」這則回報證實
這個假設錯了：記者不會照範例乖乖打單一技術名稱，一樣會打完整句子；
`handleTechQueryMessage()` 把整句原話原封不動傳給 `answerTechQuery()`，跟批次
22 修之前的自然語言路徑踩的是同一個坑。

**動工前先實測查證**（跟批次 20-22 同一個紀律，不是憑空猜規格）：用 curl 直接
打搜尋端點比較：
- `keyword=最近的國際合作`（回報的原句）→ 搜尋找到 **0** 筆資料
- `keyword=國際合作`（去掉「最近」「的」）→ 搜尋找到 **10** 筆資料

跟批次 22「半導體」那個案例同一種落差，證實不是單一個案，是這個搜尋端點
「接近精準比對」的通性。

**修法**：這次不在呼叫端（`handleTechQueryMessage()`）加邏輯，改在資料層
`lib/itri-news.js` `fetchItriNews()` 統一補一層保底——第一次查詢查無資料時，用
新增的 `stripTechQueryFiller()`（regex 拿掉「工研院」「最近」「最新」「有什麼」
「新聞」「進展」「的」「嗎」「呢」等常見語助詞／泛稱／標點）清洗關鍵字，清洗後
**跟原字串不一樣**才值得重試一次（一樣代表沒東西可去，重試只是白跑一趟）；重試
查到就用重試結果，重試本身失敗（剛好網路閃斷）不算「抓取失敗」——第一次已經
證實官網連得上，退回第一次查到的空清單，誠實回答「查無資料」，不會改口說
「暫時抓不到資料」嚇到記者。

選在這一層修、不是在 `handleTechQueryMessage()` 另外加一次關鍵字抽取，理由：
1. 按鈕引導的整句、自然語言路徑 `tech_keyword` 抽取失敗時的整句退回值，兩條路
   最後都會走到同一支 `fetchItriNews()`，修這裡一次到位，不用兩邊各自補一次。
2. 不用再打一次 Anthropic API 抽詞——零成本、零延遲，維持 `lib/itri-news.js`
   開頭訂的「零依賴、只用內建 `fetch()`」原則。
3. 這是保底，不是要取代批次 22 的 AI 抽詞——`routeIntent()` 抽出來的關鍵字通常
   比 regex 去語助詞更乾淨、更準，這層只在「AI 沒抽到／按鈕流程本來就沒有 AI
   抽詞」時墊底。

同時訂正 `api/line.js` `answerTechQuery()` 上面一段過時的註解：批次 21 寫的時候
誤判「工研院官網的關鍵字搜尋是全文檢索，整句拿去查更可靠」，批次 22 已經證實這
個假設是錯的，但當時只改了自然語言路徑那三個呼叫點的程式碼，沒有一併訂正這段
註解，讓下一個讀這支函式的人（包括這次的我自己）還是會先看到「整句拿去查更
可靠」這個已經被推翻的說法——一併訂正，並補上「查無資料時的重試邏輯統一放在
`fetchItriNews()` 裡，這支不用自己假設 keywordText 已經是乾淨關鍵字」的指引。

**測試**：`lib/itri-news.js` 新增 `stripTechQueryFiller()` 為具名 export，
`test/test-itri-news.mjs` 直接補純函式單元測試（含這次回報的原句「最近的國際
合作」）。`test/fakes.mjs` 的工研院官網 fetch stub 補上 `itriKeywordMustInclude`
（預設空字串＝關閉，不影響任何既有測試的既有行為）——設了字串才會真的檢查 URL
帶的 `keyword` 參數，模擬「整句查不到、關鍵字查得到」的真實落差；
`test/test-flow.mjs` 情境 18 新增案例，重現「按鈕引導流程回『最近的國際合作』」
這句，驗證去語助詞重試後真的查得到、不會誤報查無資料。`npm test` 全數通過
（`test-flow.mjs` 183 個檢查、`test-itri-news.mjs` 21 個檢查，其餘既有測試檔案
不受影響，整體全綠）。

**什麼故意沒做**：沒有把 `stripTechQueryFiller()` 的語助詞清單做成可設定、也
沒有做真的中文分詞——這支的角色是「查無結果時的保底」，不是精準的關鍵字抽取，
抽得不夠乾淨（殘留一兩個字）不影響最終結果，反正查到的清單最後還是交給 AI 自己
判斷相不相關（見 `answerTechQuery()` 的系統提示規則），沒必要為了一個保底機制
去養一份分詞規則。

---

### 批次 24：主動盤點「類似狀況」——不是等下一次回報，一次把同一種句型的漏網
之魚補齊（2026-09-02）

使用者的要求：「能否盤點相關對談出現類似狀況可能性一起解決，讓他對答可以更
流暢」——不是針對某一句新回報，是要求回頭檢查整個對話流程裡還有沒有同一種
「類別」的問題。批次 19-23 累積下來，真正重複出現的落空只有兩種形狀：

1. **固定觸發詞的口語追問句型**（「主題詞 + 呢／嗎」）沒被 `detectMetaIntent()`
   認出來，掉進 `routeIntent()` 判成 `other`，群組續問視窗內（沒被 @）`other`
   是安靜門檻——「你能做啥」「技術呢」「窗口呢」都是這一種，每次都是個別回報後
   單點修掉一個詞。
2. **外部近似精準比對搜尋**查完整句子查不到——只有 `fetchItriNews()` 這一種
   （見批次 22、23），已經在資料層修到兩條呼叫路徑都受益，這次盤點確認沒有
   第三條漏網的路徑。

**盤點做法**：不是猜還缺什麼詞，是把兩種形狀分別檢查過一輪。

**第一種（`lib/menu.js`）**——批次 19-21 陸續補的「呢／嗎」句型只涵蓋到當時
實際回報的那幾個詞（技術、趨勢、邀訪、窗口），從沒回頭檢查同一份選單裡**其餘**
固定觸發詞是不是也有同一個缺口。用 `node -e` 把 `detectMetaIntent()` 的六類
規則分別跟一批候選字（圖文選單六顆按鈕、`HELP_EXACT_RE` 既有清單裡的每個名詞）
配上「呢／嗎」實測，抓到五個真的會漏接的缺口：

| 類別 | 新增 | 理由 |
|---|---|---|
| calendar | `活動呢／活動嗎／場次呢／場次嗎／記者會呢／記者會嗎` | 六顆按鈕裡唯一還沒補過這個句型的一類 |
| help | `說明呢／教學呢／功能呢／使用方式呢／嗎`（及其餘同組名詞） | 只挑「名詞 + 呢／嗎」語意不變的詞，`不會用嗎`／`不知道怎麼用嗎` 這類動詞片語刻意不收——加了「嗎」語意會反過來變成在問對方，不是同一件事 |
| contacts | `聯絡呢／聯繫呢／採訪呢`（及 嗎） | 跟已收的「邀訪／窗口」同一批固定詞的口語代稱；`媒體呢` 刻意不收，兩個字太泛，容易跟「媒體效應」這類正常提問的字面重疊 |
| industry_trend | `產業趨勢呢／產業趨勢嗎` | 跟已收的「趨勢呢」同一句話更完整的講法 |
| tech_query | `技術查詢` | 跟既有清單裡的「技術詢問」同義詞，記者兩種講法都可能打 |

全部用 `^...(呢|嗎)$` 全字串錨定（跟已修的那幾條同一個寫法），不是放寬成子字串
比對——錨定才擋得住「這場活動呢，我還想問別的」這種「呢」當句中語氣詞（不是
疑問句尾）的正常說法被誤觸。動工前先用候選清單實測過一輪（含刻意設計的「不該
命中」案例），確認新規則不會誤傷既有情境，才寫進正式規則。

**第二種（外部搜尋）**——檢查了 `lib/industry-trends.js`（IEK 產業情報網）跟
`lib/contacts-directory.js`（`matchGlobalContactByText()`），確認沒有同一種
「近似精準比對，整句查不到」的問題：
- `lib/industry-trends.js` 的 `fetchIndustryTrendDigest()` 本來就不是關鍵字
  搜尋——固定抓最新清單，不會有「整句查不到」這回事，批次 20 設計時就是這樣，
  不是這次盤點才發現。
- `matchGlobalContactByText()` 是反過來的方向：檢查「主題詞有沒有出現在記者的
  整句話裡」（`t.includes(c.topic)`），語助詞、疑問句尾都不影響這個判斷，跟
  `fetchItriNews()` 那種「查詢字串要短、乾淨才比對得到」的近似精準比對是不同的
  機制，天生就不怕整句話——這條路徑是安全的，不需要修。

**這次刻意沒做的事（想過、評估過風險，決定不做）**：`routeIntent()`（`lib/router.js`）
目前只認得出 `calendar／qa／industry_trend／tech_query／other` 五種意圖，
`help` 跟 `contacts` 完全不在 AI 分類器的選項裡——代表**任何**沒被
`detectMetaIntent()` 攔到的完整自然語句（不只是「主題詞+呢」這種短句，包括
「請問要怎麼安排採訪」這種完整問句）問使用說明或邀訪窗口，AI 都沒有正確的分類
可選，只能猜進 `qa／calendar／industry_trend／tech_query` 其中一個或退回
`other`。這是比這次修的「口語追問句型」更深一層的架構限制，考慮過把 `help`／
`contacts` 也教給 `routeIntent()`，但沒有動：
1. 目前每一次實際回報、每一次盤點抓到的落空，全部都是「固定觸發詞的口語變體」，
   從沒出現過「完整自然語句問使用說明／邀訪窗口」被誤判的實際案例——這層風險
   目前只是理論上存在，不是已經發生過的問題。
2. `lib/menu.js` 開頭寫得很清楚：這幾類意圖刻意選擇「純關鍵字、不呼叫 AI」，
   為的是 100% 命中、不需要語意理解；把 `help／contacts` 交給 AI 分類器，
   等於引入這份設計原本要避開的不確定性——AI 要多分辨「問這場活動自己的
   聯絡人」（該判 `qa`）跟「想要跨場次的技術窗口清單」（該判 `contacts`）這種
   更細的語意差異，判斷難度比現有的四選一更高，出錯後果（答錯場次的內容、或
   把記者導去不對的窗口）也比「多問一次」更難察覺。
3. 這不是「以後都不做」，是「現在沒有實測證據支撐要做」——如果之後真的收到
   「完整問句問邀訪窗口沒接住」的回報，再照這次的紀律：先實測重現、再評估要
   加規則還是要教給 AI，不要現在憑空猜。

**測試**：`test/test-menu.mjs` 新增五段（calendar／help／contacts／
industry_trend／tech_query 各一段），每段都包含「應該命中的新案例」跟「刻意
設計、不該被攔走的案例」兩組——後者是防線，不是裝飾，例如 `活動嗎`（前面還有
「這場」不算整句）、`不會用嗎`（動詞片語加語助詞語意會反過來）、`媒體呢`
（裸詞太泛不收）都各自驗證新規則沒有意外放寬。`npm test` 全數通過
（`test-menu.mjs` 224 個檢查，其餘既有測試檔案不受影響，整體全綠）。

---

### 批次 25：「換一場活動」按鈕改名成「回首頁」（回報的意見，2026-09-02）

回報的意見（附截圖，圈起這顆按鈕）：這顆按鈕原本叫「換一場活動」🔄，但這個帳號
不是只能問活動——還能問產業趨勢分析、工研院技術、找邀訪窗口，只用「換一場
活動」描述，範圍寫得比實際功能窄。

**這顆按鈕實際在做什麼**：清掉目前的活動綁定，回一句提示＋活動清單。清掉綁定
之後，記者可以重新選一場，**也可以**改問產業趨勢、工研院技術——這兩件事本來
就不需要先綁定活動（`handleMetaIntent()` 的 `industry_trend`／`tech_query`／
`contacts` 三個分支在函式最前面就處理掉了，不看有沒有 `binding`）。所以這顆
按鈕從一開始做的事就不只是「換一場活動」，只是名字沒有把這件事講出來。

**修法**：改名成「回首頁」🏠（原本 🔄），更準確描述「清掉目前狀態、回到可以
做任何事的起點」，不是單純換一場活動：

- `lib/menu.js`：`REPORTER_MENU` 的按鈕定義（icon／label／sub／text 一起改）；
  `SWITCH_RE` 新增「回首頁／回到首頁／回主選單」，`換一場活動` 等舊講法**保留**
  在規則裡繼續有效——跟批次 21「使用說明／活動照片」換位置時同一個原則，換掉
  的入口不會真的消失，記者手打舊講法一樣叫得動。
- `api/line.js` 的 `switch` 分支：回覆文字從「已經離開原本那一場」改成「已經
  回到首頁」，並且順手點出「也可以打『產業趨勢分析』『想問什麼技術』問其他
  問題」——不是只列活動清單，回覆本身也要反映這顆按鈕不是只跟活動有關。
- `lib/menu.js` 的 `HELP_TEXT`／`buildWelcomeFlex()`：兩處「想換一場／打
  『換一場活動』」的說明文字一併更新成「回首頁」；`HELP_TEXT` 順手補上一句
  「想了解產業趨勢或工研院的技術，打『產業趨勢分析』『想問什麼技術』就可以，
  不用先選活動」——這句本來就該在使用說明裡，只是一直沒補，這次順手一起修。
- `assets/build-richmenu.mjs` 重新產圖，`public/richmenu-reporter.png` 換成
  新版（🏠／回首頁／看更多功能），已用 Read 工具肉眼確認排版正常、字沒有擠壓
  或截斷。

**測試**：`test/test-menu.mjs` 補「回首頁／回到首頁／回主選單」判成 `switch`
的案例，`isMetaButton` 那條選單按鈕巡檢的比對清單同步更新；`test/test-flow.mjs`
情境 1、16 的 `send('換一場活動')` 改成 `send('回首頁')`（驗證按鈕現在真正送出
的文字），並新增一個檢查：回覆文字裡真的有「產業趨勢分析」「想問什麼技術」這
兩個字眼，不是只列活動清單。`npm test` 全數通過（`test-flow.mjs` 184 個檢查，
其餘既有測試檔案不受影響，整體全綠）。

**上線需要的手動步驟**：這是圖文選單的圖片異動，不是純程式碼——`public/
richmenu-reporter.png` 換版之後，還是要照 LINE-PLAN.md 第 7 節既有流程，用
職員模式重打一次「設定圖文選單」，線上帳號才會真的換成新圖（純改 `lib/menu.js`
裡的定義不會自動同步到 LINE 後台已經上傳的圖文選單，這點從批次 1 就是這樣，
不是這次新增的限制）。

---

### 批次 26：問完產業趨勢再追問一個名詞 → 「我沒抓到您想問哪一場活動」（回報的截圖，2026-09-05）

回報的意見（附截圖）＋一次盤點的五個題目：①記者不要問 A 答 B ②有沒有 bug
③降低類似截圖的狀況 ④對答更有人味、符合米亞的人設 ⑤修改建議。

**截圖裡發生的事**：記者問產業趨勢 → 拿到 IEK 免費焦點的摘要，回覆結尾還主動
寫著「如果您對清單裡的其他產業趨勢感興趣，或有更具體的技術領域（如衛星通訊、
太空科技等），歡迎再提問」→ 記者照著打了「太空」兩個字 → 收到「嗯～我沒抓到
您想問哪一場活動耶 🤔」。

**根因**：`answerIndustryTrend()`／`answerTechQuery()` 答完什麼狀態都不留。下一則
訊息進 `routeIntent()` 時，那支只拿得到「目前綁定哪一場活動」（`currentEventId`，
批次 16 加的），完全不知道「上一則剛聊完產業趨勢」——「太空」是個沒有任何活動
線索的裸名詞，判成 `other` 完全合理，錯的是沒有人記得上一句在聊什麼（跟
`lib/staff.js` 那個「追問哪一場」是同一種病）。而落到 `other` 之後的萬用兜底文案
又寫死成「我沒抓到您想問哪一場**活動**」——記者從頭到尾沒在問活動，這句話本身
就是第二次答非所問。

**修法（兩層，刻意互不依賴）**

① **話題記憶**（`api/line.js`）：`line_users` 新增 H 欄 `last_topic`，格式
`industry_trend@<時間戳>`，TTL 10 分鐘。`answerIndustryTrend()`／
`answerTechQuery()` 答完寫入；`handleUnbound()`、`handleEvent()`、
`handleGroupMessage()` 三個呼叫端讀出來，透過新的 `currentTopic` 參數交給
`routeIntent()`（`lib/router.js`）——那支多組一個 system 區塊，講清楚「上一則剛
回答完的是趨勢／技術題，這則如果只是個裸名詞就接回同一個話題」。

⚠️ 存 Sheets 而不是行程內的 Map：這個記憶要跨兩則 webhook 請求才有意義，而
Vercel 的 instance 隨時可能被回收——記者讀完五行回答再打字，中間隔十幾秒到一兩
分鐘很正常，撞到冷啟動就整個失憶，對真正會發生的情境等於沒修（跟批次 5 群組續問
視窗、`pending`（職員追問）踩過同一個坑）。多的成本只有「答完趨勢／技術題時多寫
一格」，這兩條路不是熱路徑；讀取免費，走 `getBinding()` 早就載入的那份 60 秒快取。

⚠️ 話題提示的 system 區塊排在活動提示**後面**：同一個裸名詞兩塊會給出不同答案，
最後一條才是模型看到的最新指示。兩塊都不進吃 ephemeral cache 的第一塊（理由同
批次 16）。`test-router.mjs` 有守這兩件事。

② **兜底文案**（`sendFallbackGuide()`）：即使話題記憶過期或根本不存在（記者一進來
就直接打一個詞），也不再假設記者一定在問活動。訊息看起來像「一個光禿禿的主題詞」
時直接複誦回去，給兩條真的走得通的路（`太空的產業趨勢`／`工研院的太空技術`，按鈕
送出的文字真的路由得到，測試有守）；其餘情況給的是把四條路一次講清楚的泛用文案，
不再只講活動。判斷（`looksLikeBareTopic()`）刻意收得很緊——2～8 個字、不含空白與
標點、且不是常見招呼／應答語：把「你好」「謝謝」當成主題詞複誦回去比不複誦難看
得多。

**順手修的 bug（盤點題目②）**

- `getGroupSessionUntil()` 原本直接 `readRange()`、沒吃 60 秒快取，但群組裡**每一則**
  訊息都會先過這支（包含我們最後根本不會回的閒聊）——等於群組每有人講一句話就燒掉
  一次 Sheets 讀取配額，而那個配額是每分鐘 60 次、整個網站（含記者會現場的問答與
  `qa_log` 寫入）共用的。改吃 `getAllLineUserRows()`，跟 `getStoredNote()`／
  `getStoredMediaName()` 本來就吃快取一致；寫入路徑都有
  `invalidateLineUsersCache()`，不會讀到過期資料。
- `handleMetaIntent()` 連續兩個 `await getStoredNote()` 判斷兩個存在同一欄的旗標，
  第一個若命中會 `setContactPending()` → 快取失效 → 第二個必定真的再打一次讀取。
  改成讀一次。
- `clearBinding()`（「回首頁」）順手清掉 H 欄，並把 G 欄原值寫回——不清 H 的話回
  首頁之後打的第一個詞會被接回舊話題；G 不能跟著清，那支群組也會走到，清掉等於
  按「回首頁」把整個群組的免 @ 視窗一起關掉。

**人味／人設（盤點題目④）**：新增共用的 `TONE_RULE`，套進產業趨勢與工研院技術兩支
問答的 system prompt（兩支的開場也從「你是負責回答ＸＸ問題的助理」改成「你是米亞」）。
米亞原本只活在歡迎圖卡與 `HELP_TEXT` 裡，真正在對話的這兩支反而是公文語氣，兩邊
對不起來。這條**只調語氣、不放寬任何一條「只能照資料回答」的規則**，而且寫成「親切
但精準」而不是「可愛一點」：回覆掛著主辦單位名義、記者可能直接截圖引用，語氣軟化
不能連帶讓「我沒有這項資料」變得含糊——講不知道時要更清楚、更快給出下一步。

活動問答（`answerQuestion` → `lib/prompt.js buildSystemPrompt()`）刻意不套：那份
prompt 每一條規則都是踩過坑寫出來的，且網頁版共用同一份，動它等於同時改動網頁版
記者看到的語氣，超出這次的範圍。

**「沒有資料」時要給得出下一步（截圖的另一半）**：截圖裡 AI 老實說了「但沒有航太
專項的分析」，然後就沒有下文——誠實但沒有出口，記者只能自己猜下一步該打什麼，猜出來
的「太空」又剛好掉進兜底。兩支問答的 system prompt 各加一條「沒有直接對應資料時，
除了老實講，還要給一條明確的下一步」，並且明講**只能建議這兩條真的存在的路**，不要
自己發明「我幫您轉給某某」「稍後回覆」這種帳號做不到的事。程式面也對稱地補上跨路
按鈕：趨勢答案附「工研院的ＸＸ技術」，技術答案附「ＸＸ的產業趨勢」，關鍵字抽不乾淨
（`crossTopicKeyword()` 回空字串）就不放那顆按鈕——寧可沒有，也不要放一顆
「工研院的哪些產業趨勢重點技術」這種讀起來像亂碼的按鈕。

**測試**：`test/test-flow.mjs` 新增情境 19（19 個檢查）：截圖案例本身、技術題那條路
的對稱案例、綁定中一樣接得回來、招呼語不會被誤複誦（4 個）、「回首頁」清掉話題、
複誦給的兩顆按鈕真的路由得到、群組續問視窗內的裸名詞。`test/test-router.mjs` 新增
`routeIntent()` system 區塊組裝的 7 個檢查（區塊數量、快取切割、兩塊的先後順序、
不認得的 `currentTopic` 值直接忽略）。`test/fakes.mjs` 補 H 欄與話題提示的模擬。
`npm test` 全數通過（`test-flow.mjs` 203 個檢查、`test-router.mjs` 28 個，整體全綠）。

**這批沒有做的事**：`routeIntent()` 對「裸名詞」的判斷仍然完全交給模型（只是多給了
一個提示），沒有在程式面硬編任何「這個詞屬於哪個產業」的對照表——那種表一定會過時，
也會跟模型的判斷互相打架。另外，話題記憶只在「答完趨勢／技術題」時寫入，答完活動
問答時**不會**清掉（那是每則提問都會走的熱路徑，為此多一次 Sheets 寫入不划算），
靠 10 分鐘 TTL 自然過期即可。

---

### 批次 27：閒聊短問句被當成主題詞複誦回去（回報的截圖，2026-09-06）

**截圖裡發生的事**：記者打「天氣如何」→ 收到

> 「天氣如何」我可以從兩個方向幫您找 🙂
> ・整體產業趨勢（IEK 產業情報網的免費焦點）
> ・工研院自己在這方面的技術與發表
> 想看哪一種？點下面的按鈕就可以。

底下還掛著「天氣如何的產業趨勢」「工研院的天氣如何技術」兩顆按鈕。

**根因**：批次 26 的複誦兜底（`sendFallbackGuide()`）本身是對的——記者打「太空」時
很有用。漏的是守門 `looksLikeBareTopic()`：那支當時就寫了一段警告，說把招呼語複誦
回去「比不複誦難看得多」，但實作只有**一份寫死的招呼語名單**（你好／謝謝／哈哈…），
其餘條件是「2～8 個字、不含空白與標點」。任何一句**沒有標點的短問句**都會整個穿過去
——「天氣如何」「吃飽沒」「現在幾點」「怎麼辦」全部符合。當時預期的漏網是招呼語，
沒想到真正會漏的是問句。

而且這不只是文案難看：複誦那則的兩顆按鈕是用原字串拼的，按下去真的會把
「天氣如何產業趨勢」送進 `answerIndustryTrend()`、把「工研院 天氣如何」送去查工研院
官網——記者按了會拿到一段煞有其事的「查無資料」，比不給按鈕更浪費他的時間。

**修法**：不是把「天氣」加進黑名單——閒聊的題目列不完，列了也一定會過時（跟批次 26
「不硬編產業對照表」同一個判斷）。改成看**句子的字面結構**：主題詞是一個名詞
（太空、半導體、光通訊），閒聊與問句一定會帶到疑問詞、句尾語助詞或人稱代名詞，而
這些字不會出現在主題詞裡。三道門檻：

1. **白名單字元集**：只由中文字與英數組成（`/^[一-鿿A-Za-z0-9]{2,8}$/`）。順便擋掉
   純表情符號、顏文字、全形字母，標點也不用再另外列一串——原本的 `[^\s]{2,8}` 只擋
   了幾個列舉的標點，`😀😀`、`^_^` 都會放行。
2. **招呼／應答語**：`GREETING_RE`，原本就有，不動。
3. **句子特徵**（新增 `SENTENCE_RE`）：疑問詞（如何／怎麼／什麼／為何／哪／誰）、
   句尾語助詞（嗎／呢／吧／嘛）、人稱代名詞（你／妳／您／我／他／她／它）、
   時間量詞問句（多少／幾點／幾號／幾天／幾歲）、正反問句、句尾的「沒」「否」。

⚠️ **挑字的原則是「這個字不可能出現在記者真的想查的主題詞裡」，不是「這個字常出現
在問句裡」。** 所以刻意**不收**單字的「幾」「能」「多」「是」——「幾何」「能源」
「太陽能」「儲能」「多媒體」都是真的會被問到的題目，為了多擋一句閒聊而擋掉它們並不
划算：誤擋的代價是記者拿不到複誦，誤放的代價只是多一句難看的話。要擋這幾種句型就
寫成「幾點」「能不能」這種擋得住又不會誤傷的多字組合。

正反問句（是不是／要不要／能不能／好不好／有沒有／知不知道…）用一條疊字規則
`(.)[不沒]\1` 收掉，比一句一句列短、也不會有漏掉的句型；主題詞裡出現「不」的例子
（不鏽鋼、不織布）都是「不」開頭，前面沒有字可以疊，不會誤中。

漏掉的句型會退回**泛用兜底文案**——那份文案把四條路一次講清楚，對一句閒聊本來就是
誠實且夠用的答案，不是壞掉的狀態。這正是批次 26 寫的「寧可漏判」，這次只是把「漏判」
的方向調回原本設計的那一邊。

**測試**：`test/test-flow.mjs` 新增情境 20（24 個檢查）：8 句閒聊短問句各驗兩件事
（走泛用兜底、且不會生出用原字串拼的按鈕）、5 個主題詞照舊複誦得到（含刻意挑的
「能源」「太陽能」，守 SENTENCE_RE 不收單字這條線）、3 種表情／符號輸入。把修正
`git stash` 起來重跑，這 19 個檢查全數失敗，修回來全綠——確認測試真的守得住這個 bug。
`npm test` 全數通過（`test-flow.mjs` 從 203 個檢查增加到 227 個，整體全綠）。

**這批沒有做的事**：沒有讓 `routeIntent()` 多回一個「這句話算不算可用的主題詞」欄位。
模型判得一定比正則準，但那要動到吃 ephemeral cache 的那份 systemPrompt、每則訊息都
多付一點 token，而這件事在**路由整個掛掉時**（沒有 API key、Anthropic 出錯）還是得
有程式面的防線——`routeIntent()` 失敗一律回 `other`，照樣會走到這支。既然無論如何都
要有這層，就讓它自己站得住，不要疊第二套。另外，單獨一個閒聊名詞（例如只打「天氣」
兩個字）仍然會被複誦——那是名詞、不是問句，這條結構規則本來就分不出來，而且複誦出
的「天氣的產業趨勢」按下去至少還是一句通順的查詢，不是亂碼；真要擋只能靠列題目黑
名單，那條路上面已經否決過了。

### 批次 28：群組「亂回／答非所問」整批修正＋人味補強（回報的意見，2026-09-09）

**回報的原話**：「Line 這邊再幫我盤點哪裡可以改進，尤其是使用者 TA 體驗可以更如真人般，
並且能回答各種問題。另外在被拉進群組時，也能要他回答時不要答非所問，而且不會亂回。」

盤點下來分成三組問題，底下逐一記根因與修法。

#### 一、被拉進群組時什麼都不說（`join` 事件從來沒被處理）

`handleEvent()` 開頭第二行就是 `if (ev.type !== 'message') return;`，註解寫著「批次 2
不處理」——於是被拉進群組後這個帳號**一句話都不講**，然後從此只在被 @ 到時才開口。
群組成員看到的是一個突然出現、不講話、也不知道能幹嘛的帳號，第一次想用它時只能亂猜。
「不會亂回」這件事有一半是期待管理：沒有人告訴他們規矩，任何一次回應都可能被當成亂回。

修法是新增 `handleGroupJoin()`，在被拉進群組那一刻講三件事，順序就是重要性：

1. **先講規矩**：「我只有被 @ 到的時候才會說話，平常的聊天我不會插話，也不會主動推播。」
   這一句同時是承諾與操作說明，講在最前面。
2. 再講能問什麼（四條路一次講完，跟 `sendFallbackGuide()`、群組「只 @ 沒接問題」同一份口徑）。
3. 附快速回覆按鈕，讓第一個想試的人不用先學會怎麼 @。

`memberJoined`（有**別人**加入我們所在的群組）刻意不處理——每進一個人就自我介紹一次，
正是第 8 節「不要做推播行銷」要避免的洗版。

#### 二、免 @ 續問視窗內「什麼都答」（這才是「亂回」的真正破口）

批次 14／16 一路在補的都是**已經綁定活動之後**那一段的安靜門檻（`@` 到別人 → 安靜、
`intent === 'other'` → 安靜）。但續問視窗那 5 分鐘之內，訊息其實是**先**經過三段完全
不看「有沒有被 @」的邏輯：

| 路徑 | 群組裡會發生什麼 |
| --- | --- |
| `detectMetaIntent()` | 「那個案子後面還有活動要辦」命中 `CALENDAR_RE` → 貼一份活動清單；「這邊先換一場再說」命中 `SWITCH_RE` → **把整個群組的活動綁定清掉** |
| 一次性旗標（`await_tech_query` / `await_contact_topic`） | 旗標存在 `line_users` F 欄、群組用 `groupId` 當 key ＝ **整個群組共用一格**。A 按了「想問什麼技術」，接下來群組裡**任何人**講的**任何一句話**都會被當成技術名稱送去查工研院官網 |
| 路由的 `industry_trend` / `tech_query` / `calendar` | 只有 `other` 是安靜門檻，這三種一律開講。群組聊「台積電最近在擴廠」→ 機器人開始講 IEK 產業趨勢 |

兩個修法：

**（a）`looksAddressedToBot()`——續問視窗內的統一守門**，放在 `handleGroupMessage()`
最前面，比上表三條路都早，只在**沒有被 @** 時生效。全部是字面特徵、不呼叫 AI（群組
每一則訊息都會過這支，理由同 `getGroupSessionUntil()` 那段的配額討論）。放行的幾類，
每一類都對應一個「不放行就會壞掉」的真實路徑：

- ⓪ 我們剛問完一句、正在等**這個人**回答（一次性旗標還開著）——那則答案本身多半是個
  沒有問號的名詞，擋掉等於自己問了又不聽
- ① 整句完全等於我們送出過的按鈕文字（固定選單詞、`邀訪：ＸＸ`、`工研院 ＸＸ`、活動
  全名、同仁自訂的 chips 與邀訪關鍵字）——群組裡按鈕送出的是不帶 @ 的純文字，擋掉
  就是按鈕按了沒反應
- ② 看起來就是一句提問（問號、疑問詞、句尾語助詞）
- ③ 剛答完趨勢／技術題（話題記憶還在）時的裸名詞追問——那是我們自己邀請他打的
- ④ 其餘一律安靜：陳述句、閒聊、跟別人的對話

⚠️ 這裡刻意**不**用 `detectMetaIntent()` 當放行條件，即使它就是「這是不是固定選單意圖」
的權威判斷：那支的 `CALENDAR_RE` / `SWITCH_RE` 是寬鬆的**片語**比對，設計成「記者怎麼
講都接得住」——在 1 對 1 是體貼，在群組就是上表第一列那兩個誤判。改成只認**整句完全
相同**；記者真的想問活動清單時講的「最近有哪些活動」「有什麼活動嗎」本來就帶疑問詞，
走 ② 那條就接得住，不需要靠寬鬆比對放行。

守門是**加**在既有門檻之前、不是取代：放行之後，「@ 到別人」否決與 `other` 安靜門檻
全部照舊。被 @ 到時整道跳過——明確叫了機器人就不能不理人（同批次 14／16 的原則）。

**（b）一次性旗標綁「是誰按的」**：群組裡把發話者 id 一起寫進旗標
（`await_tech_query#U123…`），只有同一個人的下一則才用得掉。1 對 1 不加後綴（`targetId`
就是本人），舊資料沒有後綴時維持「誰都算數」的舊行為，不會因為這次改動失效。

#### 三、人味（「更如真人般」）

| 缺口 | 修法 |
| --- | --- |
| **完全沒有對話記憶**——`askAnthropic()` 每次只送一則 user message。記者問「這項技術何時商業化」，答完再問「那成本呢」，模型連上一句是什麼都看不到。這是最不像人的一點，比語氣嚴重得多 | 新增 `line_users` I 欄（`last_turn`）記住**上一輪**的問題與答案（JSON，10 分鐘 TTL），下一題組進 `messages` 回放 |
| 米亞人設只套在產業趨勢／工研院技術兩支支線，記者用最多的**活動問答**反而還是公文語氣 | `TONE_RULE` 併進 `lineExtraRules()` |
| 兜底文案是把功能選單再念一次——批次 26／27 兩次回報其實都是這件事 | 新增 `composeFallbackReply()` 智慧兜底 |
| 非文字訊息一律回同一句「目前僅支援文字訊息提問」，貼圖跟照片講同一句 | `nonTextReply()` 依訊息型別分開講 |

**對話記憶的三條防線**（都是「寧可不回放」）：

- 只記**一輪**，不是完整對話串。這個帳號的答案會被記者直接截圖引用，對話帶得越長，
  模型把幾輪前的內容混進這一題的機會越大（坑 6 是同一種顧慮）。一輪就足以接住
  「那ＸＸ呢」這種真正常見的省略式續問。
- 一併記下**這一輪是哪一場活動**，只有下一題還在問同一場時才回放。換場後回放上一場的
  問答，等於把另一場的內容當脈絡餵給模型，正是「記者不會發現答案來自別場」那種風險。
- **群組不開**（呼叫端傳 `memory: false`）。群組多人交錯提問，「上一輪」很可能是別人的
  問題，回放進去製造的正是這次要修的答非所問。1 對 1 才有「上一輪就是同一個人講的」
  這個前提。

存 Sheets 不存行程內的 Map，理由同批次 26 的話題記憶（Vercel 執行個體隨時可能被回收，
記者讀完答案再打字中間隔幾十秒很正常）。

**智慧兜底（`composeFallbackReply()`）**：四條路都對不上時，用一次 Haiku 讓米亞針對
「記者這一句」講一段真的貼題的話——先讓他知道我聽懂了、老實說這個查不到、再指到最接近
的那一條路。⚠️ 這支**不回答問題本身**，這是它跟「讓 AI 自由發揮」的根本差別，也是它敢
上線的唯一理由：沒有資料來源就生成事實內容是第 3 節從第一天就禁止的事。system prompt
把「不要提供任何事實內容」寫成最硬的一條，輸出再過一次守門（太長、混進 Markdown、或
其實是 `askAnthropic()` 自己的失敗訊息 → 一律退回固定文案）。固定文案保留成安全底線，
不要因為有了智慧兜底就拿掉。成本只發生在兜底這條路，不是每則提問都多一次呼叫。

**`TONE_RULE` 併進活動問答的做法**：走 `lineExtraRules()` 這個「頻道專屬規則」的管道，
不是去改 `buildSystemPrompt()` 本身——網頁版 `api/chat.js` 呼叫的是不帶 `extraRules` 的
版本，輸出逐 byte 不變（見 `lib/prompt.js` 開頭的 ⚠️），網頁版記者看到的語氣完全不受
影響。之前因為「動不了那份共用 prompt」而整條跳過，結果是人設只活在支線上。

#### 上線後走查補的一刀：守門要排在限流之前

批次 28 合併上線後，把整條路實際走過一遍（不是斷言測試，是把每一句實際回覆印出來看），
抓到一個**改動本身沒有蓋到的破口**——`looksAddressedToBot()` 原本放在
`handleGroupMessage()` 開頭，但 `rateLimited(groupId)` 在更上游的 `handleGroupEvent()`
裡，於是：

- 群組裡連續聊了十幾句自己的事（我們全程安靜、一句都沒回），第 16 句時機器人突然冒出
  一句「提問太頻繁，請稍候片刻再試。」——沒有人在跟它講話。**這正是「亂回」本身**，而且
  比答錯內容更莫名其妙
- 那些我們根本不會回的閒聊照樣吃掉配額，等到真的有人 @ 我們問問題時，額度早就被閒聊
  燒光，真正的提問反而被擋下來

修法是把守門上移到 `handleGroupEvent()` 裡、排在限流「之前」。限流保護的是
Anthropic／Sheets 的呼叫額度，而被守門擋掉的訊息從頭到尾走不到那些呼叫，本來就不該
計入；守門在前面，計數才對得上「我們真的做了幾次事」。走到限流那一行的訊息都已經確定
是在跟我們講話，這時候回一句「提問太頻繁」是對的，不是插話。

⚠️ 這個教訓值得記下來：**守門加在哪一層，決定它蓋得到什麼**。同樣一支
`looksAddressedToBot()`，放在 `handleGroupMessage()` 只擋得住「回答」，放在
`handleGroupEvent()` 才連「系統訊息」（限流提示）一起擋住。群組裡任何一句不請自來的話
都是插話，不分它是 AI 生成的答案還是我們寫死的提示字串。

#### 盤點過、這次刻意沒做的

- **「什麼都能答」的通用知識庫**：記者實務上會問「怎麼申請採訪證」「你們地址在哪」這類
  機構常識，目前四條路都答不到，智慧兜底也只能導流、不能回答（它被明確禁止生成事實）。
  要真的答得出來，需要同仁在後台維護一份 FAQ／機構基本資料，是新的資料維護流程與後台
  欄位，不是程式面改得完的事——**這是下一個最值得做的功能**，但要先有人願意維護那份資料，
  沒人維護的 FAQ 過期之後比沒有更危險。
- **產業趨勢／工研院技術兩支也開對話記憶**：那兩條已經有批次 26 的話題記憶接住主要的
  續問情境，再疊一層的邊際效益不高，先不動。
- **`postback` / `unfollow` / `leave` 事件**：圖文選單用的是 message action 不是 postback，
  沒有沒接到的按鈕；另外兩個沒有可用的 `replyToken`，也沒有事情可做。
- **群組裡「長得像問句的閒聊」**（例如「大家中午吃什麼」）：字面守門靠疑問詞放行，分不出
  它跟「這場的重點是什麼」的差別，最後仍然由 `routeIntent()` 判成 `other` 才安靜下來。
  也就是這一類的最後一道防線是 LLM 判斷，不是程式。要再往下收只能讓守門也去問一次
  模型，那就違背了「群組每一則訊息都會過這支、不能花錢也不能多打 API」的前提——
  現況是可接受的分工，但要知道它靠的是誰。

### 批次 30：群組裡「叫不動它」的兩個實測問題（回報的截圖，2026-09-09）

真的拉同事進群組測，抓到兩個問題。第二個比第一個嚴重得多。

#### 問題一：按鈕按了沒反應

同事按快速回覆按鈕「媒體邀訪需求」，完全沒反應。根因：LINE 的快速回覆按鈕會**一直留在
對話紀錄裡**，他往上滑、隔了約 9 分鐘才按，而續問視窗當時只有 5 分鐘、早就過期；按鈕
送出的是不帶 @ 的純文字，於是被「沒被 @ 又不在視窗內 → 安靜」擋掉。體感就是壞掉。

兩個修法：

- **視窗外也接「整句完全等於我們自己送出的按鈕文字」**（`isOwnButtonText()`）。這種訊息
  是最不可能誤判的一種——沒有人會在群組閒聊裡剛好打出「媒體邀訪需求」這五個字。
  ⚠️ 刻意收得比 `looksAddressedToBot()` 的 ① 更窄：只認固定選單詞與 `邀訪：ＸＸ`
  這種機器格式，**不含**活動全名與「工研院 ＸＸ」——那兩種在群組裡是講得出來的句子
  （「工研院 那邊怎麼說」），視窗外就接會變成新的亂回來源。那兩種按鈕在視窗內照樣
  按得動，隔太久才按就要 @ 或用喚醒詞。
- **續問視窗 5 分鐘 → 15 分鐘**。5 分鐘是「視窗內任何訊息都會被硬答」那個年代訂的——
  窗開越久越危險，所以只敢開一下下。批次 28 的守門補上之後，視窗本身不再是風險來源，
  就可以放長到真實對話的節奏（「開會中偶爾看一下手機」）。

#### 問題二：LINE 的 @ 選單裡根本找不到這個帳號

同事想改用 @ 叫它，截圖裡的 @ 選單只列出真人成員，沒有這個官方帳號——「我的 @ 找不到他
哈哈」。**對他來說「只有被 @ 到才會說話」不是有點麻煩，是完全叫不動**：他沒有任何辦法
把訊息送到機器人面前。

官方帳號會不會出現在 @ 選單是 LINE 那邊決定的（跟 app 版本、帳號設定有關），程式改不了。
與其賭它會不會出現，不如**多給一條不依賴 @ 的呼叫方式**：訊息開頭打「米亞」就等同 @ 到
我們（`WAKE_WORD_RE`）。

⚠️ 只認**開頭**，不是整句話裡出現「米亞」就算。群組裡談論這個帳號（「等等問米亞」「剛剛
米亞說的那個」）跟呼叫它是兩回事——這條界線就是「亂回」與「叫得動」之間的平衡點。後面
允許接標點或空白，也允許有人手打一個 `@`（打了 @ 但選單裡選不到，最後送出的正是純文字，
就是回報的那個情境）。

#### 講出來才算數

喚醒詞沒人知道就等於沒有。`join` 自我介紹、群組「只 @ 沒接問題」的引導、`HELP_TEXT`
三處同步改寫，兩種呼叫方式都列出來，而且**直接點名「有些人的 @ 選單裡找不到我」**這個
實際會遇到的狀況並給出替代做法——不要讓遇到的人以為是帳號壞了。

#### 這次的教訓

前面幾批都在修「它會不會亂回」，這一批修的是反面：**它叫不叫得動**。兩者是同一個設計的
兩端，只盯一端就會把另一端做壞——批次 28 收緊守門的同時，其實已經埋下「視窗外按鈕沒反應」
這個坑，只是要等真人在真的群組裡按下去才會浮出來。**這類問題測試蓋不到，只有真的拉人
進群組用才會發現。**

### 批次 31：「新聞室明明有，沒抓到」——這場答不出來時自動補查官網（回報的截圖，2026-09-09）

**回報的情況**：記者在群組問「今年院士有誰」，機器人回「我這邊目前沒有得獎名單的資料。
建議您洽詢現場新聞聯絡人⋯⋯」。答得沒錯——但工研院官網新聞中心搜「院士」，**第一筆**
就是那篇《工研院第15屆新任院士賴總統親臨授證⋯⋯》（實測過真的官網，三種寫法
「院士」「院士授證」「工研院院士」都是第一筆命中）。

#### 根因：不是資料沒有，是那條路從頭到尾沒被走過

`routeIntent()` 判 `tech_query` 的條件是**問句裡明確出現「工研院」**——這是批次 21 為了
避免誤觸刻意訂的（「訊息裡沒提到工研院、只是單純問某個技術，就判成 industry_trend，
不要用猜的」）。「今年院士有誰」沒提到工研院，加上群組當時綁著一場活動，`currentEventId`
的提示又把它拉向「延續這場的討論」，於是判成 `qa`、只拿那場的知識庫回答。

知識庫裡沒有名單，所以誠實說沒有——**每一步都照規則走，結果卻是我們手上有答案的來源
從頭到尾沒被問過**。

#### 修法：不放寬路由，改在「已經確定答不出來」之後補一刀

放寬路由是最直覺的想法，但代價太大：「這場的重點是什麼」這種正常提問也會開始被送去
官網，換來的誤判比修掉的問題更糟。改成**只在真的失敗之後**才多查一次——正常提問一個
字節都沒變慢，只有本來就要回「我沒有資料」的那些題會多花一次查詢。

判斷「這則回覆是不是在說沒有資料」用**機器可讀標記**，不是事後拿正則去猜：模型每次的
措辭都不一樣（這次是「我這邊目前沒有得獎名單的資料」，不是規則裡寫的那句「這部分我沒有
資料」），猜錯的兩個方向都很糟。`lineExtraRules()` 多一條規則，請 AI 在確定答不出來時於
結尾加上 `[[NO_DATA:關鍵詞]]`，關鍵詞是**記者這題真正想問的主題**（「今年院士有誰」→
「院士」）。這招跟產業趨勢／技術問答的「來源編號：」是同一個既有作法。

⚠️ 標記**一定要切掉**，不管後面用不用得到那個關鍵詞——漏在回覆裡讓記者看到一串
`[[NO_DATA:院士]]` 比什麼都沒做還糟。`extractNoDataKeyword()` 負責這件事，測試專門盯著。

⚠️ 補查到的報導**只給線索、不重寫內容**：直接給標題＋日期＋原文連結，不再叫一次 AI 去
摘要。記者要的是「哪裡找得到」，直接給連結最準也最快；多叫一次 AI 除了慢，還多一次把
官網原文講走鐘的機會——而這則回覆掛的是主辦單位名義。

三條退路（都是「補查失敗不能連累原本的答案」）：官網查無資料 → 只給原本那句誠實的回答，
不附空區塊；官網抓取失敗 → 同上，例外自己吞掉；關鍵詞抽不出來 → 根本不補查。

#### 這次的教訓

前面幾批修的是「亂回」跟「叫不叫得動」，這批修的是第三種失敗：**它明明有能力回答，卻
因為路由把它送錯地方而說自己不知道**。這種最難發現——回覆本身完全正當、誠實、有禮貌，
沒有任何一句話是錯的，只有知道「新聞室其實有」的人才看得出問題。單看對話截圖永遠抓不
到，要有人拿著另一個來源去對才會浮出來。

### 批次 32：Markdown 印在記者畫面上、按鈕又有一種按不動（回報的截圖，2026-09-09）

#### 一、`**徐喬涵**` 的星號原封不動印出來

記者收到的聯絡人那行長這樣：`**徐喬涵** | 03-5915128`，分隔線也是三個裸露的 `---`。

`lineExtraRules()` 早就有一條「不要用 Markdown」，但它寫成「**需要附連結時**⋯⋯」，範圍
只涵蓋連結——模型拿它去加粗人名時完全不覺得違規。

修法兩層：

- **規則放寬到整則回覆**，而且把最常見的幾種語法直接點名（`**粗體**`、`*斜體*`、
  `` `程式碼` ``、`# 標題`、`- 項目符號`、`[文字](網址)`、`---`），不要只講「Markdown」
  這個詞——模型不見得把「加粗人名」歸類成 Markdown。
- **程式面的最後一道防線** `stripMarkdownForLine()`，放在 `askAnthropic()` 的出口。
  規則是請求、不是保證：同一個模型下一次還是可能加粗。四條問答路線（活動、產業趨勢、
  工研院技術、智慧兜底）全部經過那個出口，寫一次四邊都受惠。

⚠️ 只拆掉「LINE 顯示不出來的語法符號」，**不改動任何文字內容**：粗體只拿掉星號、連結
攤成「文字 網址」（網址要留著，記者要點）、項目符號換成 LINE 上讀得順的「・」。這是
格式清理，不是重寫答案——答案內容一個字都不能動（同 `lineExtraRules()` 的顧慮：這則
回覆掛的是主辦單位名義）。
⚠️ 我們自己接在答案後面的區塊用全形破折號「———」（U+2014），不在水平線規則的
`[-*_]` 裡，不會被誤刪。

順帶：智慧兜底裡那條「輸出混進 Markdown 就退回固定文案」變成死碼，拿掉了——清乾淨的
內容本身是好的，沒有理由丟掉。

#### 二、又一種按鈕按不動

同事按「半導體相關乾淨帶畫面提供」完全沒反應，只好自己補一句「按鈕 按了 因為沒寫米亞
會沒反應」。

那是**邀訪窗口關鍵字**按鈕，而那種按鈕送出的是**原始關鍵字**（`handleMetaIntent()` 的
contacts 分支：`contacts.map(c => c.keyword)`，沒有「邀訪：」前綴）；同仁在後台自訂的
快速提問 chips 也一樣是原始文字。批次 30 的視窗外放行只認固定選單詞與「邀訪：ＸＸ」，
兩種都沒涵蓋到——**「按鈕按了沒反應」換顆按鈕又發生一次**。

規則收斂成一句話：**只要是我們自己放到按鈕上的字，就一定按得動，不分視窗內外。**
這比「哪幾種按鈕算數」的清單好記，也不會再有下一顆漏掉的按鈕。`isOwnButtonText()`
現在涵蓋：等待中的一次性旗標、固定選單詞、`邀訪：ＸＸ`、活動全名、同仁自訂的 chips
與邀訪窗口關鍵字；`looksAddressedToBot()` 直接呼叫同一支，兩邊共用一份清單才不會再
「補了一種、漏掉另一種」。

唯二不放進去的是 `工研院 ＸＸ` 與 `ＸＸ產業趨勢` 這兩顆導流按鈕：它們是用關鍵字拼出來
的，比對只能靠前綴／後綴而不是完全相同，「工研院 那邊怎麼說」這種日常對話會誤中。
它們永遠出現在一則剛送出的答案底下，視窗本來就是開的，不需要靠這條放行。

⚠️ 批次 30 有一條測試把「活動全名」列為「視窗外應該安靜」的例子，這批**刻意推翻**：
活動全名是活動清單按鈕送出的文字，它按了沒反應就是回報的那個 bug 本身。

#### 這次的教訓

批次 30 修「按鈕按不動」時只補了當下回報的那一種按鈕，沒有回頭盤點「我們到底送出過
哪幾種按鈕」——結果同一個 bug 換顆按鈕又來一次。**修這類問題要修的是規則，不是個案**：
「只要是我們自己放到按鈕上的字就一定按得動」這句話，比任何一份按鈕清單都可靠。

### 批次 33：`[[NO_DATA:院士]]` 原封不動印在記者畫面上（回報的截圖，2026-09-09）

批次 31 那條「標記一定要切掉，漏出去比什麼都沒做還糟」的警告，**在下一次回報就自己踩到了**。

#### 根因：兩條規則搶同一個位置

`NO_DATA_RE` 第一版錨定在字串結尾（`$`）。但 `lib/prompt.js` 的結尾規則要求「每則回答的
**最後**，務必另起一行加註警語」，批次 31 的標記規則也寫「整則回覆的**最後**」——模型
選了把警語放最後（那條更老、更強、講得更重），標記被擠到警語前面一行，`$` 就對不上。

一個 miss 造成兩個症狀：**標記漏給記者看到**，而且因為抽不到關鍵詞，**官網補查那條路
根本沒跑**——記者既看到亂碼、又沒拿到本來該給他的連結。回報的截圖裡兩件事同時發生。

#### 修法：不要跟位置賭

- `NO_DATA_RE` 改成 **整段裡任何地方出現都抓掉**（`g` 旗標，出現兩次也清乾淨）
- 加第二道防線 `NO_DATA_LINE_RE`：整行以 `NO_DATA` 開頭（括號打壞、少一邊、模型換了寫法）
  一律整行拿掉。中文回覆裡不可能有正當內容以 `NO_DATA` 開頭，這樣做是安全的
- 規則本身的措辭也不再寫「放在最後」，改成「另起獨立的一行，放在哪一行都可以」——
  程式端不挑位置之後，就沒有必要再跟警語規則搶那個位置

測試的假回答也改成把標記放在**警語前面**（回報的實際形狀），不是我原本假設的整段最後；
另外補上六種位置／寫法的直接驗證（最前、中間、最後、全形冒號、括號打壞、完全沒有）。

#### 這次的教訓

**「請模型把某段文字放在特定位置」本身就是脆弱的設計**，尤其當已經有另一條規則在搶同
一個位置。程式端的解析不該假設位置——能不挑位置就不要挑。

同一個教訓的另一面：批次 31 我自己寫下「標記一定要切掉，漏出去比什麼都沒做還糟」，卻
只測了「標記在結尾」這一種形狀。**警告寫得再重，測試沒有涵蓋到的形狀就是沒有保護。**

### 批次 34：補查的觸發條件寫太窄——「部分答到」也要算（回報的截圖，2026-09-09）

批次 33 修好標記外漏之後，記者再問一次「今年院士有誰」：標記不見了 ✅、Markdown 星號也
不見了 ✅，但**官網補查那一段仍然沒出現**。

#### 根因：判準訂在錯的東西上

批次 31 的規則寫的是「如果上面的背景資料裡**完全沒有**可以回答這一題的內容」，結尾還補
一句「如果背景資料答得出這題，就完全不要加這一行」。

而這則回覆的形狀是：開頭老實說「這部分我沒有資料」，接著提供了**鄰近的資訊**（出席的
38 國代表、獲頒經濟部專業獎章的人），最後給了新聞聯絡人。模型很合理地判斷成「我有部分
答到、背景資料不是完全沒東西」，於是沒加標記——補查那條路又沒跑。

**但記者要的是院士名單。旁邊的資訊再多，都不是他問的那一項。**

#### 修法：判準改成「記者問的那一項，你有沒有給出來」

規則改寫成看「那一項具體內容有沒有真的給出來」，而不是「背景資料裡有沒有沾邊的東西」，
並且明講：**即使你同時提供了其他相關或鄰近的資訊、即使你已經給了聯絡人**，只要出現
「這部分我沒有資料」「背景資料中沒有提到ＸＸ」這類說法，都要加標記。

測試的假回答也換成回報的實際形狀（同時有鄰近資訊與聯絡人），不再是乾淨的「只有一句
沒有資料」。

#### 這次的教訓

連續三批（31 → 33 → 34）都在修同一條路，而且每次都是**規則本身寫窄了**：

| 批次 | 寫窄的地方 | 症狀 |
| --- | --- | --- |
| 31 | 標記規則跟警語規則搶「最後一行」 | 標記漏給記者看到 |
| 33 | 解析正則錨定在字串結尾 | 同上，且補查沒跑 |
| 34 | 觸發條件訂在「背景資料完全沒東西」 | 補查沒跑 |

共同的病：**用「資料端有沒有東西」當判準，而不是用「使用者要的東西有沒有拿到」**。
後者才是唯一重要的事。下次再訂這類條件，先問一句：這個判準描述的是我們的資料狀態，
還是記者的實際處境？

### 批次 35：群組守門整條都是中文；導流按鈕的破例補回來（回報，2026-09-09）

#### 一、英文提問在群組裡完全叫不動

回報的截圖：群組裡打「Please reply in English.」完全沒反應。

`GROUP_QUESTION_RE` **整條都是中文**——句號結尾、沒有問號、沒有任何中文疑問詞，守門三道
規則全部落空，於是被安靜擋掉。而這個帳號本來就支援英文提問（`lib/prompt.js` 有一條很強
的「跟著記者這一則提問的語言回答」規則，還特地寫成中英雙語、放在規則清單最後的強位置）。

**規則做了，通道卻是關的**——外籍記者在群組裡等於完全問不到東西。

補上 `GROUP_QUESTION_EN_RE`：疑問詞／助動詞開頭，或句子裡有 `please`。收的範圍刻意跟
中文那條對齊（同樣偏寬鬆）——守門只是第一層，後面 `routeIntent()` 判成 `other` 一樣會安靜，
兩層一起才是完整的門檻。

順帶把補查引言也改成跟著答案語言走：那段字是**我們自己接上去的**，不受 `lib/prompt.js`
的語言規則管轄，不處理的話英文記者會拿到英文答案、下面突然接一句中文。判斷只看「模型
剛剛那段答案裡有沒有中文字」——答案的語言已經是模型跟著提問語言決定好的結果，跟著它走
就一定一致，比重新猜記者用什麼語言可靠。

#### 二、導流按鈕的破例補回來

批次 32 訂下「**只要是我們自己放到按鈕上的字，就一定按得動**」，然後在同一批裡替
`工研院 ＸＸ`／`ＸＸ產業趨勢` 兩顆導流按鈕**開了例外**，理由是「只能靠前綴比對、日常對話
會誤中」。回報又來了一次「群組點下面小按鈕沒有反應」。

改成比對**完整形狀**而不是前綴，就不必再破例：

```
/^工研院[ 　][一-鿿A-Za-z0-9]{2,8}$/     ← 工研院 太空
/^[一-鿿A-Za-z0-9]{2,8}產業趨勢$/         ← 太空產業趨勢
```

⚠️ 那個**空白**是關鍵的辨別點：中文使用者自然書寫時不會在「工研院」後面空一格（會寫
「工研院那邊怎麼說」），而我們的按鈕一定有。加上「後面只能是 2-8 個乾淨的字、而且到此
為止」，誤中機會很低；真的誤中，結果也只是查一次官網回「沒找到」。

`isOwnButtonText()` 現在真的涵蓋我們送出過的**每一種**按鈕，沒有例外。

#### 這次的教訓

**訂了規則就不要在同一批裡自己開例外。** 批次 32 我寫下「修的是規則，不是個案」，然後
立刻替兩顆按鈕破例——那個例外撐了兩批就被回報打回來。當時真正該做的不是破例，是把比對
方式從「前綴」換成「完整形狀」，也就是這批做的事。例外通常是「我還沒想到怎麼做對」的
委婉說法。

另一個角度的同一件事：批次 34 說「用『資料端有沒有東西』當判準是錯的」，這批的英文問題
是它的兄弟——**規則寫了、能力有了，但通往那個能力的路是關的**。檢查的時候不能只看「我們
有沒有做這件事」，要看「使用者走不走得到」。

### 批次 36：不再「切來切去」＋答題換模型（回報的通盤檢討，2026-09-09）

**回報的原話**：「這一定要切來切去特定活動專屬回答系統嗎？不能一體適用？另外也不是給
新聞稿，而是會讀懂消化，像是問新任院士，明明院士授證典禮新聞稿裡面就有寫。」

這是這一輪回報裡最根本的一個，指到的是**設計本身**，不是某個 bug。

#### 一、為什麼會切來切去，以及它的代價

原本的設計是「一場活動＝一份獨立知識庫」，綁定哪一場就只讀那一份。這不是意外——記者在
現場掃 QR 綁到該場，答案必須限定在那場，避免「拿別場的內容回答卻不說一聲」（坑 6：那
比答不出來更危險）。

代價就是回報看到的：**答案明明寫在隔壁那份新聞稿裡，它只肯看手上這一份**，於是不是答
不出來、就是得先切過去。而記者根本不該需要知道「這個問題該歸在哪一場活動底下」。

**修法**：主場次照舊完整帶進第一個（吃快取的）system 區塊，另外自動挑幾場**內容真的
跟這題有關**的接在第二個區塊（`lib/related-events.js`）。

- 挑選**不呼叫 AI**：每則提問都要走這一步，多打一次 API 等於每題變慢變貴，而「哪幾場
  可能有關」用字面比對就綽綽有餘。挑錯的代價很小（多帶一場沒用的資料，模型自己會忽略），
  漏挑的代價才大，所以寧可多挑。
- 中文沒有空白可以斷詞，又不想裝分詞套件（零依賴原則），所以把中文連續字串切成 2-3 字
  的片段全部當候選詞，再用 **IDF** 壓掉沒有鑑別度的詞：每篇都有的「技術」「工研院」權重
  趨近零，只出現在一場的「院士」才是訊號。
- ⚠️ **沒有做「全部場次一起塞進去」**：那是最直覺的解法，但成本跟場次數量成正比，而場次
  只會越來越多——今天可行不代表半年後可行。
- ⚠️ 安全線：帶進來的資料附一條很重的規則——**用到哪一場就必須指名是哪一場**，絕對不可以
  講得像是本場的內容。記者可能直接截圖引用，張冠李戴比答不出來嚴重得多（坑 6 的同一個顧慮，
  只是換了一種形式）。

#### 二、「會讀懂消化」——答題模型從 Haiku 換成 Sonnet 5

答題原本用 **Haiku 4.5**。當初選它是為了成本，而它拿來做「這句話要路由到哪一條路」很
稱職——但要它**讀完一整篇新聞稿、把埋在段落中間的名單抓出來**就明顯吃力，那正是回報的
症狀：新聞稿裡有寫，它卻說沒有。

換成 **Sonnet 5**：閱讀理解好很多，上下文 200K → 1M（跨場次資料塞得下也靠這個），成本
約兩倍，但既有的 ephemeral 快取讓重複提問便宜十倍。

⚠️ **路由那層刻意不跟著換**：那是分類題、每則訊息都要跑，Haiku 又快又便宜又夠準，換上去
只是白花錢。**貴的模型要花在真正需要理解力的地方。**

⚠️ `thinking` 明確關掉：Sonnet 5 省略這個參數會預設開啟 adaptive thinking，那會讓每則
回覆多等好幾秒——LINE 的 reply token 只有 60 秒，記者在等的是聊天速度的回應。讀新聞稿找
名單是閱讀題不是推理題。之後若發現答案深度不足，這是第一個該調的旋鈕。

⚠️ 順手修掉一個**還沒發作的地雷**：回應解析原本寫死 `content[0].text`。content 是一個
block 陣列，thinking 一旦開啟第一塊就是 thinking block，`.text` 會是 undefined，整支會
靜靜退化成「抱歉，無法取得回應」。改成挑出所有 text 區塊接起來——以後要開 thinking 也
不必回來改這裡。

#### 三、現在的回答層次

```
主場次的新聞稿  →  內容相關的其他場次（自動挑，指名出處）
                →  工研院官網新聞中心（批次 31，答不出來才查）
                →  老實說沒有 ＋ 給聯絡窗口
```

#### 這次的教訓

前面幾批修的都是「規則寫窄了」「路是關的」這種**實作層**的問題。這一批不一樣——使用者
指出的是**設計本身**選錯了取捨：為了防止「拿錯場次的內容回答」，把知識切成互不相通的
一塊塊，結果變成「明明有答案卻拿不到」。

安全性的作法有兩種：**把能力關掉**，或**把能力打開再要求它交代清楚**。原本選了前者，
這批換成後者——資料照樣跨場次讀得到，但用到哪一場就必須指名。多數時候，「不准做」是
比「做了要說清楚」更懶惰的設計。

### 批次 37：補查不能只靠模型自己標記（實測回報，2026-09-09）

批次 36 上線後實測：記者打「今年院士」，回覆是「這題目前我手上沒有具體的名單資料，得洽
現場新聞聯絡人確認喔。」——語氣明顯變自然了（Sonnet 5 ＋ 米亞人設有效），但**補查官網
那條路仍然沒跑**。

#### 這是第四次修同一條路，該換方法了

| 批次 | 改了什麼 | 結果 |
| --- | --- | --- |
| 31 | 加 `[[NO_DATA:關鍵詞]]` 標記 | 標記跟警語搶最後一行，漏給記者看到 |
| 33 | 解析改成整段任何位置都抓 | 標記不漏了，但補查還是沒跑 |
| 34 | 觸發條件放寬到「部分答到也算」 | 還是沒跑 |
| 37 | **不再只靠標記** | — |

結論很清楚：**請模型在回答裡順手加一個機器讀的標記，本來就不是可靠的機制**。它每次都
要在「照規則加標記」跟「把回答寫好」之間分心，而規則再怎麼寫都只是請求、不是保證。

修法不是再調一次規則，是**加一條不依賴模型的路**：標記留著（它抓到的關鍵詞語意最準，
有加就用），另外用純程式判斷「這則回覆的內容看起來就是在說我沒有這項資料」，是的話從
記者的問句猜一個關鍵詞去補查。

- `NO_DATA_PHRASE_RE`：句型判斷，刻意放寬——誤判的代價是多查一次官網（查到多兩個連結、
  查不到什麼都不加），漏判的代價是記者本來拿得到答案卻沒拿到。
- `guessNoDataKeyword()`：拿掉時間詞、疑問詞、語助詞，再切掉尾巴孤零零的動詞／繫詞。
  「今年院士」→「院士」、「今年的得獎名單是什麼」→「得獎名單」、「這次活動的合作廠商
  有哪些」→「合作廠商」。⚠️ 尾巴的「有」「是」只切結尾不全域切，否則「有機材料」會被
  切壞。
- 兩條路都會 log 走的是哪一條（`來源=標記` / `來源=句型判斷`），下次再出問題才查得到。

#### 順帶：這次回報也指出了一個「不是程式」的根因

同一張截圖裡，「最近有哪些活動」只列出一場（工研院創新日），而跨場次挑選有一道硬性條件
——**知識庫是空的場次會被直接排除**（帶進去也沒東西可讀）。這解釋了為什麼院士授證典禮
沒有被自動帶進來。

**沒有內容的知識庫，架構跟模型都救不了。** 這一類問題的排查順序應該是：先確認資料在不在，
再談檢索與模型。前面幾批我把順序做反了。

#### 這次的教訓

**當同一個機制連續修三次還是不動，要換的是機制，不是參數。** 前三批都在調「怎麼跟模型
要那個標記」，每次都合理、每次都沒解決——因為問題不在措辭，在「把可靠性押在模型的順從
度上」這個設計本身。程式判斷不漂亮，但它每次都會跑。

### 批次 38：官網找到了就「讀懂它」，不是只丟連結（2026-09-09）

**使用者確認的關鍵事實**：《工研院院士授證典禮》那場的**知識庫是空的**，內容只存在
工研院官網新聞室。

這句話一次解釋了前面好幾批的徒勞：

- 跨場次（批次 36）幫不上忙——沒有知識庫的場次本來就會被排除，帶進去也沒東西可讀
- 換 Sonnet 5（批次 36）幫不上忙——再會讀也讀不出不存在的內容
- **只有官網補查那條路答得出來**

也就是說，補查在這一題不是「錦上添花的線索」，是**唯一的答案來源**。那它就不能只丟兩個
連結叫記者自己點進去看——使用者兩輪前的原話是「不是給新聞稿，而是會讀懂消化」，只給
連結正是那句話在講的問題。

#### 修法

搜尋結果本來就帶著標題／日期／摘要，再叫一次模型把它讀成答案即可（`answerFromItriNews()`）。

- ⚠️ 這是這條路上的**第二次模型呼叫**，但它只發生在「本場答不出來」這條路——正常答得
  出來的提問一次都不會多花。用這個代價換「記者真的拿到答案」很划算。
- ⚠️ 摘要不是全文：規則明講只能根據摘要回答，摘要沒寫的細節（完整名單、具體數字）
  一律不補完，請記者看原文。跟活動問答同一條底線。
- 摘要裡真的沒有答案時，模型照規則回空字串 → **退回原本的「只給連結」**，不硬掰。
- 讀懂了照樣附原文連結——記者要能查證。
- ⚠️ 這段也過一次 `extractNoDataKeyword()`：這支的規則沒叫模型加標記，但同一個帳號的
  其他 prompt 有，模型偶爾會把習慣帶過來。批次 33 已經因為標記外漏踩過一次，統一清掉
  比賭它不會發生便宜。

順手拿掉一個魔術數字：原本寫「回覆短於 8 個字就當作沒答案」，那會把真的很短但正確的
答案（「三位，分別是⋯」）誤殺。只認「空的」就夠了。

#### 這次的教訓

**排查順序應該是：資料在不在 → 檢索找不找得到 → 模型讀不讀得懂。** 我從第 31 批開始
一路在修後兩層（標記、正則、觸發條件、跨場次、換模型），而根因在第一層——那場活動根本
沒有資料。使用者花了七輪回報才把我推到「先確認資料在不在」。

第二個教訓：**一旦某條退路變成唯一的答案來源，它就不再是退路，該用主要路徑的標準做。**
補查原本設計成「錦上添花，給個線索就好」，那個定位在「知識庫有內容」的前提下是對的；
前提不成立時，同樣的實作就變成敷衍。

### 批次 39：標記查不到就放棄，從來沒試過猜出來的那個（實測，2026-09-09）

批次 38 上線後群組實測：「米亞 今年院士」（喚醒詞正常 ✅），回覆仍然只有「這部分我沒有
資料」，**補查區塊還是沒出現**。

#### 根因：兩個關鍵詞來源被寫成二選一

```js
const lookupKeyword = noDataKeyword || fallbackKeyword;   // ← 這裡
```

標記優先，**標記查不到就整個放棄**，句型判斷猜出來的那個從來沒被試過。實測驗證：

| 關鍵詞 | 來源 | 官網結果 |
| --- | --- | --- |
| `今年受證院士的具體名單和人數` | 模型的標記 | **0 筆** |
| `院士` | 句型判斷猜的 | **10 筆** |

模型很自然地會把「記者到底想知道什麼」整句寫進標記，那是它理解的忠實反映；但工研院
官網的搜尋是接近精準比對，整句話一定查不到。而猜的那個因為只做字面清理，反而乾淨。

**兩個來源各有各的長處，不該二選一——標記的語意最準，猜的最乾淨，該依序試。**

改成候選清單依序試到查得到為止，並且在 log 印出候選與命中的是哪一個，下次再出問題
一眼就看得到卡在哪。

#### 這次的教訓

這是同一條路上第五次修正，而這次的錯誤有個很典型的形狀：**我加了 B 當作 A 的備援，卻
把它寫成「A 有值就不用 B」，而不是「A 沒成功才換 B」。** `||` 判斷的是「有沒有值」，不是
「成不成功」——備援機制寫成這樣，等於備援永遠不會啟動，除非主要來源完全空手。

寫備援時要問的是：**主要來源「失敗」的樣子長什麼樣？** 這裡主要來源的失敗不是「沒吐出
關鍵詞」，是「吐了一個查不到的關鍵詞」——而我的判斷式看不見那種失敗。

### 批次 40：群組沒有圖文選單，出口就得放在按鈕列上（回報，2026-09-09）

回報：「群組對話 如果切到特定活動 比較難切回來，因為圖示不會像 1 對 1 出現。」

#### 根因：功能一直都在，是看不到

「回首頁」「最近有哪些活動」從批次 21 起就一直認得，打字就會動。問題出在**圖文選單是
LINE 的 1 對 1 專屬功能，群組聊天室不顯示**：

| | 1 對 1 | 群組 |
| --- | --- | --- |
| 圖文選單（📅🏠🔬📊📞） | 常駐在輸入框下方 | **不存在** |
| 答完一題後的按鈕列 | 這場的自訂提問 ＋ 媒體邀訪需求 | 同左 |

1 對 1 的記者被綁在某一場也無所謂，🏠 隨時在畫面上；群組裡的記者答完一題之後，整排
按鈕都是「這場活動的快速提問」，**一條往外的路都沒有**，只能自己猜要打什麼字。

#### 這件事其實已經認過一次，但只補了一半

批次 18／28 的註解裡就寫著「群組裡沒有持續顯示的圖文選單，這則歡迎訊息附的按鈕就是
群組唯一一次看得到『還能問什麼』的機會」——當時補在**入群自我介紹**跟**只 @ 沒接問題**
兩則上。那是記者「還沒開始問」的時候；真正會卡住的時機是「已經問了幾題、想換個方向」，
而那個時機的按鈕列正好是唯一沒補到的一排。

#### 做法：把圖文選單搬到群組的快速回覆列上

群組版 `eventQuickChips()` 變成 `🏠 回首頁`、`📅 其他活動` ＋ 這場的自訂提問（8 顆）
＋ `📊 產業趨勢`、`🔬 問技術`、`📞 邀訪窗口`，icon 沿用 `REPORTER_MENU` 同一組，群組
跟 1 對 1 的人看到的是同一套東西。1 對 1 維持原樣不動——那邊選單一直在畫面上，同一組
入口再塞進按鈕列只會排擠掉同仁自訂的提問。

順手補上兩處群組專屬的死路：換場確認訊息（`已為您換到《X》✅`）原本完全沒有按鈕，
以及綁定的活動被下架時那句「這場活動目前無法問答」。

兩個刻意的決定：

- **往外的兩顆排在最前面**，不是照舊接在最後。批次 29 已經學過一次「按鈕列最後一格的
  『媒體邀訪需求』不夠明顯，滑一排按鈕容易漏看」——手機一次只看得到兩三顆，藏在 8 顆
  自訂提問後面的出口等於沒有。
- **label 與 text 分開**：顯示短標＋icon，送出的字維持原本那幾個。改 text 才是會出事
  的那種改動（按鈕送出的字沒人認得＝按了沒反應，批次 30／32 踩過兩次），這樣
  `detectMetaIntent()`／`GROUP_FIXED_BUTTONS`／`isOwnButtonText()` 三邊一行都不用改。

#### 這次的教訓

**「功能做出來了」跟「使用者找得到」是兩件事，而後者會因為通道不同而失效。** 同一份程式
碼在 1 對 1 是完整的、在群組是殘缺的，差別不在邏輯，在平台把哪些 UI 元件給了哪個場景。

之前每次補這類洞都是補「當下被回報的那一則訊息」（歡迎詞、只 @ 沒接問題），沒有回頭問
**「這個通道少了什麼，是不是每一則訊息都少」**。群組少的是常駐選單，那就是**每一則**
回覆都少，不是某幾則。

---

### 批次 41：按鈕永遠要按得動，不管隔多久（實測回報，2026-09-09）

回報：「請設定我群組只要按下按鈕都會回答……人家按按鈕，不會再特別加 @ 或米亞。」
截圖：按鈕列停在下午 6:55 那則答案上，晚上 8:58 有人滑回去點「這次活動的主要發表內容
是什麼？」——完全沒反應。

#### 根因：兩道各自獨立的門，關著任何一道都是沉默

| | 門 | 為什麼會關著 |
| --- | --- | --- |
| ① | `isOwnButtonText()` 守門 | 拿「目前綁定的那場」比對 chips，但活動綁定有 **6 小時 TTL**，過了 `getBinding()` 回 null，連比對都沒得比 |
| ② | `handleGroupMessage()` 答題 | 就算守門放行，這裡的 `getBinding()` 照樣 null → 掉進 `handleUnbound()` 的 `silentOnOther` → 沉默只是往後挪了一段 |

第二個成因同樣致命：中途換過場的話，舊訊息上的按鈕屬於**上一場**，跟現在綁的那場對不
起來，也會被判成「不是我們的按鈕」。

**LINE 的快速回覆按鈕會永遠留在對話紀錄裡。「隔幾小時往上滑再點」是常態，不是例外。**
把按鈕辨識綁在一個會過期的狀態上，從一開始就是錯的模型。

#### 做法

1. 新增 `ownChipEventId()`，比對範圍從「目前這場的 chips」放大到「所有我們可能送出去
   的 chips」（每一場的自訂 chips、活動前 chips、邀訪關鍵字，加上共用的
   `DEFAULT_CHIPS`），再回推是哪一場：上次綁的那場（**不看 TTL**，比照
   `getStoredMediaName()`）→ 唯一擁有這顆自訂 chip 的場次 → 推不出來就回空字串，
   讓呼叫端去問，但**不沉默**。
2. 綁定過期／不存在時，點到 chip 就把綁定接回那一場再答題。
3. `silentOnOther` 與 `routed.intent === 'other'` 兩道安靜門檻，對「我們自己的按鈕」
   一律關掉——安靜是為了不要對別人的閒聊插話，但按鈕是我們自己請對方按的，
   **按了沒反應永遠是 bug，不是體貼**。

保守的地方：綁定還在時，點到別場的舊按鈕**不會**偷偷換場。跨場次比對只用來「認出這是
我們的按鈕」跟「沒有綁定時推出場次」——「新聞稿」這種字同時是某場的邀訪關鍵字、也是任
何一場都可能想問的東西，拿它換場等於把記者從他正在問的那場拉走（坑 6）。

#### 這次的教訓

**同一個症狀（按了沒反應）已經修過三次（批次 30、32、40），每次都只修了當時看得到的那
一段。** 30 補了漏掉的按鈕種類、32 又補一種、40 補了群組看不到選單——但沒有一次去問
「這條路上**總共有幾道門**」。這次的兩道門一前一後，只修守門的話，症狀完全不會改變。

會反覆發生是因為每次都從「這顆按鈕為什麼不動」出發，而不是從「一顆按鈕要動起來，需要
連續通過哪些判斷」出發。後者才會逼你把整條路徑列出來，一次看完。

另一個可以更早發現的訊號：**按鈕辨識依賴一個會過期的狀態**。只要看到「辨識」跟「TTL」
綁在一起，就該問一句「過期之後這顆按鈕還在畫面上嗎？」——在 LINE 裡答案永遠是「在」。

---

### 批次 42：用窮舉證明「按鈕一定會回答」，順便抓到自己剛上線的 regression（2026-09-09）

批次 41 之後被要求的是一句保證：「只要確定群組的人按按鈕會回答他」。**保證不能靠人講，
要靠測試證明**——尤其這條路上已經連續三次（批次 30、32、40）敗在「漏掉的那一顆」。

#### `test/test-group-buttons.mjs`：採集 → 重放

寫死一份按鈕清單只能證明「我當初想到的那幾顆會動」，所以改成兩段：

1. **採集**：實際把機器人走過一輪（入群、看清單、選一場、問一題、邀訪窗口、產業趨勢、
   技術查詢、預設 chips、活動前 chips），把它**真的送出去的每一顆 quick reply** 的送出
   文字收集起來——目前 33 顆。
2. **重放**：每一顆按鈕 × 四種狀態（綁定活著＋視窗內／綁定活著＋視窗過期／綁定過期 7
   小時＋視窗過期／完全沒有綁定），一律**不 @、不寫「米亞」**，斷言一定有回覆。

以後有人加了新按鈕，只要那顆出現在採集路徑上就自動被涵蓋；漏接了會在這裡紅掉，不用
等使用者回報。反面也釘住：8 句一般閒聊在同樣的狀態下必須維持安靜。

#### 這支測試當場抓到批次 41 自己造成的 regression

`ownChipEventId()` 拿「這場按鈕列上的所有按鈕」回推場次，而按鈕列裡**也包含導覽鈕**
（回首頁、媒體邀訪需求、產業趨勢分析…）。那幾顆每一場都有，拿它們回推等於「按任何一顆
導覽鈕都會把綁定接回上一場」。實際症狀：群組按「回首頁」（本意就是解除綁定）之後再按
「媒體邀訪需求」，會被接回剛剛那場、拿到那場的窗口，而不是跨活動的全域清單。

修法是把 `eventContentChips()` 拆出來：按鈕列送「內容 ＋ 導覽」，回推場次只用**內容**。

#### 這次的教訓

**「每一顆按鈕都會有回覆」這個目標，有一個很便宜也很錯的達成方式：把每一顆按鈕都拿去
接回綁定。** 那確實讓每顆都有回覆，卻讓「回首頁」自己把自己取消掉。一個用「不能沉默」
描述的需求，很容易被實作成「什麼都接住」，而漏掉「接住之後要做對的事」。

窮舉測試會逼出這種錯：它同時檢查「有沒有回」跟「回的對不對」，只滿足前者會在後者紅掉。
採集式（而不是寫死清單）又讓它不會隨著功能增加而失效——這兩點才是這支測試的價值，不是
多了幾個斷言。

---

### 批次 43：群組的「常駐選單」＝每一則回覆都自己帶著它（回報，2026-09-09）

回報的截圖：群組按「媒體邀訪需求」→「邀訪：綠能」，拿到窗口聯絡人之後**整則訊息一顆
按鈕都沒有**，問完就斷在那裡。附帶的問題是：「建議改成常駐嗎？」

#### 先回答那個問題：LINE 沒有「常駐的快速回覆」

| | 綁在哪 | 群組有嗎 |
| --- | --- | --- |
| 快速回覆（quick reply） | **單一則訊息**，送出下一則就消失 | 有 |
| 圖文選單（rich menu） | 聊天室，常駐在輸入框下方 | **沒有**（1 對 1 專屬） |

所以群組裡「常駐」唯一的實作方式，就是**每一則回覆都自己帶著那排導覽**。這不是替代
方案，是這個平台上唯一存在的做法。

#### 做法：包一層，而不是補一輪

`api/line.js` 有五十幾處 `replyOrPush`，群組走得到的至少十幾處。手動補第四個參數就是又
一次「漏掉的那一顆」——這條路上已經連續四次（批次 30、32、40、41）敗在同一種錯。

改成把 `replyOrPush` 包起來：**呼叫端沒有自己給按鈕、而且對象是群組時，自動補上導覽。**
呼叫端給了就不覆蓋（那代表那則訊息有更貼切的選項：活動清單、邀訪主題、這場的快速
提問…）。群組／1 對 1 用 LINE 的 id 前綴分辨（使用者 U、群組 C、聊天室 R）。

新增的回覆從此自動有導覽，不用記得，也不會忘記。

#### 測試補的是另一個缺口

批次 42 的窮舉只保證「按了會有回覆」，保證不了「回覆之後還走得下去」——按鈕能按、但回
完就沒有下一顆，路一樣是斷的。新增一段走完整路徑（清單→選場→問答→這場的窗口→全域
窗口→「其他」自由輸入→產業趨勢→技術查詢→使用說明→換場→貼圖）的檢查，斷言**每一則
群組回覆都帶著按鈕**。在修好之前抓到 4 則死路，其中第一則就是回報的那一則。

#### 這次的教訓

**「能不能開始」跟「能不能繼續」是兩個不同的保證，測了前者不等於測了後者。** 批次 42
把「每一顆按鈕都會有回覆」窮舉得很乾淨，卻整個漏掉「回覆本身是不是死路」——因為那組
測試是從按鈕出發，不是從回覆出發。同一條路上，換一個方向看就是另一組洞。

實作上的對應教訓：**當一個規則要套用在幾十個地方時，正確的做法是讓它變成預設，而不是
去每個地方補一次。** 前四次都在補，第五次才想到包起來。

---

### 批次 44：補查不能亂抓，還有把使用說明變成一支 30 秒動畫（回報，2026-09-10）

#### 一、「新聞稿」三個字把補查帶進溝裡

回報的截圖：群組裡打「新聞稿」，本場正確地回了「這場狀態是稍晚提供，還沒有完整新聞稿
全文」＋大會手冊＋新聞聯絡人——**到這裡都對**。壞在後面自動接上的補查區塊：

- 拿「新聞稿」三個字去搜官網，撈回三篇只因為內文出現過這個詞而中的無關報導
  （致茂論文競賽、管風琴演奏會），還一本正經列出原文連結
- 引言寫「我在工研院官網新聞中心**找到了**：」，內文卻寫「我手上資料**沒有**能直接回答
  的內容耶 😊⋯⋯建議您換個更明確的關鍵字」——同一則訊息自己打自己

兩個各自獨立的洞：

| | 洞 | 修法 |
| --- | --- | --- |
| ① | 泛用詞也拿去查官網 | 「新聞稿／照片／資料／名單／簡報⋯」講的是**資料本身**、不是主題，官網是字面比對，用這種詞查，命中的必然是雜訊。整個關鍵詞剛好等於這些字就跳過補查（不做包含比對，「得獎名單」「開幕照片」照查） |
| ② | 補查的模型自己說「答不出來」，卻被當成答案用 | prompt 早就叫它「摘要裡沒答案就回空字串」，但**規則是請求、不是保證**。在出口補一道程式判斷：這段話本身就是「答不出來」的話，一律當空的處理，退回只附連結 |

②的形狀跟 `stripMarkdownForLine()` 一模一樣——請模型守規矩，再在出口擋一次。這比賭它
守規矩便宜太多，而且第二次踩到了。

**測試的教訓**：第一版把泛用詞測試綁在 quad，全綠——因為 quad 的 fixture 把「新聞稿」
設成邀訪窗口關鍵字，那條路先命中，根本走不到補查。**在壞掉的程式上綠燈的測試，比沒有
測試更危險。** 改綁 semi（沒設定 contacts）之後，7 項在修好前確實會紅。

#### 二、使用說明變成一支 30 秒動畫，而且點得到

回報：「說明影片 OK，只是米亞變太瘦了吧」「這影片可以在 1 對 1、群組裡，小按鈕按使用
說明會出現這個影片」。

- 米亞加寬加高（身體 72×62 → 100×78，手往外放大），比例從「瘦長」變回 Q 版該有的份量
- 動畫另存一份到 `public/guide.html`，掛在自家站台，`HELP_TEXT` 最前面加一行網址

⚠️ 網址一定要用自家站台的公開網址。這一行會直接送到記者手機上，**點不開的連結比沒有
連結更糟**——測試也釘住這一點（不可以出現需要登入或本機的網址）。

放在 `HELP_TEXT` 最前面是刻意的：整份說明四十幾行，記者在手機上多半只看得到開頭幾行；
「看 30 秒影片」的門檻比「讀完四十行」低得多，願意讀的人往下滑照樣讀得到。

---

### 批次 45：簡體字防線（回報，2026-09-10）

回報：「千萬不能出現簡體字好嗎」。截圖裡一整則回覆都是繁體，**只有最後那句警語跑出
簡體**——「内容仅供参考，以工研院官网新闻稿或发言为准。」

#### 根因：整份 prompt 從頭到尾沒有任何一條規則說「要用繁體」

警語那條規則只寫「內容等同於『內容僅供參考，以⋯⋯』」，模型就照自己的習慣重打一次，
偶爾打成簡體。而這句話**每則回答都有**，所以踩到只是遲早。

#### 兩層，跟 Markdown 那次同一個結構

| 層 | 做什麼 | 性質 |
| --- | --- | --- |
| ① | `askAnthropic()` 統一加一條 `ZH_TW_RULE`（所有模型呼叫的唯一入口，四條問答路線＋兜底文案全部受惠） | 請求 |
| ② | 出口 `toTraditionalTW()`，接在 `stripMarkdownForLine()` 後面 | **保證** |

#### 轉換表怎麼設計才不會把對的改成錯的

繁簡轉換最大的災難不是漏轉，是**把本來正確的字改壞**：「公里」→「公裡」、「余先生」→
「餘先生」、「皇后」→「皇後」。一個簡體字對到多個繁體字（发→發／髮、准→準／准、
后→後／后、里→裡／里、复→複／復／覆）時，無腦一對一轉必錯。

所以拆成三張表，每一張都寧可漏轉也不要轉錯：

1. **`S2T_WORDS`**：有歧義的字只在詞裡轉（`为准`→`為準`、`采访`→`採訪`、`其余`→`其餘`）
2. **`S2T_CONTEXT`**：有歧義但有規律的字，看左右鄰居決定（后→後，除非前面是皇／太／
   王／母）——比一條一條列詞組涵蓋得廣，實測就是靠這層接住詞表沒有的「活动后」
3. **`S2T_PAIRS`**：只收「繁體正常行文裡根本不會出現」的字，一對一，沒有歧義

`S2T_PAIRS` 在**載入時**就檢查格式（每項必須剛好兩個字、簡繁不得相同），錯了直接 throw——
寧可整支掛掉，也不要讓錯誤的轉換安靜地出現在記者的訊息裡。

⚠️ 鄰居清單要同時收簡體與繁體：出口拿到的字串多半**已經是繁體**（模型大致寫對，只有
零星幾個字是簡體），只列簡體會漏判——實測踩到「鄰里」被轉成「鄰裡」。

#### 測試分兩半，第二半比第一半重要

`test/test-zhtw.mjs`：① 該轉的轉得乾淨；② **本來就正確的繁體，一個字都不可以被改壞**
（公里／里程碑／鄰里／批准／不准／准許／皇后／太后／頭髮／髮型／余先生／于小姐／
范先生／系統／體系／風采）。

實測抓到兩個漏洞：`网`（！）根本沒收進表裡——回報截圖裡的「官网」就是它；`将` 也漏了。
**高頻字更要逐個確認**，越常用的字漏掉，被看見的機率越高。

#### 這次的教訓

**「模型會照著做」不能當成保證，第三次了。** Markdown（批次 32）、`[[NO_DATA]]` 標記
（批次 37）、補查說自己答不出來（批次 44），現在是繁體字。形狀完全一樣：prompt 有寫、
模型大部分時候照做、偶爾不照做，而偶爾不照做的那一次就是使用者截圖給你看的那一次。

**只要一件事「絕對不能發生」，它就必須在程式的出口被強制執行，而不是寫在 prompt 裡。**

---

### 批次 46：影片直接播在對話裡＋用對話教米亞（回報，2026-09-10）

#### 一、使用說明不再是一條連結

回報：「影片現在是跳連結，有可能直接在對話傳或播影片嗎？**不會有人特別還會去點連結
的**」。完全正確——使用說明的目的是「讓人真的看」，一條連結把「看」變成一個要主動決定
的動作，多數人就滑過去了。

把 `public/guide.html` 錄成 30 秒 MP4（720×1280、1.3 MB，遠低於 LINE 的 200 MB 上限），
按「使用說明」時送 `video` ＋ `text` 兩則，影片直接在對話裡顯示成可播放的畫面。

錄影踩到三個坑，都寫進腳本註解：

| 坑 | 症狀 | 解 |
| --- | --- | --- |
| viewport 直接開 720×1280 | 字級相對畫面縮成一半 | 版面照 360 手機寬設計，要用 360×640 錄再放大 |
| DSF 2 ＋ recordVideo 720×1280 | 畫面只畫在左上角 1/4 | viewport 與 recordVideo 尺寸要一致 |
| 起點靠猜 | 前段是空白的載入畫面 | 起點＝總長減 30.6（結束時間固定是按下重播後 30.6 秒） |

#### 二、用對話教米亞

回報：「我能設定，進入職員模式後可以透過跟他對話來改進他的能力或內容嗎？」

分兩種，因為性質差很多：

| 類型 | 例子 | 存哪、怎麼用 |
| --- | --- | --- |
| `fact` | 「記住：這場地點改到南港展覽館」 | 綁在某一場，接在該場新聞稿後面，標成**以這裡為準** |
| `style` | 「語氣：回答再短一點」 | 全站，接在**每一次**模型呼叫的規則後面 |

⚠️ **一條刻意的界線：style 不拿來解決「絕對不能發生」的事。** 「不要回簡體字」這種要求，
正確的做法是在程式出口強制轉（批次 45），不是加一條 prompt 規則。同仁真的這樣打，我們
照樣記起來（記著沒壞處），但真正的保證在程式裡。

⚠️ **自然語句一律先問一次再生效。** 「這場的地點改成南港展覽館了嗎？」是提問，去掉問號
就是指示——判斷錯的代價是把同仁的提問寫進知識庫、然後**每個記者都讀得到**。打明確指令
（「記住：⋯⋯」）的人已經表達得很清楚，就不用多問。

其他幾個刻意的決定：

- 指令用**字面比對**，不交給模型判——跟 `isExitStaffCommand()` 同一個理由：會不會被寫進
  知識庫，不該取決於模型當下判得準不準
- 沒綁定場次時**不會默默記成全站**，先問是哪一場（那是兩件完全不同的事）
- 生效中的筆數有上限（40 條）：這些內容每一題都會帶進 prompt，太多會讓每一題都變慢變貴
- `bot_memory` 讀取吃 60 秒快取，寫入後立刻失效——同仁剛教完，下一題就要用得到

#### 這次的教訓

**同一個需求，該用哪一層解決，取決於「做不到的時候會怎樣」。** 語氣偏好偶爾沒照做，
沒人會受傷；簡體字或張冠李戴跑出去，記者可能直接引用。前者交給 prompt 沒問題，後者
一定要在程式裡強制。把兩者混在同一個機制裡，看起來很彈性，實際上是把最重要的保證
交給最不可靠的環節。

---

### 批次 47：影片沒跳出來——LINE 收下了 200，然後靜靜不顯示（回報，2026-09-10）

回報：按「使用說明」只出現文字，影片沒跳出來。

#### 先查 log，不要猜

正式站的 runtime log 裡**一個錯誤都沒有**：

```
[line] meta intent=help user=U4af71... q="使用說明"   ← 200，沒有任何 error
```

`replyOrPushMessages()` 只在 `!res.ok` 時記錯誤，而 LINE 是**收下請求就回 200，之後才
自己去抓媒體檔**——抓失敗它不會回頭通知我們。**這個洞在伺服器端完全看不見，只有使用者
看得到。**

順手排除了服務端的可能：正式站的 `/mia-guide.mp4` 回 `206`（支援 Range，LINE 的抓取器
會用）、`content-type: video/mp4`、`content-length` 正確。所以問題在檔案本身。

#### 根因：那支 MP4 只有影像軌，沒有聲音軌

```
index=0  codec_type=video  h264      ← 只有這一條
```

錄螢幕本來就沒有聲音，當時就直接 `-an` 不做音訊。一般播放器對「沒有聲音軌的 mp4」
接受度不一，LINE 就是不吃。補一條靜音的 AAC 軌，讓檔案長得跟手機錄出來的影片一樣
（H.264 Main ＋ AAC LC，997 KB）。

#### 兩個配套

1. **文案改成「影片沒出來也讀得通」。** 上一版寫「⬆ 上面那支 30 秒影片就是完整說明」——
   影片真的沒出來時，這句話指著一個不存在的東西。改成「▶ 30 秒看完：上面的影片，或開
   這一頁」，兩種情況都成立。測試也釘住不准再寫成前者。
2. **`test/test-guide-video.mjs`**：直接在檔案裡找 MP4 的 sample entry 盒子名稱
   （`avc1` ＝ H.264、`mp4a` ＝ AAC），不依賴 ffprobe（CI 不一定有）。順便驗 faststart
   （`moov` 要在 `mdat` 前面）、檔案大小在 LINE 上限內、封面是 JPEG 且小於 1 MB。
   在無聲軌的版本上跑，那一條確實會紅。

#### 這次的教訓

**「送出成功」不等於「使用者看得到」。** 只要一段流程有「我送出去 → 對方之後才去取」的
非同步環節，回傳的 200 就只代表前半段。這種洞不會出現在任何 log 裡，只會出現在截圖裡——
所以能在**送出之前**檢查的東西（檔案格式、尺寸、必要的軌），就要在送出之前檢查。

---

### 批次 48：1 對 1 也找不到回家的路（回報，2026-09-10）

回報：「切入每場活動後，就不知如何回到首頁」——附的截圖是 **1 對 1**，按鈕列上是同仁自訂
的「技術影片／媒體聯訪對象／新聞稿／大會手冊…」，一條往外的路都沒有。

#### 🔄 這是批次 40 的判斷錯誤，不是新問題

批次 40 只在群組加導覽，理由白紙黑字寫著：

> 1 對 1 有圖文選單撐著，同一組入口再塞進按鈕列只會排擠掉同仁自訂的提問

聽起來合理，實際不成立：**圖文選單雖然常駐，但它是收合的**（要先點輸入框上方那條
「功能選單」才展開），而記者的視線在剛收到的那則答案上——按鈕列就貼在答案下面，圖文選單
不在視線裡。

**「存在」跟「當下看得到」是兩件事。** 這條路上已經是第二次栽在同一個分辨上：批次 40 是
「群組沒有選單」，這次是「有選單但沒展開」。當時我把後者當成前者的反例，其實兩者是同一
個問題的兩種樣子。

#### 做法

兩邊都放往外的路，只是密度不同：

| | 往外 | 內容 | 其他 | 合計 |
| --- | --- | --- | --- | --- |
| 群組 | 🏠📅 | 8 | 📊🔬📞 | 13 |
| 1 對 1 | 🏠📅 | 10 | 📞 | 13 |

1 對 1 少放「產業趨勢／問技術」，是因為那兩條路不是用來「脫困」的，而且圖文選單展開後
就有——真正被回報找不到的是「回首頁」。常數名稱也從 `GROUP_NAV_*` 改成 `NAV_*`：它不再
只屬於群組，名字繼續叫 GROUP 會誤導下一個讀的人。

#### 順手補掉一個測試的坑

新增的測試要驗「同仁放了 20 顆自訂提問時導覽不會被擠掉」，於是覆寫了 fixture 的 chips 欄——
結果**後面每一個情境都拿到被改過的 fixture**。`reset()` 原本不會還原 `state.events`。

這種 bug 的症狀是「單獨跑會過、整份跑會紅」，最難查。改成 `reset()` 每次還原一份深拷貝。
連帶要修的是情境 21.99：它原本把 `fellow` 那場**加在 reset() 之前**（當時是對的），現在必須
加在之後。

#### 這次的教訓

**當你為了「不要排擠掉別的東西」而決定不放某個入口時，先問那個東西當下看不看得見。**
批次 40 的取捨是拿「按鈕列的空間」去換「圖文選單的入口」，但後者在使用者需要它的那一刻
根本不在畫面上——等於用有的東西換了沒有的東西。

---

### 批次 49：文字在前、影片在後，並配上台灣腔口白與背景音樂（回報，2026-09-10）

回報兩點：「使用說明，先放文字再放影片，不然文字這麼多，影片早就被淹沒看不到」、
「可以配聲音，有口白＋背景音樂，口白要用台灣腔，可愛一點也行」。

#### 一、順序反了，而且文字太長

聊天室是**由上往下長**的，最後一則才停在畫面最下方、緊貼輸入框。影片放前面，後面那串
33 行的文字會把它整個推出畫面——使用者看到的就是一堆字，影片在哪不知道。

兩件一起改：**文字在前、影片在後**，並把 `HELP_TEXT` 從 33 行 609 字砍到 **18 行 343 字**。
影片負責講完整流程，文字只留速查（現場／平常／迷路／採訪／群組五行）。

⚠️ `quickReply` 要跟著搬到**最後一則**：LINE 只顯示最後一則訊息的快速回覆，留在文字那則
整排會消失。

⚠️ 測試釘住長度上限（20 行／400 字）。這不是形式主義——文案一長回去，影片就又被推出
畫面，而那正是這次被回報的問題。

#### 二、聲音

| | 做法 |
| --- | --- |
| 口白 | `edge-tts` 的 `zh-TW-HsiaoYuNeural`（三個台灣語音裡唯一標「Taiwanese Mandarin」的），語速 +15%、音高 +12Hz 讓它輕快一點 |
| 背景音樂 | **自己合成**（`tools/guide-video/bgm.py`）：I–V–vi–IV 的音樂盒琶音。不用現成音軌＝授權乾淨，長度也能剛好切在 30 秒 |
| 混音 | 口白對齊每格起點，背景音樂用 `sidechaincompress` 讓給口白 |

兩個關鍵細節：

- **一句一檔，不是整段念完。** 整段的長度不可能剛好落在每 5 秒的切點上，畫面換了口白還在
  講上一格，比沒有口白更糟。
- **每句要唸得完在 5 秒內。** 第一版每句 5.5～7.7 秒、總長 37.9 秒，塞不下——**砍句子**，
  不是一味加快語速（語速再快會變得不像人講話）。

整條流程（錄畫面 → 口白 → 配樂 → 剪接）連同三個踩過的坑都寫進
`tools/guide-video/README.md`，改了 `guide.html` 就照著重跑一次。

#### 這個環境的一個坑

對外 HTTPS 走 agent proxy、TLS 在那裡重新終結，而 `edge_tts` 讀的是 **certifi 的 bundle**、
不是 `SSL_CERT_FILE`，aiohttp 預設也不吃環境變數裡的 proxy——兩件都要自己補（把
`HTTPS_PROXY` 傳進 `Communicate(proxy=...)`）。**絕對不是關掉 TLS 驗證**。

#### 這次的教訓

**排版順序是內容的一部分，不只是美觀問題。** 同樣兩則訊息，順序對調就決定了使用者「看不
看得到影片」。在由上往下長的介面裡，**最重要的東西要放最後**——這跟文件、簡報的直覺剛好
相反，很容易寫反。

---

### 批次 50：米亞的聲音定下來了（回報，2026-09-10）

上一批的口白用 edge-tts，回報是「聲音太像機器人」，朱朱自己去
[Vidnoz](https://tw.vidnoz.com/text-to-speech.html) 挑了一個童聲匯出 wav 給我。
聽過之後的評語是「**還是有點 AI 感覺，幫我改得可愛活潑一點**」，外加一個長期要求：
「**以後都用這個聲音，當成這個 LINE 的聲音**」。

#### 一、「AI 感」是三件事，不是一件

拿到的檔是 16 kHz 單聲道、27.4 秒、六句。逐項拆開來看：

| 症頭 | 為什麼聽起來假 | 處理 |
| --- | --- | --- |
| 音色偏成人、偏平 | 跟「可愛」的角色設定對不上 | `rubberband` 往上移調 1.7～2.4 個半音，共振峰跟著移＝聽起來像個子小一點的人 |
| 8 kHz 以上完全空 | 16 kHz 取樣的必然結果，耳朵聽起來像在講電話 | `aexciter` 合成高頻諧波，9 kHz 以上從 −54 dB 拉到 −43 dB |
| 每句音高語速一模一樣 | 這才是「在念稿」的主因 | **逐句**給不同的移調與語速，打招呼那句與收尾那句最亮最快 |

第三項最關鍵。前兩項是音色，第三項是**語調**——人講六句話不會每句都同一個高度，
TTS 會。既然拿到的是已經合成好的音檔、改不了合成參數，就在後製逐句拉開差距。

#### 二、兩個踩到的坑

**`aexciter` 排在 `aresample` 前面 → 整段變方波。** 原檔 16 kHz，Nyquist 只有 8 kHz，
叫 exciter 去合成 15 kHz 的諧波會全部折返成鋸齒雜訊。症狀很好認：峰值卡在 0.0 dB、
直方圖全部擠在同一格。**先升到 44.1k 再加高頻。**

**用單次 `loudnorm` 對齊音量 → 底噪被推上來。** loudnorm 是動態的，遇到句子頭尾那半秒
的靜音會一路加大增益。當時六句的尾巴全都是一模一樣的 **−4.2 dB**——一模一樣就是線索，
那不是聲音，是被放大的嘶聲。要對齊音量就**量完再套固定增益**（`volume`）。

> 這兩個坑有同一個形狀：**指標好看不代表東西是對的。** 第一版量出來的響度是漂亮的
> −17 LUFS，但那個數字是嘶聲貢獻的。跟批次 44「在壞掉的程式上綠燈的測試」是同一件事。

#### 三、把「以後都用這個聲音」變成真的

朱朱要的不是這一支影片好聽，是**下一次不用再交代一次**。所以：

- `tools/guide-video/voice/mia-voice-ref.wav` — 原始匯出檔，台詞沒變就直接重用，不用回 Vidnoz
- `tools/guide-video/voice/cute.py` — 加工配方，靠靜音自動切句；**段數對不上台詞句數就直接停**，
  不硬切（硬切會把兩句黏成一句，對齊全跑掉，而且要整支聽完才會發現）
- `tools/guide-video/voice/README.md` — 台詞變了怎麼辦（回 Vidnoz 用同一個語音，句間停 1 秒）
- **`CLAUDE.md`** — 新增。把「米亞只有一種聲音」跟其他幾條長期約定寫進去

最後一項才是重點。寫在對話裡的交代會隨著對話消失，寫在 `tools/` 深處的 README 要有人
想到去開——`CLAUDE.md` 是唯一每次都會被讀到的地方。

語音名字是「**HsiaoYu（Taiwan）**」（朱朱當天補的）。有趣的是，這跟被嫌「太像機器人」
的 edge-tts `zh-TW-HsiaoYuNeural` **是同一個語音**——差別全在 Vidnoz 那邊的語調設定。
所以要念新句子時，得回 Vidnoz 挑同一個語音，不能改用 edge-tts 直接合成。

> 這件事本身也是個提醒：**「聲音不好聽」不一定是選錯了聲音。** 當時的直覺是「換一個
> 語音」，實際上換的是同一個語音的**唸法**。先確認問題出在哪一層，再決定要換什麼。

#### 這次的教訓

**「以後都要這樣」是一種需求，跟功能需求一樣要交付。** 使用者講這句話的時候，交付的
不是這一次的成品，是「下一次不用再講一次」。放對地方（會被自動讀到的地方）比寫得詳細
更重要。

---

### 批次 51：問新聞稿，回的卻是行事曆（回報，2026-09-10）

截圖裡的流程：群組打「米亞 最近工研院有哪些新聞」→ 米亞回【近期活動】→ 記者只好
再問一次「最近發的新聞稿麼」。問新聞稿、回記者會場次表。

#### 一、根因：兩個句型長得太像

這句話沒對到任何死板規則，掉進 `routeIntent()`。而路由的 prompt 裡，`calendar` 的
例句就是「**最近有哪些活動**」——跟「**最近工研院有哪些新聞**」只差兩個詞。模型判成
calendar 一點都不意外。

真正諷刺的是，這條路早就鋪好了：`lib/itri-news.js` 的 `fetchItriNews()` 註解寫著

> 不給關鍵字（或空字串）就退回「最新新聞」不篩選——**這個分支是給之後萬一有
> 「不指定技術、直接看工研院最新動態」需求時的退路**

寫了，但從來沒有任何入口叫得到它。**能力存在，跟使用者叫得出來，是兩件事**——這跟
批次 48「圖文選單存在，但預設是收合的」是同一個形狀。

#### 二、修法：兩層，不是一層

| 層 | 做什麼 | 擋什麼 |
| --- | --- | --- |
| ① `isLatestNewsQuestion()` | 死板規則，命中直接走最新新聞清單 | 保證回報的那幾句一定答對 |
| ② 路由 prompt ＋ `answerTechQuery()` 泛稱守門 | AI 判成 tech_query 但抽不出技術關鍵字時，也走最新清單 | 規則接不住的講法 |

第一層的判斷刻意**不寫成一條長 regex**，而是「扣掉核心詞（新聞／新聞稿），再扣掉
時間詞、疑問詞、語助詞、『工研院』，看還剩不剩字」。這個寫法的好處是**失敗的方向
是安全的**：漏收一個語助詞只會讓這條不命中（退回原本的 AI 路由），不會反過來把
「半導體最近有什麼新聞」這種有主題的問題吃掉——那才是真正糟糕的結果，記者會拿到
一份不相干的清單。

兩個刻意的取捨：

- **裸詞「新聞」不命中。** 理由同 `TECH_QUERY_EXACT_RE` 不讓裸詞「技術」命中：兩個字
  太容易是別句話的一部分，邀訪窗口的關鍵字設定裡就有「新聞稿」。
- **「消息」不收。** 群組裡命中 `detectMetaIntent()` 就會回答，而「最近有什麼消息」
  在記者群裡十之八九是人在問人——多收一個口語詞換來的是米亞插話，划不來。

第二層順手補掉一個一直都在的洞：按了「想問什麼技術」、記者卻打「新聞稿」時，以前
會拿那三個字去官網做字面搜尋，撈回來的必然是雜訊。`NO_DATA_GENERIC` 當初就是為了
擋這件事而生的，但那道守門只裝在補查那條路上，`answerTechQuery()` 沒裝。

#### 三、還有一件不是程式能修的

同一張截圖上面，IEK 的兩張連結預覽卡都印著一個裸的 `<p>`。查了 IEK 那一頁的原始碼：

```html
<meta name="description" content="<p
```

**是 IEK 自己網頁的 meta 標籤壞了**，LINE 只是忠實地把它顯示出來。我們這邊沒有東西
可以改（除非自己架一層帶 og 標籤的轉址頁，但 Vercel Hobby 的 function 額度只剩
1 格，見坑 2，不值得為這件事用掉）。IEK 是工研院自己的產科國際所，請朱朱直接反映
比較快。

#### 這次的教訓

**「已經做好但沒有入口」跟「沒做」，對使用者來說是同一件事。** 這個專案現在踩到第二
次了（批次 48 的圖文選單、這次的最新新聞清單）。寫下退路的時候順手問一句：**誰會叫
得到它？** 叫不到就不算做完。

順帶一提，這次的修法也在 `HELP_TEXT` 補了一行「【新聞】打『最近有哪些新聞』」——
規則修好只解決「問了會不會答對」，那一行解決的是「知不知道可以問」。

---

### 批次 52：職員模式是「另一個世界」，不是疊在記者模式上（回報，2026-09-10）

原話：「**職員模式要重新思考改進，不好用**」。截圖：在職員模式問「最近有發什麼
新聞稿」，拿回來的是一份八條的職員功能清單。

#### 一、根因不是判錯，是結構

走進 `handleStaffMessage()` 之後，每一則訊息都只過 `routeStaffIntent()` 那七個管理
意圖。記者端有的東西——最新新聞稿、產業趨勢、工研院技術、邀訪窗口——**一個都叫不
到**，全部落進 `'other'`，換來一面功能清單的牆。

也就是說：**公關同仁是這個帳號用得最兇的人，卻是能力最少的人。** 進職員模式等於
放棄記者模式，而不是在它之上多拿到管理權限。

#### 二、修法：疊加，不是取代

| 位置 | 改成 |
| --- | --- |
| `routeStaffIntent()` **之前** | `detectMetaIntent()` 命中 news／industry_trend／tech_query／contacts → 直接走記者端那套（順便省一次模型呼叫） |
| `detectMetaIntent()` 命中 help／switch | 給職員功能表，不是記者那份 30 秒影片 |
| 最後的 `'other'` | 不再丟功能清單，改交給記者端的自然語言路由 `handleUnbound()`，用它的智慧兜底 |

⚠️ **`'calendar'` 刻意不在字面比對那層短路。** 職員的「所有場次的後台數據」也會命中
`CALENDAR_RE`（「所有…場次」），短路掉就再也查不到後台數據了——那條要留給
`routeStaffIntent()` 用語意判。測試釘住了這一條，也釘住了六顆職員按鈕都不會被記者端
意圖攔走：**管理功能被攔走，比原本「問新聞稿拿到清單」嚴重得多。**

#### 三、功能清單本身也重做

原本那面牆只列管理功能。同仁根本不知道記者問得到的東西自己也問得到，也不知道
可以用對話教米亞（批次 46 做好了，除了這份文件之外沒有任何地方寫著怎麼叫它）。
現在分三段：【管理】【記者問得到的，您一樣問得到】【教米亞】，而且**只在同仁真的
問「使用說明」時才出現**，不再拿來當「聽不懂」的兜底。

快速回覆也補了「最近有哪些新聞」「記憶清單」兩顆——補的不是新功能，是**入口**。

#### 四、順手修好第二次回報的講法

批次 51 收的是「最近工研院有哪些新聞」，但這次截圖的原話是「最近**有發**什麼新聞
稿」——第一版只收了「有什麼」「發了」這種**詞組**，「有」跟「發」拆開來各自出現就
接不住。

> **修規則的時候要照使用者的原話測，不要只測自己想得到的講法。** 我上一批才剛修過
> 同一條規則，用的是我自己想的句子，結果他真正打的那一句照樣漏掉。

#### 這次的教訓

**權限模式應該是「加法」，不是「切換」。** 給一個人更多權限的時候，順手問一句：他
原本有的東西，還在嗎？這次的答案是「全都不見了」——而且不見了很久都沒人發現，因為
功能清單看起來很像一個正常的回答。

---

### 批次 53：點到旁邊，兩百字的知識庫就沒了（回報，2026-09-10）

原話：「**我在後台新增活動時，如果滑鼠點到旁邊，就會跳掉，造成資料未存**」。

#### 一、根因只有一行

```html
<div id="event-modal" class="modal-overlay" onclick="if(event.target===this)closeModal()">
```

`closeModal()` 就只是 `display:none`。點到 modal 外面那圈灰底 —— 整份表單連問都不問就
消失。知識庫欄位一填就是好幾百字，範本裡有十四個區段，而且**填到一半正是最容易點歪的
時候**（要捲動、要對照旁邊的新聞稿）。

這個 `onclick` 從第一版就在，一直沒人回報，是因為出事的人多半以為是自己手殘。

#### 二、修法：三層，而且第三層才是真的防線

| 層 | 觸發 | 行為 |
| --- | --- | --- |
| 1 | 點外面那圈灰底 | 有改動就**完全不關**，只跳提示。這是誤觸，連 confirm 都不該跳——跳了也會被順手按掉 |
| 2 | 「取消」／ESC | 這是明確意圖，`confirm()` 問過再關 |
| 3 | 草稿（localStorage） | 不管用什麼方式離開，內容已經躺在那裡，下次打開同一場活動跳橫幅問要不要還原 |

前兩層擋的是「這個 UI 上的誤觸」，第三層擋的是**所有我們想不到的意外**——關分頁、按到
上一頁、瀏覽器當掉、筆電沒電。這跟批次 45（簡體字）是同一個判斷：與其把每一種離開方式
都想過一遍再各補一道，不如假設「總有一種方式會漏掉」，在更下游放一張網。

順手補上的同一形狀的洞：

- **儲存失敗時 modal 不關**（原本會關，錯誤只用 toast 飄一下，內容一起帶走）
- **「以既有活動為範本」下拉會整段蓋掉知識庫**——已經填了東西就先問過
- **照片上傳中不准關**——關掉的話檔案已經在 Blob 上，網址卻永遠拿不回來
- Ctrl/Cmd+S 直接存檔。不接住的話瀏覽器會跳「儲存網頁」，反而讓人以為存好了

#### 三、`MODAL_FIELDS`：一份清單，三個地方共用

dirty 判斷、草稿存檔、草稿還原都讀同一個陣列。真正的用意在測試那一條：**測試會去比對
markup 裡每一個 `id="input-…"` 有沒有在清單裡**。以後有人加了欄位卻忘了加進清單，草稿
就會少存那一欄——那是「看起來有救回來，其實少一段」的爛 bug，比整段不見更難發現。

dirty 也刻意**不用「有沒有打過字」的旗標**，改成跟開啟當下的快照比對。打了又改回來算
沒改；而上傳照片、複製範本這種**程式化寫入不會觸發 `input` 事件**的路徑，也一併涵蓋，
不用每個地方都記得舉手。

#### 四、測試怎麼寫

`test/backend-event-modal.test.mjs` 把 `public/index.html` 裡那段 `<script>` 抓出來，配一個
最小的假 DOM 用 `node:vm` **真的跑起來**，驗的是行為不是字串：點外面不會關、取消要先問、
草稿存得進也還原得回、存檔成功要清草稿、存檔失敗要保住。

驗證方式是**把修好的那行改回舊行為，確認測試會紅**（6 條失敗）。沒做過這一步的測試，等於
沒有測試。

#### 五、順帶：三個各說各話的字數上限，合併成一套

同一批回報的第二件事：「合併上限」。原本的狀況是

| 位置 | 畫面上寫著 | 實際行為 |
| --- | --- | --- |
| 後台 `index.html` | 最多 8000 字 | **完全不擋**，送出去才被後端退 400 |
| 同仁編輯頁 `edit.html` | 最多 8000 字 | 超過 8000 跳 confirm，45000 才被後端退 |
| `api/events.js` | —— | 硬上限 45000（Sheets 單一儲存格約 5 萬字元） |

也就是說**畫面上那句「最多 8000 字」是假的**，而真正會被退件的那條線沒有任何地方講。
同仁貼了一份四萬多字的完整技術資料，要按下儲存、等它跑完，才看到一句「內容過長」。

合併成兩層意思，兩個前台講法與行為一致：

- **8000 = 建議值（軟）**：超過只是讓每次回答的輸入變長、變慢、變貴，還是存得進去 → confirm
- **45000 = 硬上限**：超過真的存不了 → 前台自己擋下，一個請求都不送

而且**打字當下就看得到**：字數計數器分三段，超過建議值變橘、超過硬上限變紅並直接寫
「存不進去」。順手把邀訪窗口分工那條沒人提過的 20000 也一起補上前台檢查。

⚠️ `public/` 是靜態檔，沒辦法 `import` `lib/`，所以這兩個數字只能在三個檔案各寫一份。
`test/kb-limit.test.mjs` 就是那份「不准漂開」的保證，而且**連給人看的中文文案也一起釘**
——常數改了、旁邊那句說明沒改，是這種鏡射最常見的下一個 bug。兩種漂法都驗過會紅。

#### 這次的教訓

**「關閉」跟「捨棄」是兩件事，UI 上常常長成同一個動作。** 任何一個按了就會讓使用者輸入
消失的地方，都要問一句：這個動作**誰按得到、按錯的機率多高、按錯的代價是什麼**。灰底那
一圈是整個畫面上最大的一塊點擊區，代價卻是最高的——這個組合本身就是設計錯誤。

**還有一條：介面上寫的限制如果不是真的，比沒寫更糟。** 「最多 8000 字」這句話存在很久，
它同時做到了兩件壞事——讓守規矩的人以為 8000 是硬牆而白白刪內容，又讓真的會撞牆的人
（四萬字那種）毫無預警。數字只要出現在給人看的地方，就要跟程式裡的那個是同一個。

---

### 批次 54：按了一顆按鈕，其他八個入口就不見了（回報，2026-09-11）

原話：「**職員模式 小按鈕這樣邏輯怪怪的**」。截圖：在職員模式點「記憶清單」，米亞
回「目前沒有記住任何東西」，底下**只剩孤零零一顆「最近有哪些活動」**。

#### 一、兩個問題疊在一起，第二個嚴重得多

第一眼看到的是**那顆按鈕接不上上下文**——剛看完記憶清單，給的卻是「最近有哪些
活動」，而且那是記者端的按鈕。

但真正的問題在旁邊：同仁是**從九顆按鈕裡點「記憶清單」進來的**，回來只剩一顆。

```js
// 職員模式的一般回覆
await replyOrPush(..., STAFF_QUICK_REPLIES);     // 9 顆完整入口

// 但 handleTeachMessage() 裡每一則都自己手寫
await replyOrPush(..., formatMemoryList(mems), ['最近有哪些活動']);   // 1 顆
await replyOrPush(..., '已經忘記這條 🗑⋯',      ['記憶清單']);       // 1 顆
```

整條記憶分支自己另外做了一套按鈕規則。**按一顆按鈕就被降級**，看起來像掉出職員模式。

#### 二、這是批次 52 的同一個形狀

批次 52 的教訓是「權限模式應該是加法，不是切換」——當時不見的是**能力**（職員叫不到
記者端的功能）。這次不見的是**入口**（走進某個分支，按鈕列縮水）。同一件事的兩個面向。

`api/line.js` 裡其實早就寫過這條原則，就在 `sendFallbackGuide()` 上面：

> 同仁在職員模式裡拿到一整排記者按鈕會以為自己被踢出去了。

原則寫過了、但只套用在那一個函式上，沒有變成整個模式的規則。所以改法不是「把那顆
按鈕換掉」，而是**把規則做成一個函式，讓偷懶的寫法自動是對的**：

```js
// 情境按鈕排前面，後面一律接上整套入口，去重後砍到 LINE 的 13 顆上限
function staffChips(...front) {
  return [...new Set([...front.filter(Boolean), ...STAFF_QUICK_REPLIES])].slice(0, 13);
}
```

記憶分支的八則回覆全部改走它。清單是空的時候第一顆排「使用說明」（職員版的使用說明
裡有【教米亞】那段，講的就是怎麼教）；有內容時就照原順序，「記憶清單」自己也在裡面，
想再看一次不用打字。

⚠️ **刻意不做「記住：⋯⋯」按鈕。** 快速回覆是把固定字串直接送出去，送一句沒有內容的
「記住：」只會換來一次聽不懂。

⚠️ **也刻意不做「忘記 1」按鈕。** 那是一鍵刪除、沒有二次確認——按錯的代價跟批次 53
的「點旁邊就沒了」是同一類。

#### 三、唯一的例外要寫在程式旁邊

「這句話要我以後都照做嗎？」那一則維持只有【記起來／不用記】兩顆。這一刻只有兩條路，
旁邊擺一排別的出口只會讓人點走、留下一筆永遠 pending 的內容。

理由直接寫在那一行上面，**標明「不要順手統一掉」**——不然下一個人（包括下一次的我）
看到七則都用 `staffChips()`、只有這則沒有，第一個念頭就是把它補齊。

#### 四、測試補的是「按鈕」，不是「回答」

記憶這條路本來就有十幾條測試，但**全部在驗回覆的文字與資料庫狀態，沒有一條看按鈕**。
所以這個 bug 在測試全綠的情況下活了兩批。

新增 11 條：空清單／有內容的清單／教完／編號不存在／沒綁定場次，每一則都斷言整套
九顆入口都在；確認問句斷言**只有兩顆**；再加一條按鈕數不超過 13。反向驗證過——把那
一行改回 `['最近有哪些活動']`，四條轉紅。

#### 這次的教訓

**「原則寫在註解裡」不等於「原則會被遵守」。** 那句「同仁拿到一整排記者按鈕會以為
自己被踢出去了」寫得很清楚，就在同一個檔案裡，然後隔壁的函式照樣違反它——因為遵守
它要多打字，違反它比較快。

跟第 2 節的判準是同一件事：**把對的做法變成最省事的做法**。`staffChips()` 沒有增加
任何新能力，它只是讓「隨手寫一顆按鈕」這個動作自動帶上其他八顆。

順帶補上一個一直缺的入口：`STAFF_QUICK_REPLIES` 現在有「使用說明」。兜底文案一直叫
同仁「打『使用說明』可以看內部功能」，卻沒有任何一顆按鈕點得到——功能有、入口沒有，
又是批次 51 那個形狀。

---

---

## 6. system prompt 要加的規則

沿用 `api/chat.js` 那份（搬到 `lib/prompt.js` 共用），額外附加：

**LINE 通用**

```
- 這是 LINE 對話，請控制在 5 行以內。記者要完整新聞稿時，給該場網址請他自行下載，不要在對話裡倒全文。
- 需要附連結時直接給網址純文字，不要用 Markdown 語法（LINE 不會渲染，記者會看到一堆星號與方括號）。
- 你的回覆會出現在掛著主辦單位名義的官方帳號裡，記者可能直接截圖引用。任何不確定的內容，寧可說「這部分我沒有資料，建議洽新聞聯絡人 ○○○」。
```

**跨場次模式專用（批次 3，這三條是新風險的防線）**

```
- 你手上是多場記者會的資料。回答時務必說明「這是哪一場、什麼時候」，不要讓記者以為是同一場。
- 絕對不要跨場次做歸納、統計、比較或趨勢推論（例如「工研院今年共發表 N 項技術」「這比去年進步」）。這類問題只能回「以下這幾場提到相關內容」並分場引述原文，剩下的請記者自行判斷。你做的任何跨場總結都會被當成主辦單位的官方說法。
- 若同一件事在不同場次有不同數字或說法，如實並列並標明各自出處與日期，不要自行挑一個或取平均。
- 尚未舉行的場次，只能提供日期、地點、性質與新聞聯絡人，不得透露任何內容細節。
```

**archived 場次的時間戳警語**（與現有結尾警語**並存，不是取代**）

```
（本場為 YYYY 年 MM 月資料，最新進展請洽新聞聯絡人。）
```

---

## 7. 人類要先做的事

### 批次 1 不需要任何前置，可直接開工

### 批次 2 之前要做的（LINE，約 20 分鐘，全部免費）

1. 用**個人 LINE 帳號**到 https://manager.line.biz 建立官方帳號
   - **前三批刻意用「未認證帳號」**：免審核、5 分鐘開好、搜尋不到（只能靠 QR／連結加入）
   - 名稱要**跨場次通用**（終局是常設窗口），但**先不要掛「工研院」**。例如「記者會新聞小幫手」
2. LINE Official Account Manager →「設定 → 回應設定」：Webhook 開啟、聊天也開啟（提示時選**同時啟用**）；**關掉「自動回應訊息」**否則會跟 webhook 搶著回；歡迎訊息改成引導語
3. https://developers.line.biz 建立 Provider → 選這個帳號的 Messaging API channel
4. 取得 **Channel secret**、**Channel access token（長期）**、**官方帳號 LINE ID（@開頭）**
5. Vercel → Settings → Environment Variables：

   | 變數 | 值 |
   |---|---|
   | `LINE_CHANNEL_SECRET` | Channel secret |
   | `LINE_CHANNEL_ACCESS_TOKEN` | 長期 access token |
   | `LINE_BASIC_ID` | `@` 開頭的官方帳號 ID |
   | `LINE_ADMIN_USER_ID` | 使用者自己的 LINE userId（批次 3 的邀訪通知用，可先留空） |

   設完要 **Redeploy**
6. LINE Developers Console → Messaging API → Webhook URL 填 `https://itri-event-ai.vercel.app/api/line`，按 **Verify** 要顯示 Success
7. 同頁把「Auto-reply messages」「Greeting messages」關掉（與步驟 2 重複，兩邊都要確認）
   - ⚠️ 歡迎訊息一定要在 LINE 後台**關掉**：批次 4.5 之後 `follow` 事件會由 webhook 送
     Flex 歡迎圖卡，後台那則若還開著，記者加好友會一次收到兩則歡迎詞
8. 部署完成後，用職員模式在 LINE 打一句「**設定圖文選單**」，把聊天室下方的常駐選單裝上去
   （只需做一次；之後只有改選單文案時才要再打一次）
9. 想開放群組模式（被 @ 才回答，見批次 6）：LINE Official Account Manager →「設定 →
   回應設定」把「允許加入群組/多人聊天」打開，才有辦法把官方帳號拉進既有的記者群。
   沒開這個，LINE 根本不會讓人邀請帳號進群組，跟程式碼無關

### 記者端的入口

- 記者會現場：該場專屬 QR，內容 `https://line.me/R/oaMessage/{LINE_BASIC_ID}/?%23{活動代碼}`
  （`%23` 是 `#` 的百分比編碼；`@` 與中文要 `encodeURIComponent`）
  掃碼後輸入框已預先帶好代碼，按送出即綁定該場
- 平時：一般加好友連結／QR，不帶參數，直接走意圖路由

---

## 8. 明確不要做的事

1. **不要做「兩邊同步」。** 單一資料來源，見第 1 節。任何時候你想寫一個 sync function，就是走錯路了。
2. **不要用 LINE 內建的「AI 聊天機器人（β）」。** 知識庫綁在 LINE 後台、不能多活動切換、問答不會進 Google Sheet——後台分析、露出交叉、媒體訓練全部失效。而且醫療／金融／宗教／政治行政等業種可能被 LINE 擋，本平台場次常涉及智慧醫療。
3. **不要在前三批就申請「工研院」認證官方帳號。** 認證要用法人身分送件、先買專屬 ID（年費約 756 元），一旦掛上機構名義，改一句文案都要跑品牌／法務／資安／個資的流程。先把批次 3 做出來當可運作原型，用數據去談。
4. **不要做推播行銷。** `LINE_ADMIN_USER_ID` 的邀訪通知是推給使用者自己，不是推給記者。一旦開始群發，官方帳號被封鎖或靜音的機率大增，這條通道就毀了。
5. **不要引入向量資料庫／embedding／RAG 框架**（坑 7）。
6. **不要存記者的 LINE 顯示名稱與大頭貼。** 只存 `userId` 與自報的媒體名稱。歡迎訊息要有一句告知：「本帳號會記錄您的提問內容以改善新聞服務，不會蒐集您的個人資料。」
7. **不要把 `LINE_CHANNEL_ACCESS_TOKEN` 寫進任何前端檔案。** 只能待在 Vercel 環境變數與 `api/`／`lib/` 裡。

---

## 9. 人類的最後待辦

1. **Anthropic Console 的每月 spend limit 要先設好**（FIX-PLAN.md 最後一節第 1 項）。常設窗口不像單場記者會有天然的流量上限，程式端限流只是 best-effort。
2. `api/line.js` 的限流用 `line_user_id` 當 key，**不要用 IP**——LINE 的 webhook 全部來自 LINE 伺服器 IP，用 IP 當 key 等於全部記者共用一個額度，會誤殺。其餘比照 `api/chat.js` 的 `ipHits`，60 秒 15 次。
3. **邀訪功能上線前，先決定自己的回覆 SLA 並寫進文案。** 收了單沒回比沒有這個功能更傷。
4. **想辦法讓其他處室的記者會也進到 `events` 表。** 行事曆能不能撐起「工研院的媒體窗口」這個定位，全看這件事。現成機制是後台的「同仁編輯連結」（每場一組 `edit_code`，同仁不用密碼、只能改自己那場、問答數據仍全部回流），缺的是把它變成流程慣例。**這是唯一不能靠寫程式解決的一件。**
5. **驗證指標不是問答數，是回訪率。** 平時無活動的日子流量本來就低，一個月沒動靜是正常的，不要提早判死。要看的是同一個 `line_user_id` 有沒有在**不同場次**都出現過——那才證明「常設窗口」這個定位成立。
